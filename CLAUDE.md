# Radiant by Provenance — Repository Operating Manual

Vite + React 19 single-page app. No TypeScript. All styling is inline JSX + CSS variables from `index.css`.

For historical phase notes (every per-phase completion entry from Phase 1 through Phase 13), see [`CLAUDE-phase-log.md`](./CLAUDE-phase-log.md). For the architectural model, see [`architecture-spec.md`](./architecture-spec.md). For the open / partial / deferred work queue, see [`polish-backlog.md`](./polish-backlog.md). For the next-conversation foundation upload checklist, see [`ROUND-13-CONTEXT.md`](./ROUND-13-CONTEXT.md).

**Commands:**
- `npm run dev` — development server
- `npm run build` — must pass clean before shipping any change

---

## Architecture

The canonical source of truth for the platform architecture is `architecture-spec.md` in the repo root. Read it first; return to this file only for repo conventions and the (archived) phase history.

The architecture rests on two foundational rules:

1. **Assets are distinct from Claims.** Claims *reference* Assets; they don't *contain* them. Assets are first-class parent-layer nodes.
2. **Evaluation Agreements gate Claim visibility.** A Disclosure Agreement alone grants visibility; the paired Evaluation Agreement is what pulls another party's Claim onto your canvas.

There is one edge type (the Agreement Edge). Ownership, Proof-of-Evaluation, and Public Directory relationships are all modeled as implicit Disclosure Agreements on the same edge primitive. See spec §4 for the rationale.

### File layout

- `src/v2/` — application code (V2App, V2Canvas, DirectoryLayer, boot screen, PrimeRadiant, data model).
- `src/v3/` — archived V3 reference (UI patterns only, not active).
- `src/components/modals/` — modal components.
- `src/components/DetailPanel/` — node + agreement detail panels.
- `src/v2/animations/` — animation primitives (`reveal.js`, `unravel.js`).
- `src/assets/`, `src/index.css` — shared styling + static assets.
- Entry points: `v2.html` (primary), `v3.html` (archived reference).

### Demo actors

- **Bob Donloe** @ GovCo (buyer) — DOT: `DONLOE.BOB.J.1384297560`
- **Alice Nakamura** @ MicroCo (supplier)
- **Carol** @ AuditCo (auditor)
- **Dave** @ ChipCo (supplier — added Phase 11A; switchable role since 11C)
- Role switching via user menu; does not replay boot animation.

### Boot sequence

CAC login → Prime Radiant 3D → golden ripple → network build animation. Session storage key: `radiant-v2-booted`.

---

## Working Conventions

### Code style

- CSS variables always; never hardcode colors. Use `color-mix()` for alpha blends.
- SVG icons only. Unicode symbols (✓, ×, ▸, ◂, ■, ◆, ◇) are acceptable. No emojis.
- Timestamps use `date` + `dateTime` fields. Display format: `YYYY-MM-DD · HH:MM UTC`.
- Escape key handling: input/textarea focused → blur only; editor mode → exit to view; nothing focused → close modal.
- Click-to-copy on all PIN displays with visual feedback.
- Type labels (`ASSET`, `CLAIM`, `PARSE RESULT`, `EVAL RESULT`) in mono font, informational only — see spec §3. State badges (`PROVISIONAL` / `DECLINED` / `SUPERSEDED` / `NEW`) render as separate inline badges.

### UX patterns

- **Accept/decline in modals, not notifications.** Party-to-party action responses (accept, decline, amend, etc.) open a modal for the user's decision. The notification is the *entry point* — it surfaces that an action is required — but the decision itself happens in a modal following the Disclosure Response pattern (`CombinedResponseModal`). Do not embed accept/decline buttons inline within notification rows. When declining, the decline-reason textarea lives inside the modal, not inline.
- **Picker defaults + scroll containers.** Multi-select artifact pickers that require at least one selection (Assets, Claims, Requirements Sets, Eval Results, etc.) default to zero selected — force the user to make an explicit choice. Empty-state help text uses amber italic inline styling and states the requirement ("Select at least one Asset to continue."). Picker lists render inside scroll containers (`max-height` + `overflow-y: auto`, typically 240–320px) so they scale to N>>10 without breaking the modal layout. Optional pickers (e.g., evaluation evidence, which can be run as self-attestation) may sensibly pre-select, but default to zero whenever selection is required.
- **Reciprocal notifications for all party-to-party actions.** Every party-to-party action that requires counterparty acknowledgment (accept, decline, cancel, amend, etc.) MUST fire reciprocal notifications on each state change. A transfer request creates `v22-transfer-request` on the recipient; acceptance creates `v22-transfer-accepted` on the sender; decline creates `v22-transfer-declined`; cancellation creates `v22-transfer-cancelled`. Both sides see every transition in their notification chrome. Missing reciprocal notifications are bugs regardless of whether the state change is visible elsewhere in the UI.

### Autonomous workflow

- Work at `xhigh` effort.
- Perform a structured review against the task's acceptance criteria before declaring a change complete.
- **Runtime verification is required** for any change that touches a React component. Start the Vite dev server (`npm run dev`), confirm the app loads with no console errors, and exercise the affected flow. Build success and pure-data sanity scripts are insufficient — they don't catch TDZ errors, hook-rule violations, or other init-time exceptions. If a real browser isn't reachable, fall back to a JSDOM render check.
- When you hit a genuine ambiguity (a contradiction in the spec, a missing piece of information required for a correct decision), **stop and surface it.** Do not guess.
- The V2Canvas 3D raycaster does not respond to DOM-dispatched events — scripted UI walkthroughs of canvas-click flows are not possible from the agent session. Manual mouse interaction is the canonical verification path for canvas clicks; data-layer probes + module-load verification are the structural backstops. This limitation has been documented since Phase 9A.6.

### Documentation conventions

- **Per-phase completion notes** go into [`CLAUDE-phase-log.md`](./CLAUDE-phase-log.md) under a new `### Phase X.Y completion notes (YYYY-MM-DD) — short title` heading. Each note covers what shipped, deviations from the brief, fold-ins, runtime verification, known scope boundaries, and a `**Status:** [x] Complete.` footer.
- **Spec changes** go into `architecture-spec.md` Changelog section as a `- **§X.Y — Phase Z:** ...` bullet. Substantial new behavior gets a new subsection inline at the relevant section.
- **Open backlog items** go into `polish-backlog.md` topic sections with `Status` + `Effort` fields. Completed items move to the bottom-of-file `## Completed` section preserving full Status entries.
- **In-app Changelog modal** entries go in `src/v2/V2App.jsx`'s Changelog releases array, prepended above the previous version. Footer version constant in V2App.jsx is also bumped.
- **Cross-role notification fan-out** is documented in `architecture-spec.md` §7.4 as new rows in the notification table.

### Git workflow

**Phase commits go directly to `main`. Do not create feature branches for prototype work.** Worktree-based agent sessions must fast-forward into `main` and push at the phase boundary; the side branch (if any) is deleted afterward. This is the established workflow for this prototype — every phase from 12.4 through 15.1.1 has shipped this way.

---

## Current state of the world

- **Footer version:** v0.15.8 (bumped during Phase 15.5 — patch increment: VReg Re-Run demo simplified into single Eval Result + 5 SAT + 2 MISSING narrative arc).
- **Last shipped phase:** Phase 15.5 — VReg Re-Run Demo Simplification. VReg evaluation seed redesigned into a coherent demo narrative arc: prior eval shows 5 SAT + 2 MISSING criteria → Alice amends with Test Report → Bob's re-run "discovers" values for the missing rows in the new evidence → save → 7/7 SAT → create PoE. (1) **Chain collapsed** — Phase 13.2's V0/V1/V_main supersession chain on VReg removed entirely (`erBobVregV0`, `erBobVregV1`, `daProofBobVregV0/V1`, `daOwnEvalBobVregV0/V1` all deleted; registries trimmed; Phase 13.2 chain comment removed). (2) **7 rows in `erBobVreg`** — 5 SAT (req-001 through req-005, Datasheet anchors only) + 2 MISSING (req-006 SEL immunity, req-007 Burn-in qualification, Test Report anchors via narrowed demo trick). (3) **req-004 value** updated `TID > 100 krad(Si)` → `TID ~ 80 krad(Si)` for visual coherence with the Datasheet PDF (prototype RS doesn't encode threshold logic; SAT label independent). (4) **Test Report PDF** rewritten as a 1-page focused supplementary document — 2 sections (SEL Immunity + Burn-in Qualification), 2 anchored values, replacing the redundant 2-page Phase 15.3/15.4 version. evidenceAnchors.js auto-regenerated. (5) **Demo trick narrowed** to req-006/007 only (was req-001-005 in 15.4); the 5 SAT rows reference Datasheet exclusively. (6) **Walkthrough doc** Sections 3 + 5 rewritten with the new narrative; Section 5 includes the explicit "Bob updates the missing rows then saves" sub-flow leading to 7/7 SAT and PoE creation.

- **Phase 15.4 (prior phase):** Re-Run Demo Seed Correction + PDF Value Parity Fix. Two Phase 15.3 QA followups; closes a demo-design conflation surfaced after 15.3 shipped. (1) **VReg Claim reverts to single-Asset initial seed** — `cVreg.referencedAssetIds` and `daAliceToBobVreg.scope` revert from `[Datasheet, Test Report]` to `[Datasheet]`. PRM remains the canonical multi-Asset Expand modal demo; VReg's Expand modal goes back to `Asset 1 of 1`. The Test Report Asset stays defined as Alice-owned-but-unattached, surfacing in her amend Asset picker. (2) **Demo trick** — `erBobVreg.evidenceUsed` reverts to `[Datasheet]` (what was actually evaluated) but `results[].evidenceAnchors[]` arrays retain references to BOTH Datasheet and Test Report. When Alice attaches Test Report during the amend prereq, Bob's re-run accordion renders annotations against the pre-stamped anchors — enables the amend-then-rerun-with-annotations narrative without contriving a prior eval that already used Test Report. In production this would violate the anchors-must-reference-evidenceUsed-Assets invariant; accepted for prototype demo purposes. Inline comment block in `v2_2Data.js` flags the trick. (3) **Compliance Notes Asset retired** — `aVregComplianceNotes` definition deleted; `microco-vreg-compliance-notes.pdf` deleted; PDF generator spec entry removed (paragraphs[] page-spec affordance preserved for future docs PDFs). (4) **PDF value parity verified** — `microco-vreg-test-report.pdf` req-004 already shipped 15.3 with `'TID > 100 krad(Si)'` (matches chain-head SAT value); the "TID ~ 80 krad(Si)" string lives only in the Datasheet PDF (correct as published spec) and `erBobVregV1` (correct as historical superseded value). (5) **Walkthrough doc** — Section 2 reverts to single-Asset; Section 5a names Test Report with demo-trick rationale; Section 5b describes 2-Asset accordion.

- **Phase 15.3 (prior phase):** Re-Run Demo Seed Enhancement + EVIDENCE Rename Followup. Two Phase 15.2 QA followups. (1) **VReg Claim now multi-Asset** — new `aVregTestReport` Asset (Alice-owned, pre-attached to `cVreg.referencedAssetIds`) anchored against `erBobVreg` chain-head on all 5 MIL-PRF-55681 v1 requirements alongside the existing `aVregDatasheet`. Each `results[]` row's `evidenceAnchors[]` now carries 2 entries (datasheet + test report). Asset switcher in Expand modal flips between annotated PDFs; Re-Run flow accordion inherits multi-Asset annotations via `priorActiveResult.results`. Test report PDF values authored at parity with seed strings (5.0V ±0.4% etc.). Predecessor V0/V1 left single-Asset (preserves chain causality). (2) **`aVregComplianceNotes` floating Asset** — Alice-owned but NOT pre-attached, surfaces in the amend Asset picker as the named candidate for the Re-Run amend prerequisite step. Paragraph-only PDF (no anchors). PDF generator script extended with optional `paragraphs[]` page-spec field for documentation Assets. (3) **EVIDENCE → ASSETS rename** in V22RunEvaluationModal Δ delta info box — the missed surface from the 15.1.2 sweep. (4) **Walkthrough doc updates** — Section 2 rewritten as multi-Asset, Section 5a names Compliance Notes Asset with rationale, Section 5b describes three-Asset accordion. `daAliceToBobVreg` scope extended to include both VReg Datasheet + Test Report so Bob has full disclosure to both pre-existing Assets.

- **Phase 15.2 (prior phase):** Download Fix + Demo Walkthrough Guide + Legacy PDF Cleanup (#172 part 3 close). Three deliverables, closes the #172 PDF annotation arc. (1) **Download icon button fix** — the Phase 15.1.2 button shipped disabled because the spec premise (a pre-existing Phase 13.4 onClick handler ready to bind) was wrong; the original `<DownloadButton>` placeholder has always been disabled with `title="Export coming soon."`. Phase 15.2 introduces the first working Download in the eval-output / poe surfaces: `<DownloadIconButton>` accepts an optional `onClick` prop and renders via the shared `<Tooltip>` primitive (was native `title=""`). `EvalResultOutputBody` wires `handleDownloadJson` (Blob from `getEvalResultJsonRecord` → transient `<a download="eval-result-<id>.json">` click → revokeObjectURL on next tick); PoE schema inherits via the shared `<EvalResultOutputBody>`. (2) **Demo walkthrough guide** — `docs/PHASE-15-DEMO-SCENARIOS.md` rewritten from skeleton into a complete six-section walkthrough covering all four scenarios (PRM Eval, VReg Eval, PoE, Run Eval Re-Run) with role-switch steps, navigation paths, expected outcomes, tryable interactions, and the Re-Run prerequisite (Alice amends VReg Claim before Bob's re-run, gated by `hasNewAssetsForRerun`) documented explicitly. (3) **Legacy PDF cleanup** — 5 unreferenced `/public/*.pdf` files removed (connectorassembly, pcbsubstrate, powerregulationmodule, thermalinterfacepad, voltageregulator); 4 still-referenced legacy PDFs (emishielding, prm-3a-ic-datasheet, prm-3a-ic-qualification-report, voltage-reference-ic-datasheet) retained — surfaced as a Phase 15.3 candidate to migrate to MicroCo seed-pdfs/ flow OR trim from seed (acceptance criterion #6's "only three" wording was overstated; removing referenced PDFs would silently break Asset previews).

- **Phase 15.1.2 (prior phase):** Eval Result + PoE Expand Modal Layout Consolidation. Three chrome-cleanup refinements from 15.1.1 QA. (1) **Canonical modal header carries metadata** — Eval Result Expand modal header gains `Evaluated: ... · Evaluator: ...` line below the title; PoE gains `Created: ... · Owner: ...`. Phase 15.1.1's thin header band inside the Output body is removed (~52px reclaimed). (2) **Download icon button** — promoted into the right-panel "EVALUATION RESULTS" title bar as a 24×24 icon button at right edge (download glyph + tooltip "Download Evaluation Results JSON" — wired up in 15.2). Healthbar block now hosts aggregate + minibar only. (3) **File info repositioning + Owner/Registered merge** — left panel reads `[ASSET title bar] → [file viewer] → [combined 6-row metadata block: Filename, Size, MIME, Hash, Owner, Registered]`. AssetEvidencePanel refactored: new `AssetFileViewer` + `AssetFileMetadata` (variant: `file-info` | `combined` | `owner-row`) sub-component exports; `AssetEvidenceViewer` default export composes them in legacy order so every existing call site is unaffected. (4) **EVIDENCE → ASSET rename** in eval-output / poe Output tab left-panel title bars; amber accent unchanged. (5) **PoE Output cleanup** — full-width PROOF OF EVALUATION info box removed (canonical header covers it now); "Final Evaluation" SectionHeading dropped; PoE Output body becomes structurally identical to Eval Result Output body. Evaluation Provenance section heading retained.

- **Phase 15.1.1 (prior phase):** Annotation Numbering + Shape + Right-Panel Layout. Three contained refinements on top of 15.1: (1) annotation labels simplified — dropped the `{assetOrdinal}.` prefix; PDF dots, right-panel row indicators, and Run Evaluation review rows now show only `{rowOrdinal}` (per-RS, 1-indexed). Same row keeps the same number across Asset switches; RS color coding distinguishes RS membership. The `assetOrdinal` prop on `AnnotatedPdfViewer` preserved in the API but no longer renders in the label — reserved for future compound-label scenarios. (2) Annotation shape — rounded rectangles (32×22, borderRadius 6) replace the 26px circles in the PDF overlay; right-panel row indicators + Run Evaluation review-row indicators picked up the rounded-rectangle treatment at 28×20 to match. (3) Eval Result Expand modal Output tab layout — healthbar (SAT/UNSAT/MISSING minibar + aggregate count) + Download button moved out of the full-width header into the right panel's top section; thin header band above side-by-side row keeps only name + dates + evaluator. New "EVALUATION RESULTS" title bar in the right panel mirrors the left panel's "EVIDENCE" strip (amber accent, same height + padding). PoE Expand modal Section 1 inherits the same layout reorganization automatically (it calls `<EvalResultOutputBody>`). Global `<TabHeaderActions>` Download suppressed on the Output tab for `eval-output` + `poe` schemas (right-panel Download is canonical there); JSON tab keeps it for all schemas.

- **Phase 15.1 (prior phase):** Annotation Visual Redesign + Bidirectional Interaction (#172 part 2 of 3). Each evidence anchor now renders two visual elements: a translucent highlight rectangle (RS color, 15% opacity) over the cited text + a numbered indicator placed immediately to the left of the rect. When highlighted, rect bumps to 30% opacity, indicator picks up a 3px ring in RS color. Side-by-side layout for Expand modal Output tab (eval-output + poe): PDF + indicators left (~60%, sticky), per-RS results tables right (~40%); below 900px viewport collapses to vertical stack. Each results-table row gains a numbered indicator on the left; rows with empty `evidenceAnchors[]` get an empty indicator slot. Bidirectional interaction: row click scrolls PDF + highlights matching dot; dot click scrolls results table + highlights matching row; cross-Asset clicks auto-flip the multi-Asset switcher first then scroll once the new PDF mounts. Same interaction extended to V22RunEvaluationModal Step 2/3 review surface (accordion left panel auto-flips on cross-Asset row clicks). New `src/v2/data/anchorIds.js` utility for synthesized IDs (`{sourceAssetId}|{requirementsSetId}|{requirementId}|{page}|{x}|{y}`) shared between dot + row consumers. **Step 0 outcome**: VReg re-run gated by `hasNewAssetsForRerun` until Alice amends the Claim — acceptable demo prerequisite per docs/PHASE-15-DEMO-SCENARIOS.md, no seed adjustment.

- **Phase 15.0.1 (prior phase):** Annotation Diagnosis + Layout Fixes + Git Catchup. Bug fix: the `makeEvaluationResult` factory in `src/v2/v2_2Data.js` was silently dropping `evidenceAnchors` from each result row even though Phase 15.0's seed authoring populated them. Factory now preserves the field; all four target surfaces (eval flow, Eval Result expand Output, PoE expand Output, Re-Run flow Step 2) show dots correctly. PDF fit-to-width: AnnotatedPdfViewer computes the render scale from the host container's `clientWidth` at load time (capped 1.6×), eliminating horizontal scroll. Multi-Asset switcher: Previous/Next arrow controls in the expand modal Output tab flip between displayable Assets. New `docs/PHASE-15-DEMO-SCENARIOS.md` documents the four QA scenarios. **Workflow note recorded:** runtime probes verify components, not integration — Phase 15.0 shipped with the viewer correctly verified standalone but never opened the actual Detail Panel → Expand → Output flow with seed data, which would have caught the factory bug immediately. **Git catchup:** Phases 12.4 → 14.5 + 15.0 pushed to GitHub as two commits (catchup + 15.0); the repo had 13 unpushed phases since v0.12.3 when 15.0.1 began.

- **Phase 15.0 (prior phase):** PDF.js Integration + Annotated Evidence Seed Content (#172 part 1 of 3). Three branded MicroCo PDFs (PRM-3A Datasheet, PRM-3A Test Report, VReg-12C Datasheet) generated by `scripts/generate-seed-pdfs.mjs` (deterministic pdf-lib script that also emits a coordinate-map JSON in lockstep, so seed and PDFs never drift). Eval Result `results[]` rows gain `evidenceAnchors[]` with `{ sourceAssetId, page, x, y, w, h }` per anchor. New `<AnnotatedPdfViewer>` component renders PDFs via pdfjs-dist v5 and overlays numbered dots at the anchored coordinates with per-Requirements-Set color coding. `usePdfJs` opt-in on AssetEvidencePanel applies to the three target surfaces (Run Evaluation modal Step 1, Eval Result expand Output, PoE expand Output); iframe rendering preserved everywhere else. 2-Asset / 2-RS demo: Bob's PRM Eval Result references both PRM Datasheet + PRM Test Report Assets; MIL-PRF and System Integration RSes. Static rendering only in 15.0 — Phase 15.1 will wire interaction. **Implementation note:** pdfjs-dist v5 changed the render API to use `canvas` (not `canvasContext`); the legacy parameter still paints the canvas but the returned promise never resolves on v5 — render task hangs indefinitely. `BadgeShieldIcon`'s `strokeColor` halo trick is unrelated.

- **Phase 14.5 (prior phase):** Chip Container Visual Tuning (#176c). Six tuning corrections to Phase 14.4's chip container. (1) **2px halo on each shield** stroked in the rectangle's exact background color (`color-mix(in srgb, var(--accent-indigo) 18%, var(--bg-card))`). When shields overlap, the halo reads as negative space — produces the classic overlapping-tokens cut between adjacent shields. (2) **Shields shrink 20 → 18px** to fit the halo without growing the rectangle. (3) **STEP_IDLE 15 → 12** for a more pronounced ~33% overlap. (4) **Shields vertically centered** via `top: (HEIGHT - SHIELD_SIZE) / 2`. (5) **Single-shield case** centers the lone shield horizontally + vertically inside a 24×24 square-ish rectangle (no fan-out branch since a single shield can't un-overlap). (6) **STEP_FAN 24 → 22** (= 18 + 4px gap; 4px gap unchanged). Container background, border, shadow, fan-out animation timing, and tooltip behavior all unchanged. `BadgeShieldIcon` gained an optional `strokeColor` prop rendering a wider halo path beneath the silhouette path; `overflow: visible` on the SVG so the halo doesn't clip the viewBox.

- **Phase 14.4 (prior phase):** Chip Container Visual Polish (#176b). Single-purpose visual polish on top of Phase 14.3's `BadgeChipContainer`: dropped the circular wrappers around each shield and the pill wrapper around the "+N" indicator. The shield silhouette is now the visual element (no chrome behind it); "+N" renders as plain indigo monospaced text. Shields scaled 16px → 20px to fill the freed space; container height adjusts 22px → 26px. STEP values rescaled proportionally — STEP_IDLE 12 → 15 (75% of 20px for 25% overlap), STEP_FAN 20 → 24 (20px + 4px gap). Container background / border / shadow + fan-out animation timing + tooltip behavior all unchanged.

- **Phase 14.3 (prior phase):** Issue Badge Modal Scope Fix + Chip Container Refinement (#176a). Two contained corrections to Phase 14.2's Badge work plus a backlog filing. (1) **Issue Badge picker** restricted to the current actor's own Badge Templates only (was showing every actor's templates in a sectioned picker; only the owner of a template can issue against it, so cross-actor templates were never legitimately issuable). The picker collapses to a single un-sectioned scroll list of own templates with the latest-version SUGGESTED auto-promotion preserved. The empty-state copy ("Create one in the Library » Badges tab first.") is reused. (2) **Chip container refactor** (#176a) — replaced independent absolute-positioned chips + a separate "+N" pill with a single rounded-rectangle container matching the NEW pill's visual treatment (4px border-radius, indigo-tinted background, subtle shadow). Hover fan-out (when 2+ shields): rectangle expands leftward with 180ms ease-out transition; rightmost shield + "+N" stay anchored to the right edge; previously-overlapped shields slide left to un-overlap with 4px spacing. Per-shield tooltip reveals badge name + version (line 1) + issuer party (line 2); "+N" tooltip lists buried badges as `Badge Name · Issuer Party` rows. Both tooltips auto-flip below the anchor when insufficient vertical space above. Click guard via `stopPropagation` on the chip rectangle so clicks don't trigger card-level interactions. (3) **Backlog filing** — Netgraph cleanup item (#4) gains a "Child-layer burial rules (Phase 14.3 design notes)" sub-section capturing burial candidates per node type (Parse Results burial-safe under parent Asset; Eval Results + PoEs depend on Cascading Disclosures #26 ordering). New file `src/v2/BadgeChipContainer.jsx` (~190 lines) extracted from `AssetNode.jsx`. `_activeBadges` decoration in `V2App` enriched from raw badge-issuance objects to `{ id, badgeName, badgeVersion, issuerParty }` shape so tooltips render without a second prop chain through V2Canvas.

- **Phase 14.2 (prior phase):** Badge Architectural Correction + PoE Notification Regression Fix. Two architectural shifts that correct misalignments from Phase 14.1: (1) badges target **Claims**, not PoEs (the Claim is what earns the badge; PoEs that wrap qualifying Eval Results display via aggregation); (2) issuance gate becomes `issuerParty !== claim.ownerParty` (any party with disclosure access can issue except the Claim owner — enables OSHA-style third-party endorsement of self-evaluations). Plus a regression fix: `handleV22CreatePoE` now fires `v22-poe-created` notification on the Claim owner's inbox (Phase 13.1 wired visibility but missed the notification). PoE visibility itself was confirmed working correctly via empirical probe — the proof-of-eval DA's grantee=Claim owner makes Bob's PoE appear on Alice's canvas. **Eliminated the standalone Badge Issuance Detail Panel** (#169b) — Detail Panel over Detail Panel violated the prototype's overlay conventions; Badge Issuance row clicks now open the expand modal directly (modal over Detail Panel is the correct pattern). Issuance entry points expanded to Claim Detail Panel + Claim node action bar. PoE Badges section gains "Badges earned by [Claim name]" subtext (clickable). Notification recipient routing for all three badge notification types now resolves via Claim owner (was PoE owner — same in most cases, OSHA scenario newly correct). New `_claimOwnerParty` decoration on PoE nodes drives the action-bar gate. JSON record + dispatcher signature: Badge Issuance now carries `targetClaimId` (was `targetPoeId`); dispatcher context shape is `{ claims, badgeTemplates }` (was `{ poes, badgeTemplates }`). Filed new backlog item **#182** — Amend Claim to include a PoE as an Asset (deferred to a future phase). Closes Badges work. New `makeBadgeIssuance` factory + 3 aggregation helpers (`getBadgesForPoE`, `getBadgesForClaim`, `getBadgesForRecipient`) in `src/v2/v2_2Data.js`. Recipient is NOT stored on issuance — derived at render time from `targetPoe.ownerParty` (single source of truth). Self-issuance is blocked at action bar + Detail Panel footer + IssueBadgeModal as final guard. Two new modals: IssueBadgeModal (two-step picker + description) and RevokeBadgeModal (single-step required reason). Three new notification types: `v22-badge-issued`, `v22-badge-revoked`, `v22-badge-template-new-version` (informational fan-out hooked into `handleSaveBadgeTemplate` from Phase 14.0). Card chip rendering on PoE + Claim node cards via shared `BadgeShieldIcon` primitive: top-right corner, max 3 visible at 25% horizontal overlap, +N indicator when > 3, positioned LEFT of NEW label when both present. Per-node `_activeBadges` decoration computed in `v22DataWithReveal` memo. Three Detail Panel sections: PoE Badges section now populated (was placeholder since Phase 13), new Claim Badges aggregated section (walk: claim → eval results → PoEs → badges), new Actor "Badges Received" section (filtered to received-only). New `V22BadgeIssuancePanel` standalone Detail Panel + `'BADGE ISSUANCE'` router entry; mounts as separate overlay (z-index 220) since Badge Issuances aren't canvas nodes — selection via independent `v22BadgeIssuanceSelected` state. Active Issuances section in Badge Template Detail Panel populated (was placeholder in 14.0): per-version list + lineage-wide subtext rollup. Expand modal coverage via new `'badge-issuance'` schema. **Architectural:** `getJsonRecordFor` dispatcher now accepts an optional `context` parameter for cross-artifact reference resolution; Badge Issuance uses it to derive recipient + lineage fields (clearly marked with `_computed_` prefix in the JSON record). Seed data: 5 Badge Issuances exercising all surfaces, with PRM Claim aggregating 4+ badges to render +N overflow on the Claim card chip on first load.
- **Active phase queue:** Phase 14 complete; Phase 15 closed across all three parts of #172 plus polish + demo enhancement + corrections + simplification — 15.0 (PDF.js infrastructure + seed PDFs + static dots), 15.0.1 (hotfix + multi-Asset switcher), 15.1 (annotation visual redesign + bidirectional interaction), 15.1.1 (annotation numbering + shape + right-panel layout reorg), 15.1.2 (Eval Result + PoE Expand modal consolidation), 15.2 (Download fix + walkthrough guide + legacy PDF cleanup), 15.3 (Re-Run demo seed enhancement + EVIDENCE rename followup), 15.4 (Re-Run demo seed correction + PDF value parity verified), 15.5 (VReg Re-Run demo simplification — chain collapsed, 5 SAT + 2 MISSING narrative). **Next cycle:** backlog hygiene (filing Phase 14.6 + items H/I/J), then transition to a fresh thread. **Phase 15.6 candidate (carried from 15.2):** migrate the 4 retained legacy `/public` PDFs (emi, prm-3a-ic-datasheet, prm-3a-ic-qualification-report, voltage-reference-ic-datasheet) to MicroCo seed-pdfs/ flow OR trim from seed. Backlog hygiene cycle queued after 15.2 closes. **Phase 16+:** Directory Layer (#43, #45, #47, #132, #162, #163), then AI Shopper (#47 overlap, #123), Cascading Disclosures (#26), Detail Panel UI cleanup (#58, #104, #116, #161+, #180), Netgraph cleanup (#4, #130), Search/aggregate (#27), Network Event Log (#30). **Phase 14+ priority order (strict ordering):** (1) ~~Badges #169~~ ✓ → (2) Directory Layer #43, #45, #47, #132, #162, #163 → (3) AI Shopper #47 (overlaps), #123 → (4) Cascading Disclosures #26 → (5) Detail Panel UI cleanup #58, #104, #116, #161+, #180 → (6) Netgraph cleanup #4, #130 → (7) Search/aggregate #27 → (8) Network Event Log #30. Other deferred follow-ups: #167 (DA amendment-as-proposal model — extension of #164), #170 (consolidate `assetReferences[]` / `referencedAssetIds[]`), #172 (PDF annotation overlay — depends on #41), #174 (unify proof-only DA scope shape — discriminated-union is a stepping stone), #180 (substantive Output content for DA / EA / Parse Result expand modals), #181 (user-uploaded Badge Template graphics).
- **Disclosure types fully wired end-to-end:** Full, Selective, Proof-only — all three branches of grantee view derivation (canvas + edges + Detail Panel + Expand modal) shipped through Phase 11D.
- **Animation primitives:** `playUnravelAnimation` (Phase 9D.2.x — node leaves canvas) and `playRevealAnimation` (Phase 11C.3 — provisional → active flip). Both live under `src/v2/animations/` and are await-able promises returning at phase 'done'.
- **Layout grid:** all node X / Y / column / hierarchy-shift constants are multiples of 100 (Phase 10.2.1 grid alignment); `symmetricRowY(i)` helper distributes rows alternately around y=0; per-column `COL_Y_OFFSET = 100` separates adjacent column traffic lanes.
- **Chrome surfaces:** notification bell (with unread badge + cross-role amber dot per Phase 11D), Library button (unified Parsing Templates + Requirement Sets + Published Requirements per Phase 10.3), AI Shopper magnifier, Radiant Network globe (toggle Directory Layer entry/exit), Credits pill, user menu (Switch User dropdown with per-role amber dot for cross-role notifications), Theme toggle.
- **Detail Panel surfaces:** V22ActorPanel / V22AssetPanel / V22ClaimPanel / V22ParseResultPanel / V22EvalResultPanel (in `V22NodeDetailPanel.jsx`), DisclosureAgreementDetailPanel, EvaluationAgreementDetailPanel. All node panels carry an Agreements section (Phase 9C). Modal Expand affordance restored Phase 11B + 11D.x.

---

## Phase log

The full per-phase completion log lives in [`CLAUDE-phase-log.md`](./CLAUDE-phase-log.md). It contains every phase note from Phase 1 (V2.2 data model foundation, 2026-04-17) through Phase 13 (Proof of Evaluation node type, 2026-05-04), in chronological order.

The original V2.2 migration ran in eight phases:

- [x] Phase 1: Data Model Foundation
- [x] Phase 2: Parent Layer Restructure
- [x] Phase 3: Edge Clickability + Agreement Panels
- [x] Phase 4: Combined Request + Response Flows
- [x] Phase 5: Evaluation Flow + Eval Results on Parent Layer
- [x] Phase 6: Amendment Flows
- [x] Phase 7: Directory Layer + AI Shopper
- [x] Phase 8: Consolidation + Cleanup

Post-migration work shipped through Phases 9A → 9E-parallel.x, 9D.1 → 9D.2.x, 10.x, and 11A → 11E.1. See `CLAUDE-phase-log.md` for the full narrative.
