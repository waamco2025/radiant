# Radiant by Provenance — Repository Operating Manual

Vite + React 19 single-page app. No TypeScript. All styling is inline JSX + CSS variables from `index.css`.

For historical phase notes (every per-phase completion entry from Phase 1 through Phase 16.1.5), see [`CLAUDE-phase-log.md`](./CLAUDE-phase-log.md). For the architectural model, see [`architecture-spec.md`](./architecture-spec.md). For the open / partial / deferred work queue, see [`polish-backlog.md`](./polish-backlog.md). For the Round 17 entry doc (per-phase load-in lists, conventions, demo roles, current state), see [`ROUND-17-CONTEXT.md`](./ROUND-17-CONTEXT.md).

**Commands:**
- `npm run dev` — development server
- `npm run build` — must pass clean before shipping any change

---

## Architecture

The canonical source of truth for the platform architecture is `architecture-spec.md` in the repo root. Read it first; return to this file only for repo conventions and Round-level state.

The architecture rests on two foundational rules:

1. **Assets are distinct from Claims.** Claims *reference* Assets; they don't *contain* them. Assets are first-class parent-layer nodes.
2. **Evaluation Agreements gate Claim visibility.** A Disclosure Agreement alone grants visibility; the paired Evaluation Agreement is what pulls another party's Claim onto your canvas.

There is one edge type (the Agreement Edge). Ownership, Proof-of-Evaluation, and Public Directory relationships are all modeled as implicit Disclosure Agreements on the same edge primitive. See spec §4 for the rationale.

### File layout

- `src/v2/` — application code (V2App, V2Canvas, DirectoryLayer, boot screen, PrimeRadiant, data model).
- `src/v3/` — archived V3 reference (UI patterns only, not active).
- `src/components/modals/` — modal components.
- `src/components/DetailPanel/` — node + agreement detail panels.
- `src/v2/animations/` — animation primitives (`reveal.js`, `unravel.js`).
- `src/assets/`, `src/index.css` — shared styling + static assets.
- Entry points: `index.html` (primary, renamed from `v2.html` in Phase 8.5), `v3.html` (archived reference).

### Demo actors

- **Bob Donloe** @ GovCo (buyer) — DOT: `DONLOE.BOB.J.1384297560`
- **Alice Nakamura** @ MicroCo (supplier)
- **Carol** @ AuditCo (auditor)
- **Dave** @ ChipCo (supplier — added Phase 11A; switchable role since 11C)
- Role switching via user menu; does not replay boot animation.

### Boot sequence

CAC login → Prime Radiant 3D → golden ripple → network build animation. Session storage key: `radiant-v2-booted`.

---

## Working Conventions

### Code style

- CSS variables always; never hardcode colors. Use `color-mix()` for alpha blends.
- SVG icons only. Unicode symbols (✓, ×, ▸, ◂, ■, ◆, ◇) are acceptable. No emojis.
- Timestamps use `date` + `dateTime` fields. Display format: `YYYY-MM-DD · HH:MM UTC`.
- Escape key handling: input/textarea focused → blur only; editor mode → exit to view; nothing focused → close modal.
- Click-to-copy on all PIN displays with visual feedback.
- Type labels (`ASSET`, `CLAIM`, `PARSE RESULT`, `EVAL RESULT`) in mono font, informational only — see spec §3. State badges (`PROVISIONAL` / `DECLINED` / `SUPERSEDED` / `NEW`) render as separate inline badges.

### UX patterns

- **Accept/decline in modals, not notifications.** Party-to-party action responses (accept, decline, amend, etc.) open a modal for the user's decision. The notification is the *entry point* — it surfaces that an action is required — but the decision itself happens in a modal following the Disclosure Response pattern (`CombinedResponseModal`). Do not embed accept/decline buttons inline within notification rows. When declining, the decline-reason textarea lives inside the modal, not inline.
- **Picker defaults + scroll containers.** Multi-select artifact pickers that require at least one selection (Assets, Claims, Requirements Sets, Eval Results, etc.) default to zero selected — force the user to make an explicit choice. Empty-state help text uses amber italic inline styling and states the requirement ("Select at least one Asset to continue."). Picker lists render inside scroll containers (`max-height` + `overflow-y: auto`, typically 240–320px) so they scale to N>>10 without breaking the modal layout. Optional pickers (e.g., evaluation evidence, which can be run as self-attestation) may sensibly pre-select, but default to zero whenever selection is required.
- **Reciprocal notifications for all party-to-party actions.** Every party-to-party action that requires counterparty acknowledgment (accept, decline, cancel, amend, etc.) MUST fire reciprocal notifications on each state change. A transfer request creates `v22-transfer-request` on the recipient; acceptance creates `v22-transfer-accepted` on the sender; decline creates `v22-transfer-declined`; cancellation creates `v22-transfer-cancelled`. Both sides see every transition in their notification chrome. Missing reciprocal notifications are bugs regardless of whether the state change is visible elsewhere in the UI.

### Autonomous workflow

- Work at `xhigh` effort.
- Perform a structured review against the task's acceptance criteria before declaring a change complete.
- **Runtime verification is required** for any change that touches a React component. Start the Vite dev server (`npm run dev`), confirm the app loads with no console errors, and exercise the affected flow. Build success and pure-data sanity scripts are insufficient — they don't catch TDZ errors, hook-rule violations, or other init-time exceptions. If a real browser isn't reachable, fall back to a JSDOM render check. **Render-path branches with state-dependent code paths require LOD-swap + hover + selection + click exercises at every affected LOD**, not just a single screenshot at one zoom level — the Phase 17.0.1 → 17.0.2 TDZ incident (a state-mutation probe rendered the RFP branch but bypassed the actual render path the user would hit; a real zoom + click would have crashed at the first state transition) is the canonical example.
- **Seed-data probe pattern:** verify the data layer first, then the UI walkthrough. Standalone component probes verify the component in isolation, NOT the integration. Phase 15.0.1's factory bug (`makeEvaluationResult` silently dropping `evidenceAnchors`) shipped because the component was probed in isolation, never via the actual Detail Panel → Expand → Output user flow. Phase 15.6's auto-fill verification used the seed-data probe pattern (direct JS exec inside the dev server confirming the data transformation) and caught issues before declaring complete. Probe the path the user will actually take.
- When you hit a genuine ambiguity (a contradiction in the spec, a missing piece of information required for a correct decision), **stop and surface it.** Do not guess.
- The V2Canvas 3D raycaster does not respond to DOM-dispatched events — scripted UI walkthroughs of canvas-click flows are not possible from the agent session. The Directory Layer's Three.js Raycaster has the same limitation (documented since Phase 9A.6, re-confirmed in Phase 16.1.5). Manual mouse interaction is the canonical verification path for canvas clicks; data-layer probes + module-load verification are the structural backstops. Plan QA accordingly: mark canvas-click items as code-verified-only when they can't be exercised via scripted DOM events.

### Documentation conventions

- **Per-phase completion notes** go into [`CLAUDE-phase-log.md`](./CLAUDE-phase-log.md) under a new `### Phase X.Y completion notes (YYYY-MM-DD) — short title` heading. Each note covers what shipped, deviations from the brief, fold-ins, runtime verification, known scope boundaries, and a `**Status:** [x] Complete.` footer.
- **Spec changes** go into `architecture-spec.md` Changelog section as a `- **§X.Y — Phase Z:** ...` bullet. Substantial new behavior gets a new subsection inline at the relevant section.
- **Open backlog items** go into `polish-backlog.md` topic sections with `Status` + `Effort` fields. Completed items move to the bottom-of-file `## Completed` section preserving full Status entries.
- **In-app Changelog modal** entries go in `src/v2/V2App.jsx`'s Changelog releases array, prepended above the previous version. Footer version constant in V2App.jsx is also bumped for forward-progress phases.
- **Cross-role notification fan-out** is documented in `architecture-spec.md` §7.4 as new rows in the notification table.

### Footer version convention

- **Forward-progress phases roll the footer constant forward.** Phase 16.1.5 → footer reads `v0.16.1.5`.
- **Backtrack-hotfix versions leave the footer unchanged.** A backtrack-hotfix is a fix INSERTED into the Changelog modal between existing entries (e.g. Phase 14.6 inserted between 15.0 and 14.5 — the v0.14.6 entry sits in the modal between v0.15.0 and v0.14.5 in chronological order, but the footer stayed at v0.15.9 because the latest forward-progress phase was 15.6).
- **Round 16 caveat:** the "footer stays frozen" rule was over-applied across the entire 16.0.x cycle (footer held at v0.15.9 through 16.0.3). Phase 16.1.0 corrected this — those were forward phases and should have rolled the footer. The convention now: 16.0.x retroactively considered forward (but footer history kept as-is for the prototype), 16.1.0 onward rolls forward each forward-progress phase.

### Git workflow

**Phase commits go directly to `main`. The worktree feature is currently disabled — `git rev-parse --abbrev-ref HEAD` must return `main` before any code changes.** Do not create feature branches for prototype work. One Claude Code prompt = one phase = one commit. Phase boundaries get commits without exception, even within a single Claude Code session.

If a worktree session is in use (older convention), it must fast-forward into `main` and push at the phase boundary; the side branch is deleted afterward. Phase 16.1.4 shipped this way; Phase 16.1.5 onward shipped commit-to-main directly.

---

## Current state of the world

- **Footer version:** v0.18.3.1.1 (rolled forward during Phase 18.3.1.1).

- **Last shipped phase:** **Phase 18.3.1.1 — Directory action-bar: hide Issue Badge, surface Reject Solicitation.** Two surgical fixes closing 18.3.1 QA #8 findings on the Directory Claim-card action bar. **(Fix 1 — `src/v2/AssetNode.jsx` CLAIM branch):** the non-owner Issue Badge `buttons.push` was unconditional, so it surfaced on every Directory Claim card and no-op'd on click — Issue Badge dispatches through `onV22CardAction` → `onClaimCardAction` for Directory cards, but that handler has no `'issueBadge'` branch (no anchored Asset / parent-canvas position to attach a badge to). Gated behind a new `isDirectoryMountedClaim = !!(node._directoryWarmPathRequestCandidate || node._directoryExistingEa || node._directoryRequestEaCandidate || node._solicitationContext)` check — the Directory adapter applies these stamps and the parent-canvas adapter does not — so Issue Badge stays on parent-canvas non-owner Claims (Phase 14.2 wiring intact) but disappears on Directory cards. **(Fix 2 — `src/v2/DirectoryLayer.jsx` card adapter + `src/v2/V2App.jsx` `onClaimCardAction`):** 18.3.1 Deviation #2 stamped `_solicitationContext` at the parent-canvas adapter (`v22DataWithReveal`) + the Directory *panel* `syntheticNode`, but NOT the Directory *card* synthetic node (where `_directoryWarmPathRequestCandidate` etc. land in DirectoryLayer), so the V22ActionBar Reject icon was never reached on a solicited Claim's dot. Added a parallel `_solicitationContext` stamp at the DirectoryLayer card adapter — **inlined** (importing `computeSolicitationContext` from V2App would be a wrong-direction dep; V2App imports DirectoryLayer) with `solicitations` in its array form (`.find` rather than the Map `.get`), same predicate otherwise (active solicitation DA via the `_solicitationId` stamp targeting the active actor, linked solicitation still `'pending'`). **Necessary completion beyond the prompt's literal Fix 2 scope:** the Directory card's action bar dispatches through V2App's `onClaimCardAction` handler (not the parent-canvas `onV22CardAction` switch where 18.3.1's `rejectSolicitation` case lives), which had no `rejectSolicitation` branch — so one was added there (resolves `_node._solicitationContext` → opens the existing `SolicitationRejectModal` via `setV22SolicitationToReject`). Without it the newly-surfaced Reject button would itself be a dead affordance — the exact bug class Fix 1 closes. Reject parity restored at the Directory card surface; the parent-canvas card + the Detail Panel footer were already correct in 18.3.1. **Runtime status (build clean — 132 modules, 0 errors; app boots clean with no console errors, parent canvas renders + footer reads v0.18.3.1.1):** the Directory-card rendering (Fix 1 + the Fix 2a stamp) + the Reject cascade (Fix 2b dispatch) are canvas-raycaster-gated + need a *live solicitation* (the documented V2Canvas/Directory limitation every solicitation phase hits) → flagged for a manual pass (QA #1–6). Both fixes are surgical, additive branch edits verified against the actual file state + a clean build + a clean boot. Footer rolls forward to v0.18.3.1.1.

- **Phase 18.3.1 (prior phase):** **Bob-side solicitation UI: clickable rows, context-aware Claim panel, Reject cascade.** Six surface-level parts wiring the RFP owner's side of the solicitation flow, all reusing Phase 18.3 primitives (no new architecture). **(A — `src/components/DetailPanel/SolicitationCard.jsx`):** dropped the front actor-pill / "{party} solicited" row + the now-dead `ActorPill` component; the title becomes the Claim name with a "from {owner}" subtitle; `MessageBlock` lost its indigo `borderLeft` (uniform borders now); and the card body (title/subtitle/message/dates) is wrapped in a clickable `<div>` (pointer cursor + 4%-indigo hover tint, wired for both viewer roles) — the accepted-state pointer + action bar stay as siblings *outside* the clickable region so their own clicks aren't captured. New `onSelectSolicitation` prop. **(B — `src/v2/V2App.jsx` + `src/components/DetailPanel/RfpDetailPanel.jsx`):** new `handleSelectSolicitation(solicitation)` routes to the offered Claim via the existing `routeToPublishedClaim(claimId)` machinery (Phase 18.2.3; the name is a holdover — the Claim is directed-disclosed via the solicitation DA, not "published"). **Deviation:** the prompt's B1 said to declare it "after `handleRejectSolicitation`," but `handleSelectSolicitation` deps on `routeToPublishedClaim` (declared at V2App:4703, far below `handleRejectSolicitation` at 2819) — declaring it at 2819 would put `routeToPublishedClaim` in the TDZ when the useCallback deps array is evaluated at render. Placed it right after the `routeToPublishedClaimRef.current = routeToPublishedClaim` assignment instead. Threaded through `RfpDetailPanel` (`onSelectSolicitation`) to both SolicitationCard mounts + wired on the V2App RfpDetailPanel mount. **(C — `src/v2/V2App.jsx`):** new **module-scoped** `computeSolicitationContext(claim, activeParty, disclosureAgreements, solicitationsMap)` returns `{ solicitationId, rfpId }` when an active solicitation-DA (identified by the `_solicitationId` stamp minted in `handleCreateSolicitation`) targets the viewer AND the linked solicitation is still `'pending'`; null otherwise (pending-only gating is intentional — post-acceptance the EA takes over, post-rejection the DA is revoked). `_solicitationContext` is stamped at **both** Claim-node mount paths: `v22DataWithReveal` (parent canvas — added to the `anyDecoration` gate + the per-node early-return guard + the returned spread + the memo deps via `v22Solicitations`, following the existing `_publishedToDirectory` pattern) and the Directory mount's `syntheticNode` (the primary surface, since solicitation/umbrella DAs surface on the Directory not the parent canvas per §17.5.0.4). **(D — `src/components/DetailPanel/V22NodeDetailPanel.jsx` + `src/v2/V2App.jsx`):** V22ClaimPanel footer gains a `✕` "Reject Solicitation" `FooterButton` (new `hasRejectSolicitationAction = !isOwner && !!node._solicitationContext && !!onRejectSolicitation` flag, added to the early-return guard, rendered after the Run-Eval/Request-EA/View-EA chain + before Issue Badge) — **additive**, so the warm-path Request EA still renders alongside it; wired on both V22NodeDetailPanel mounts (parent + Directory) to open the existing `SolicitationRejectModal` via `setV22SolicitationToReject`. **(E — `src/v2/AssetNode.jsx` + `src/v2/V2App.jsx`):** V22ActionBar CLAIM branch gains a `✕` Reject icon (standalone `if (!isOwner && node._solicitationContext)`, additive with the `▷` Request EA branch) + a new `rejectSolicitation` dispatch case in `onV22CardAction` — universal footer↔action-bar parity. So Bob sees **both** `▷` Request EA and `✕` Reject on a solicited Claim. **(F — `src/v2/V2App.jsx`):** `v22-rfp-solicitation-received` notification clicks split into their own branch routing to the offered Claim (`clearEdgeUiState()` + `handleSelectSolicitation({ claimId: req.claimId })`, using the `claimId` Phase 18.3 added to the payload); the four other RFP notification types (`-rejected` / `-accepted` / `-closed` / `-reopened`) stay in the existing branch routing to the RFP marker, unchanged. Reject reuses Phase 18.3's `handleRejectSolicitation` cascade-revoke; Request EA reuses the Phase 11C warm-path (a solicitation DA satisfies the `_hasActiveDaWithoutEa` predicate by structural identity — no new wiring). **Runtime status (build clean — 132 modules, 0 errors; app boots clean with no console errors, parent canvas renders + footer reads v0.18.3.1, exercising the modified `v22DataWithReveal` hot-path memo + the new module-scoped helper + the new mounts without crash):** the redesigned card + the Reject footer/action-bar + the notification-routing round trip all require a *live solicitation*, which is created in real time via the canvas-raycaster-gated Directory RFP-marker → SolicitationCreateModal flow (the documented V2Canvas/Directory raycaster limitation every prior solicitation phase hit) → flagged for a manual pass (QA #1–16). The six parts are additive, surface-level prop-threading / branch edits verified against the actual file state + a clean build + a clean boot. Footer rolls forward to v0.18.3.1.

- **Phase 18.3.0.2 (prior phase):** **Self-eval orphan check: replace `evaluatorParty` signal with synthetic-EA id prefix.** Single-block predicate change at `src/v2/V2App.jsx:9300–9327` (the Eval Result `isOrphaned` check) + footer + Changelog. **Root cause of the dead 18.3.0.1 bypass:** Phase 18.3.0.1 applied the self-eval orphan bypass correctly but keyed it on `er.evaluatorParty === claim.owner` — and `evaluatorParty` isn't preserved on the output Eval Result artifact. It's passed *into* `makeEvaluationRunArtifacts` (V2App:1956) as a config field, but the factory stores it as `owner`/`evaluator` on the artifact (`v2_2Data.js:7243/7258`), not as `evaluatorParty`. There are **no** `er.evaluatorParty` read sites anywhere in V2.2 (only `node.evaluatorParty`, an adapter-computed property on the synthetic card node — confirmed by grep). So `er.evaluatorParty === undefined`, the bypass returned false for every self-eval result, and the predicate fell through to the EA lookup — which (correctly, per architecture) never finds the synthetic, unpersisted self-eval EA → false "Orphaned Evaluation Result" banner. **Fix:** switch the signal to the synthetic-EA id prefix `er.evaluationAgreementId.startsWith('self-eval-')`. The synthetic self-eval EA's id is built at `V2App:1913` as `` `self-eval-${claimId}` `` and is the **sole** site in the codebase emitting that prefix (grep across `src/` returns only V2App:1913); Andrew's orphan-banner screenshot literally showed the agreement id as `self-eval-claim-microco-rf-filter`, confirming the prefix is present on the artifact in the wild. The check reads the same `evaluationAgreementId` field the EA lookup (line 9325) already uses, so the field is guaranteed populated on every Eval Result reaching this branch — if it weren't, the existing EA lookup would already be broken for every result. The `const claim = …find…` lookup + the `evaluatorParty === claim.owner` line are removed; the new `if (er.evaluationAgreementId.startsWith('self-eval-')) return false` line sits between the `superseded` check and the EA lookup; the superseded check still runs first, the EA lookup still runs last. **Strict relaxation** — every previously-orphaned cross-party result stays orphaned; only self-eval is bypassed. **Side-benefit (parity restored):** with `isOrphaned = false` on self-eval results, the V22NodeDetailPanel footer (line ~2003) falls through past the Dismiss-only early return into the Re-Run + Create PoE branch (lines ~2016–2061, gated on `onCreatePoE`), matching the action bar (`AssetNode.jsx:1421–1438`, which never checked `isOrphaned`) — no V22NodeDetailPanel edit needed. **Verified:** `git diff src/v2/V2App.jsx` confirms the three checkpoints (old `evaluatorParty` check + `const claim` lookup gone; new prefix line present in the right place; comment block documents the 18.3.0.2 rationale + why 18.3.0.1's attempt didn't fire); `npm run build` clean. The live self-eval footer walkthrough (run a self-eval → no orphan banner → footer shows Re-Run + Create PoE → Create PoE works end-to-end) + the cross-party orphan regression (revoke Carol's EA on an Alice Claim → still flagged orphaned + Dismiss-only) are canvas-gated to set up → flagged for a manual pass; the predicate is a deterministic field-prefix edit that can't regress the non-self-eval cases by construction. Footer rolls forward to v0.18.3.0.2.

- **Phase 18.3.0.1 (prior phase):** **Solicitation modal polish + self-eval orphan fix.** Five items from 18.3 QA. **(Item 1 — `src/components/modals/SolicitationCreateModal.jsx`):** the modal title changed from `Solicit with my Claim — Re: {rfp.name}` to `Solicit '{rfp.name}' with my Claim` (single-quoted RFP name, "with my Claim" at the end). **(Item 2 — `src/components/modals/ModalShared.jsx`):** the `ModalHeader` "Step N of M" pill gains `whiteSpace: 'nowrap'` + `flexShrink: 0` so it stays on one line when a long title squeezes the header flex row (the renamed title is ~the same length as the old, so the wrap persists without the fix; the change propagates harmlessly to every modal using the shared `ModalHeader`). **(Item 3):** submit-lag observation filed as **polish-backlog #206** — no code this phase. **(Item 4 — SolicitationCreateModal Step 1):** the Claim-picker row subtitle now shows three counts — `{N} referenced asset{s} • {M} parse result{s} • {K} PoE{s}` — sourced from the `parseResultsByClaim`/`poesByClaim` props V2App already threads (Phase 18.3 Edit C2), so the user can see at a glance which Claims support Selective (needs parse results) or Proof-Only (needs PoEs) disclosure in Step 2; singular for 1, plural otherwise; **zero counts shown, not suppressed** ("0 PoEs" is itself informative); the greyed-row `opacity: 0.45` is preserved (the subtitle inherits it). **(Item 5 — `src/v2/V2App.jsx` `isOrphaned` predicate, ~line 9306):** self-evaluation Eval Results were falsely flagged orphaned. Root cause: a self-eval EA is synthetic (`id: self-eval-${claimId}`, grantor = grantee = active actor, built at evaluation-submit time) and **never persisted** to `v22Provisionals.evaluationAgreements`, so the predicate's `(v22View?.evaluationAgreements || []).find(e => e.id === er.evaluationAgreementId)` returned `undefined` → orphaned-true. The orphaned state short-circuited the Eval Result Detail Panel footer to Dismiss-only (hiding Re-Run + Create PoE), while the action bar still showed them — a footer↔action-bar parity break. Fix: bypass the orphan flag when `er.evaluatorParty === claim.owner` (the architectural signal of self-evaluation — `evaluatorParty` is `ea.grantee.party`, which for self-eval equals the Claim owner). A **strict relaxation** — every previously-orphaned result stays orphaned except the self-eval case; no V22NodeDetailPanel edit needed (the footer falls through to the existing non-orphaned Re-Run + Create-PoE branch). **Runtime-verified** (Alice/MicroCo soliciting Bob's Sentinel-4 RFP; build clean): the modal title reads "Solicit 'Sentinel-4 RF Module Compliance' with my Claim"; the "Step 1 of 4" pill renders inline (no wrap); Step 1 Claim rows show the three-count subtitle (e.g. "3 referenced assets • 0 parse results • 1 PoE" — zero counts present); the self-eval orphan bypass is a strict-relaxation predicate edit confirmed by reading the data flow (`evaluatorParty === claim.owner` ⇒ not orphaned). The live self-eval footer walkthrough (run a self-eval → footer shows Re-Run + Create PoE) + the genuine-orphan regression are canvas-gated to set up → flagged for a manual pass; the predicate change can't regress the non-self-eval cases by construction. Footer rolls forward to v0.18.3.0.1.

- **Phase 18.3 (prior phase):** **Solicitations carry a Disclosure Agreement (architecture + creation flow).** RFP solicitations now mint a paired Disclosure Agreement (solicitor → RFP owner, scoped to the offered Claim) atomically, so the owner gets real visibility into the Claim instead of just a message + reference. **This phase ships the architecture + creation flow; owner-side UI lands in Phase 18.3.1.** **(Part A — `src/v2/v2_2Data.js`):** `makeRfpSolicitation` gains a `disclosureAgreementId` link-back field. The prompt's view-builder *override* (A2) + directed-DA *cluster pass* (A3) are **already delivered by existing infrastructure** — a solicitation DA is structurally a directed claim DA (grantee = owner, grantor = non-owner non-network, subject = claim), identical in shape to a Phase 16.0/17.4 umbrella DA, so `buildV22DirectoryDataForRole`'s umbrella pass (`umbrellaClaimIdsByGrantor`) already pulls the offered Claim into the solicitor's cluster (upserting the cluster if needed) and `buildViewForActor`'s party-scoping already suppresses the conflicting public DA (the owner never holds a Radiant-Network public DA in their party-scoped set). **No redundant directed-DA passes were added** (they'd double-add cluster dots); a documenting comment marks the umbrella pass instead. **(Part B — `src/components/modals/SolicitationCreateModal.jsx`):** full restructure from a single-step Claim picker into a **4-step flow** — Step 1 RFP Description + Required Standards accordion + Claim picker (Phase 17.2.1.1 greying preserved); Step 2 Disclosure Type (3 `DecisionCard`s, **no Decline**; Proof-Only disabled + Tooltip when the Claim has no PoEs); Step 3 DA-expiry (`ExpiryPicker`) + a type-branched scope picker (Full Asset checkboxes / Selective per-Parse-Result field picker / Proof-Only PoE list) + the Message textarea (moved from Step 1); Step 4 Review. Mirrors CombinedResponseModal's `directoryPublishMode` primitives — `DecisionCard`/`ExpiryPicker`/`StepDots`/`expiryLabel` from `ModalShared`; `isoFromPicker` + an `SdaLine` + the 3-card `TYPE_DECISIONS_NO_DECLINE` set inlined locally (no cross-modal import on CombinedResponseModal). Submit fires `onSubmit({ rfpId, claimId, message, disclosureType, scope, daExpiryIso })`. **(Part C — `src/v2/V2App.jsx`):** `handleCreateSolicitation` mints the DA via `makeDisclosureAgreement` (grantor = solicitor, grantee = RFP owner, subject = the Claim, `_solicitationId`/`_rfpId` stamps, `status: 'active'`), pushes it into `v22Provisionals.disclosureAgreements` alongside the solicitation, and includes `claimId` in the `v22-rfp-solicitation-received` notification (for 18.3.1 routing); the `<SolicitationCreateModal>` mount pre-computes per-Claim `referencedAssetsByClaim`/`parseResultsByClaim`/`poesByClaim` (CombinedResponseModal mount contract). A shared `revokeSolicitationDa(daId, reason)` helper cascade-revokes the linked DA on solicitation reject (`handleRejectSolicitation`) AND on RFP close (`handleCloseRfp` now rejects every open solicitation on the RFP + revokes each linked DA — no per-solicitation rejected notification, since the existing `v22-rfp-closed` notification already informs each solicitor). **Runtime-verified** (Alice/MicroCo soliciting Bob's Sentinel-4 RFP; build clean; only the pre-existing benign `NaN`-CSS-`left` edge-menu transient in console): the modal opens at **Step 1 of 4** (RFP Description + Required Standards (2) accordion + Claim picker with PRM Assembly / Voltage Regulator IC greyed "ON NETWORK"); **Step 2 of 4** = 3 type cards (no Decline); **Step 3 of 4** = DA-expiry picker (description names GovCo) + Asset list ("0 of 1") + Message textarea; **Step 4 of 4** Review = For RFP / From MicroCo / To GovCo / Claim being offered EMI Shield Assembly / Disclosure Type Full+SdaLine / Scope 1 of 1 Asset / Disclosure Agreement Expires Never expires / Message — **no EA Expires, no Acknowledgments** — + "Submit Solicitation"; **Submit closes the modal and Alice's EMI Shield Claim panel shows a new "Full Disclosure · with GovCo" DA row** (atomic mint confirmed live). Data-probes confirm the factory carries `disclosureAgreementId`, the umbrella pass pulls a directed claim DA into the owner's cluster, and an **active** solicitation DA is present in Bob's view while a **revoked** one (cascade) is absent. **Seed caveat:** every Claim is already publicly disclosed to the Radiant Network in the seed, so the cluster *appear/disappear* effect (prompt QA #9/#12/#13) isn't observable for seeded Claims — every Claim is already a dot; the verified architectural effects are the atomic DA mint, the directed scope, and the cascade. The live owner-side Reject walkthrough is canvas-gated + uses the OLD reject UI that 18.3.1 redesigns → flagged for manual. Footer rolls forward to v0.18.3.

- **Phase 18.2.3 (prior phase):** **Post-publish orchestration + already-published gate + Parse Asset rename.** Three independent items in one phase. **(Part A) Post-publish orchestration** (the original 18.2.1 plan, renumbered): on publish submit, `handlePublishToDirectorySubmit` stamps `v22RecentlyPublishedClaimId` + fires `routeToPublishedClaimRef.current(claim.id)` (the deferred-orchestration comment block is gone). New `routeToPublishedClaim` callback in `src/v2/V2App.jsx` (byte-for-byte mirror of `routeToRfp`) closes the parent selection (`setSel(null)` + `clearEdgeUiState()`), opens the Directory, sets the selected Directory Claim (auto-opens its Detail Panel), and runs a **cancellable** 240-frame rAF retry loop (`routePublishedClaimSelectRafRef`) calling DirectoryLayer's new imperative `selectClaim(claim, { zoom: 0.6 })` until the just-published dot appears in the rebuilt layout. `routeToPublishedClaimRef` mirrors `routeToRfpRef` (lets the earlier-declared submit handler call it without a forward dep). In `src/v2/DirectoryLayer.jsx`: new `recentlyPublishedClaimId` prop + ref-mirror; new `focusClaimInternal` helper (parallel to `focusRfpInternal` — pins the dot + pans/zooms with panel-offset compensation); new imperative `selectClaim` (mirror of `selectRfp`, resolves the dot via `layoutRef.allDots.filter(kind !== 'rfp')`, returns false until the dot is in layout so the caller's rAF retry consumes it); the synthetic Claim card gets `_isNew: true` when its id matches `recentlyPublishedClaimId` (NEW badge — renders at card-LOD via the existing AssetNode pipeline). A clear-on-deselect effect drops `v22RecentlyPublishedClaimId` once the selected Directory Claim is no longer the published one (mirrors the RFP clear-on-deselect effect). Mirrors the RFP arc's 17.5.1 → 17.5.2 split. **(Part B) Already-published gate:** `v22DataWithReveal` builds a `publishedToDirectoryClaimIds` Set (DAs with `subject.kind === 'claim'` + `grantor.party === activeParty` + `grantee.party === RADIANT_NETWORK_PARTY` + `type !== 'provisional'` + not declined/revoked — same shape as the §18.2.1.1 predicate) and stamps `_publishedToDirectory: true` on owned Claim nodes in that set (added to the early-return guard + the return spread). The V22ClaimPanel footer "Publish to Directory" `FooterButton` (`src/components/DetailPanel/V22NodeDetailPanel.jsx`) and the V22ActionBar CLAIM-branch button (`src/v2/AssetNode.jsx`) both read `node._publishedToDirectory` → `disabled` + an explanatory tooltip ("This Claim is already published to the Public Directory. Revoke the existing Disclosure Agreement in the Agreements section to republish." / the action bar's shorter "This Claim is already published to the Public Directory."). `ActionButton` gained `disabled` support (muted `var(--text-dim)`, `cursor: not-allowed`, `opacity: 0.55`, inert onClick) so the action-bar parity works; the V22ActionBar render passes `disabled={b.disabled}`. The flag clears reactively on revoke (`v22View` recomputes from `v22Provisionals`); a defensive `&& !node._publishedToDirectory` no-op gate added to V2App's `publishToDirectory` dispatch case. **(Part C) Parse Asset rename + iframe fallback:** the Parse modal title (`src/components/modals/V22ParseEvidenceModal.jsx`, 3 `ModalHeader` titles + the "Parse Asset →" action button), the V22AssetPanel footer `FooterButton` label + tooltip, and the V22ActionBar ASSET-branch tooltip all rename "Parse Evidence" → "Parse Asset" (the internal dispatch action name `parseEvidence` is unchanged; the dead legacy `ActionBar`'s "Parse Evidence (PEP)" tooltip — only rendered for non-`v22Type` nodes, which don't exist in V2.2 — is left untouched). **The iframe bug fix** lives in `AssetFileViewer` (`src/components/AssetEvidencePanel.jsx`): many demo Assets reference a `/…pdf` path whose physical file doesn't exist under `public/`, so the dev server (and Netlify SPA fallback) serve `index.html` for that path → the plain `<iframe src={file.localPath}>` rendered the whole app inside itself. Fix: new `iframeFailed` state (reset on `file.localPath` change) + an `onLoad` content-sniff (if the loaded same-origin document carries the app root `#root` / the app title → flag failed) + an `onError` handler; when `fileLooksValid && !iframeFailed` is false, render a "No preview available." placeholder instead of the iframe. Real PDFs render unchanged (their native-viewer `contentDocument` is null/inaccessible → not flagged). Imported `useState`/`useEffect` into `AssetEvidencePanel.jsx`. **Runtime-verified** (Alice/MicroCo; build clean; zero console errors throughout): **Part A** — publishing the unpublished PCB Stackup Claim auto-transitioned Parent → Directory and auto-opened its Detail Panel showing the new "with Radiant Network" DA row (the 60% camera landing + NEW-badge-on-card are rAF/Three-gated → flagged for manual; `selectClaim` + `_isNew` mirror the verified RFP pattern, the build is clean, no errors). **Part B** — the footer Publish button is **disabled + `not-allowed` + correct tooltip** on the published PRM Assembly Claim and **enabled** on the unpublished PCB Stackup; **V22ActionBar parity confirmed** (the card's ⊕ button is disabled + `not-allowed` + parity tooltip via the fiber); `_publishedToDirectory` is correct across all 8 of Alice's Claims (3 seeded-public true, 5 umbrella-seed false). **Part C** — modal title "Parse Asset" + "Parse Asset →" button + footer "Parse Asset"; the **missing-PDF Asset (RF Filter RFF-900 → `/microco-rffilter.pdf`) shows "No preview available"** (no app-in-iframe), the **real-PDF Asset (PRM Datasheet → `/seed-pdfs/microco-prm-datasheet.pdf`) renders the iframe** normally. Revoke→re-enable (B), Selective/Proof-Only orchestration parity (A — type-agnostic), the defensive dispatch gate (B), and the RFP-routing regression (A leaves `routeToRfp` + its refs untouched) are code-verified. Footer rolls forward to v0.18.2.3.

- **Phase 18.2.2 (prior phase):** **Retire `PublishToDirectoryModal`, reuse `CombinedResponseModal`.** Retired the bespoke publish modal shipped in Phase 18.2 (the file `src/components/modals/PublishToDirectoryModal.jsx` is **deleted**). The Publish-to-Directory flow now runs through `CombinedResponseModal` via a new `directoryPublishMode` prop. **Mental model:** publishing is "disclose to the Radiant Network as a non-human auto-accepting grantee" — structurally identical to the cold-path Disclosure Response (spec §7.1) minus the EA half — so one modal + one set of primitives, parity-by-construction with the responder flow. **11 surgical branches in `src/components/modals/CombinedResponseModal.jsx`:** (M1) new `directoryPublishMode = false` prop; (M2) derived `const isDirectoryPublish = directoryPublishMode === true`; (M3) `totalSteps` short-circuits to `3` ahead of the existing cold-path/decline/eaOnly fork; (M4) `handleSubmit` fires DA-only `onAccept({ type, scope, daTerms: { expires } })` (no `eaTerms`); (M5) banner renders "Publishing to / Radiant Network" + "Claim" (no Requester/Message rows); (M6) header titles/subtitles branch ("Publish to the Public Directory" → final-step "Review your Public Directory Disclosure"); (M7) Step 1 grid drops the Decline card via `.filter((d) => !isDirectoryPublish || d.id !== 'decline')` + `repeat(3,1fr)` + relabels FieldLabel "Choose disclosure type"; (M8) Step 2 scope descriptions (DA-expiry, Full, Proof-Only) swap counterparty-name copy for "the Public Directory" / "Actors browsing the Public Directory" via inline ternaries (Selective's copy never referenced the requester, so it's untouched); (M9) Step 3 EA-Terms render gated `&& !isDirectoryPublish`; (M10) Step 4 review gate moves `step === 4` → `step === totalSteps` (matches both the 3-step publish + 4-step cold-path flows) + the EA-Expires row and the Acknowledgments IIFE are wrapped in `!isDirectoryPublish` guards (Disclosure Type, scope, and DA-Expires rows still show); (M11) Accept button reads "Publish to Directory". **V2App (`src/v2/V2App.jsx`):** removed the `PublishToDirectoryModal` import + mount block; the new mount renders `<CombinedResponseModal … directoryPublishMode={true} />` with a synthesized `request` (active actor as `ownerParty`, `RADIANT_NETWORK_PARTY` as `requesterParty`, `proposedEaTerms: null`) + the same `referencedAssets` / `parseResults` / `poesForClaim` prop contract as the cold-path response mount (`poesForClaim` keeps the Phase 18.2.1.1 Claim-membership-only filter); `handlePublishToDirectorySubmit` renamed its payload field `payload.expiryIso` → `payload.daTerms?.expires` (only that derivation changed — the `makeDisclosureAgreement` call + `v22Provisionals` push are unchanged). Three stale `PublishToDirectoryModal` comment refs (V2App ×2 + V22NodeDetailPanel ×1) repointed to the new flow. **Behavior delta:** the Full Asset picker now defaults to **zero-selected** (the CombinedResponseModal picker convention per CLAUDE.md — the user explicitly opts in), vs the retired modal's all-checked default; this actually *aligns* the publish flow with the documented picker-defaults convention the bespoke modal violated. **Runtime-verified** (Alice/MicroCo, PRM Assembly Claim; build clean; zero console errors): the publish modal opens in directoryPublishMode — title "Publish to the Public Directory", "Step 1 of 3", banner "Publishing to / Radiant Network" + "Claim", 3 cards (Full/Selective/Proof-Only, **no Decline**); Step 2 Full shows the DA-expiry picker + Asset list with publish-specific copy ("the Public Directory will surface…" / "…accessible from the Public Directory"); Step 3 Review shows DISCLOSURE TYPE / ASSETS IN SCOPE / DISCLOSURE AGREEMENT EXPIRES — **no EVALUATION AGREEMENT EXPIRES row, no ACKNOWLEDGMENTS row** — and a "Publish to Directory" button; the **Full publish is confirmed end-to-end** (Submit → modal closes → a "Full Disclosure · with Radiant Network" row appears in the Claim's Agreements section); the **Proof-Only Step 2** PoE picker renders both third-party PoEs ("by GovCo · wraps 1 · 8 SAT · 1 UNSAT", "by AuditCo · wraps 1 · 2 SAT · 1 UNSAT" — neither owned by Alice, confirming the 18.2.1.1 filter carries over) with publish copy ("Actors browsing the Public Directory…"). **Cold-path response (4 cards / 4 steps / EA-Expires + Acknowledgments rows), EA-only warm path, and Decline path are preserved by construction** — every new branch is gated behind `isDirectoryPublish` (false for those flows) — but couldn't be live-confirmed in-session (no seeded `v22-request` notification + canvas card-selection harness limits); flagged for a manual pass (QA #13–15). `grep -r PublishToDirectoryModal src/` returns zero. Post-publish orchestration renumbered 18.2.1 → **18.2.3** (still deferred). Footer rolls forward to v0.18.2.2.

- **Phase 18.2.1.1 (prior phase):** **V2App PoE picker filter hotfix.** Single-symbol fix at two parallel V2App modal-mount sites (`src/v2/V2App.jsx`). Both the `CombinedResponseModal` Proof-Only step (`poesForClaim`, ~line 7476) and the `AmendDisclosureModal` Proof-Only scope picker (`candidatePoEs`, ~line 7882) pre-filtered PoEs with `poe.claimId === <claim> && poe.owner === da.grantor.party`. The `&& poe.owner === da.grantor.party` clause was wrong: per Phase 13 / spec §10.4, a proof-only Claim DA carries `subject.kind === 'claim'` + `scope.poeIds`, and the grantor's right to grant proof-only disclosure derives from their ownership of the **Claim**, not the PoE — so any PoE *visible to the grantor on that Claim* (including third-party-authored PoEs that reached the claim owner via the original evaluator → claim-owner proof-of-evaluation DA) is a valid scope candidate. The clause excluded every third-party-authored PoE — the common evaluator-to-claim-owner case, and exactly what 18.2.1 QA surfaced (Carol completed an eval chain on Alice's EMI Shield Assembly Claim + created a PoE; the PoE rendered on Alice's netgraph, but when Bob requested DA+EA and Alice opened the response modal with Proof-Only selected, the picker showed the empty state because Carol's PoE owner AuditCo ≠ DA grantor MicroCo). Fixed by dropping the clause at both sites (filter by Claim membership only). The two sites were pre-PoE-era code that survived the Phase 13 migration with stale ownership semantics; this aligns them with the seed precedent (Phase 17.5.0.4 mock-supplier proof-only Directory DAs publish third-party PoEs) and Phase 18.2's correctly-wired `PublishToDirectoryModal` filter. The new filter is a **strict superset** of the old, so the self-evaluation case (`owner === grantor`) still matches. No view-builder changes (Alice's `v22View.proofsOfEvaluation` already contains Carol's PoE — proven by the netgraph render), no modal changes (the picker rows already show `by {owner}` evaluator attribution), no data-model changes. **Verification:** build clean; the change is a deterministic filter-predicate edit (drop one `&&` clause at each site) — **code- + build- + data-probe-verified** (seeded `poeBobPrm`/`poeCarolPrm` both carry `claimId === cPrm.id` with owners GovCo/AuditCo, both ≠ MicroCo, so the old filter excluded both and the new filter includes both for Alice as grantor). Live confirmation of the response/amend modal at the picker step is blocked in-session by the documented no-seeded-`v22-request` + canvas-card-selection harness limits; flagged for a manual pass (QA #1-11). Footer rolls forward to v0.18.2.1.1.

- **Phase 18.2.1 (prior phase):** **CombinedResponseModal hotfix (Step 1 button gating + Proof-Only copy parity).** Two surgical fixes to `src/components/modals/CombinedResponseModal.jsx` surfaced during Phase 18.2 QA; both bugs predate 18.2 (the modal is Phase 11/13-era — 18.2 just exposed them). No new components, props, handlers, or data-model changes. **Bug 1 (P0 crash) — `totalSteps` formula.** The cold-path branch `(isDecline ? 2 : action ? 4 : 1)` collapsed to `1` at Step 1 before a disclosure type was picked (`action === null`), so `step === totalSteps` evaluated true at Step 1 and rendered the final-step (ungated) "Accept" button instead of the disabled "Continue" (whose `step < totalSteps` guard was false). Clicking Accept fired `handleSubmit()` with `action === null` → fell through to `onAccept?.({ type: null, scope: null, … })` → V2App's `handleV22Accept` null-faulted finalizing the provisional pair. Fixed by hardcoding the non-decline cold path to `4` (`(isDecline ? 2 : 4)`): StepDots now reads "Step 1 of 4" from modal open, Continue renders gated by the pre-existing `canAdvanceFromStep1 = action != null` (disabled until a type is picked), and Accept only renders at the true final step (`step === 4`, reachable only after picking a non-decline action + walking Steps 2-3). The Decline path still drops to 2 once `action === 'decline'` flips `isDecline`; the EA-only warm path (`isEaOnly`) is untouched. **Bug 2 (copy regression) — Proof-Only "Evaluation Results" → "Proofs of Evaluation".** The Step 2 Proof-Only picker was migrated to PoEs in Phase 13 (#168) but three surrounding strings + the empty-state styling were missed. Swept: the FieldLabel ("Select Proofs of Evaluation to share"), the description ("…the pass/fail outcome of the selected Proofs of Evaluation…"), the empty-state card (re-styled from amber-tinted to the neutral `var(--bg-card)`/`var(--border)`/`var(--text-dim)` that matches the Selective empty state byte-for-byte; copy "No Proofs of Evaluation available. Proof-Only Disclosure requires at least one Proof of Evaluation on this Claim." mirroring Selective's "No parsed Assets available…" structure), and the Step 4 review row label ("Proofs of Evaluation shared", which renders mono-uppercase as `PROOFS OF EVALUATION SHARED` — the existing `minWidth: 230` absorbs the longer string). **Verification:** build clean. Both fixes are deterministic — Bug 1 is pure render-logic (the exact crash vector traced + closed), Bug 2 is copy/style swaps — so they're **code- + build-verified**. Live confirmation of the cold-path response modal at Step 1 is blocked in-session because there's no seeded `v22-request` notification and generating one needs an undisclosed-Claim PIN (which the requester can't see) + a multi-role request→respond flow — disproportionate + harness-fragile for a one-line ternary + copy hotfix; flagged for a manual pass per the documented V2Canvas card-selection limitation. Console shows only the pre-existing benign `NaN`-CSS-`left` edge-menu transient (unrelated to this modal). Footer rolls forward to v0.18.2.1.

- **Phase 18.2 (prior phase):** **Publish Claims to the Public Directory (foundation).** Restores the V2.0/V2.1 "publish a Claim to the Radiant Network" mechanic lost in the V2.2 Directory rebuild. **New `src/components/modals/PublishToDirectoryModal.jsx`** — two-step Form → Review modal (cloned from `RfpCreationModal`): Step 1 = a Full/Selective/Proof-Only disclosure-type picker (custom vertical rows with `SdaLine` illustrations + the disclosure subtitles; Proof-Only **disabled** with `cursor: not-allowed` + a Tooltip "This Claim has no Proofs of Evaluation yet…" when the Claim has no PoEs) + a **type-branched scope picker** (Full → Asset checkboxes default-all-checked; Selective → expandable Asset rows exposing parsed-field checkboxes, composite ids `${parseResultId}::${fieldId}`; Proof-Only → PoE checkboxes default-zero) + an `ExpiryPicker` (default "Never expires"); Step 2 = a labeled review (For Claim / Granted to = Radiant Network + globe / Disclosure Type + SdaLine / Scope / Expires). On Submit fires `onSubmit({ type, scope, expiryIso })`. **Owner-only entry points** (per the universal footer↔action-bar rule): the V22ClaimPanel footer "Publish to Directory" button (icon `⊕`, after Amend Claim + Self-Evaluate; new `onPublishToDirectory` prop) + the V22ActionBar CLAIM branch (`onV22CardAction('publishToDirectory')`). **V2App** adds `v22PublishToDirectoryClaim` state (single state — non-null carries the Claim + signals the modal open), `handlePublishToDirectorySubmit` (mints a DA via `makeDisclosureAgreement` with `grantee = { RADIANT_NETWORK_PARTY, RADIANT_NETWORK_DOT }`, `subject: { kind: 'claim', id }`, the picked `type` — NOT `'provisional'` — + discriminated `scope`, `status: 'active'`, then pushes into `v22Provisionals.disclosureAgreements`), the modal mount (enriches `ownedAssets` with their parsed fields + derives `ownedPoEsOnClaim` by matching PoEs whose wrapped Eval Result's `claimId` equals the Claim), the `onPublishToDirectory` mount wiring (owner-gated; the Directory-mount Claim panel intentionally omits it per 17.4.4), and the `'publishToDirectory'` dispatch case. **No new merge helper, session-state Map, or builder signature change** — `mergeProvisionals` merges the DA by id and `buildV22DirectoryDataForRole`'s existing public-DA detection (`grantee.party === 'Radiant Network'` + `subject.kind === 'claim'` + `type !== 'provisional'`) surfaces the Claim as a dot in the active actor's own cluster on next Directory render. Radiant Network is an auto-accepting grantee (a directory, not an actor) → no provisional/accept handshake. **Imports added** to V2App: `makeDisclosureAgreement`, `RADIANT_NETWORK_PARTY`, `RADIANT_NETWORK_DOT` (+ the modal). **Deviation:** the prompt's mount snippet mapped `ownedAssets` from raw `shared.assets`; I enriched each with a `parsedFields` array (from its Parse Results) so the Selective field picker actually has fields to render — a necessary refinement, not a departure from intent. Icon kept as `⊕` per the prompt default (no icon deviation). **Runtime-verified** (Alice/MicroCo; build clean; no console errors): the `⊕ Publish to Directory` footer button renders; the modal opens with all three disclosure-type radios + correct Step 2 review; the **Full publish flow is confirmed end-to-end** (Publish → modal closes → a "Full Disclosure · with Radiant Network · Never expires" row with AMEND/REVOKE appears in the Claim's Agreements section); Proof-Only **disabled gating** confirmed on a no-PoE Claim (`cursor: not-allowed` + exact tooltip via the React fiber). **Code/data-verified, not live-confirmed in-session** (documented V2Canvas harness limits — canvas card-selection after role-switch+FIT couldn't reliably place Alice's PRM-Assembly card on-screen; Directory dots are canvas-raycaster-gated): the Selective field-picker UI, the Proof-Only-enabled PoE list (deterministic from the seed — `poeBobPrm`/`poeCarolPrm.claimId === cPrm.id`), and the published Directory dot rendering. **Foundation only** — post-creation orchestration (auto-route Parent → Directory + variant load animation + auto-select + NEW badge) is deferred to **Phase 18.2.1**. Footer rolls forward to v0.18.2.

- **Phase 18.1 (prior phase):** **Placeholder RELEASE action on Internal Disclosure Agreement rows.** A muted, non-functional `RELEASE` action now appears on every Internal Disclosure Agreement row in the Agreements section, across all node Detail Panels that mount `<AgreementsSection>` (Actor / Asset / Claim / Eval Result / Parse Result). **Sole code change in `DisclosureAgreementRow`** (`src/components/DetailPanel/V22NodeDetailPanel.jsx`): (1) new module-scoped `DA_SUBJECT_KIND_LABEL` map (`asset → 'Asset'`, `claim → 'Claim'`, `evalResult → 'Evaluation Result'`, `parseResult → 'Parse Result'`, `poe → 'Proof of Evaluation'`, `'artifact'` fallback); (2) `const showRelease = isInternal && !isRevoked` + `const subjectLabel = DA_SUBJECT_KIND_LABEL[da.subject?.kind] || 'artifact'`; (3) the AMEND slot's ternary gains a `showRelease` branch rendering a **disabled** `<ActionLabel label="Release">` with the tooltip "Releasing your disclosure agreement to this {subjectLabel} will remove it from your network. Coming soon." The tooltip noun resolves from the DA's own `subject.kind`, NOT the panel node type — so an Asset panel's claim-ref internal DA correctly reads "...to this Claim..." (Phase 17.4.5 architectural note: the Agreements section can surface DAs whose subject is a *related* artifact). `showAmend`/`showRelease` are mutually exclusive by construction (`showAmend` requires `!isInternal`), so they share the slot-1 ternary; the REVOKE slot + the ~32px two-slot row geometry are untouched. **Single-source change** — `DisclosureAgreementRow` is consumed by the shared `<AgreementsSection>`, so no prop-chain plumbing was needed. Disabled-only surface: no `onClick`, no V2App handler, no `onRelease` callback, no data-model changes; previews the eventual "release this artifact from my network" mechanism (handler + wiring land in a later phase). **Supersedes the original 18.1 brief** (footer-button placeholder on owned Assets/Claims) — Release attaches to the DA itself because "release the disclosure agreement keeping this artifact on your network" is the more accurate mental model. **Runtime-verified** (build clean; no console errors) on Bob's Avionics Module Asset panel: 3 Internal DA rows render RELEASE (muted `var(--text-dim)` = `rgb(122,131,153)`, cursor `default`, disabled); subject-aware tooltips confirmed via the React fiber — Row 1 (asset subject) "...to this Asset...", Rows 2-3 (evalResult subjects) "...to this Evaluation Result..." (proving the noun is subject-driven, not panel-driven, since all three render on an Asset panel); clicking RELEASE is a no-op (panel unchanged, no console error); the 2 cross-party AMEND + 18 REVOKE rows are unchanged. EA rows (`EvaluationAgreementRow`) + Parse Result panels (empty Agreements section per #116) are untouched by construction. Footer rolls forward to v0.18.1.

- **Phase 18.0.1 (prior phase):** **Hotfix: sticky edge tooltip on Parent → Directory transition (closes #203).** Demo-blocking bug Andrew hit recording the RFP walkthrough — the parent-canvas "SELECT EDGE TO VIEW" hover tooltip persisted over the Directory layer after transitioning Parent → Directory. **Root cause** (`src/v2/V2Canvas.jsx`): the `onMouseMove`-bound `handleEdgeHover` had no Directory-open guard — only the existing `transitioningRef.current` check, which covers V2Canvas-internal dive/surface transitions, NOT the Directory transition. Phase 17.4.3's `clearEdgeUiState` cleared all three edge-tooltip state layers correctly + synchronously on the Directory-open transition, but afterward any mousemove during/after the ~600ms clip-path wipe (and indefinitely, while V2Canvas stays mounted underneath) re-raycasted a parent-canvas edge, re-fired `onEdgeHover({ edgeId, sdaType, x, y })`, and re-set V2App's `edgeHover` → the `EdgeHoverMenu` re-rendered in hover mode (portaled to document.body at z-index 6000, above the Directory). The "intermittent" #203 framing was because repro needed the cursor over a hit-range edge + any post-clear mousemove — both common during recording. **Fix.** (1) **V2Canvas**: new `directoryOpen` prop; early-return guards added to `handleEdgeHover` and `handleBackgroundClick` next to the existing `transitioningRef` check (both useCallback dep arrays updated with `directoryOpen`); a new `useEffect` clears V2Canvas's internal hover state (`hoveredEdge`/`edgeTooltipPos`/`hoveredEdgeSdaType`) + cancels the pending 150ms `edgeHideTimeout` debounce on the `directoryOpen` false→true flip (belt-and-suspenders for the small SDA cursor dot at z-index 5900). (2) **V2App**: passes `directoryOpen={v22DirectoryOpen}` to V2Canvas; defensive `!v22DirectoryOpen` gate on both `EdgeHoverMenu` mount conditionals (hover-mode + pinned-mode), so even a stray `onEdgeHover` after the transition can't mount the menu over the Directory; `clearEdgeUiState` extended to also `setEdgeMenuPanning(false)`. **Deviation:** the prompt's Part 2C referenced a `setEdgeMenuProjected` setter, but no such state exists — the pinned menu re-projects `edgeMenu.anchor` in place via a fade effect keyed on `edgeMenu?.edgeId`, with `edgeMenuPanning` as the fade flag (already reset to false when `edgeMenu` goes null), so `setEdgeMenuPanning(false)` is the real-world equivalent. **Audit:** all Directory-open paths (globe toggle, Radiant-Network double-click, RFP-notification routing, role switch, `routeToRfp`, `routeToAsset` exit) already call `clearEdgeUiState`; with the V2Canvas guard + the V2App mount gate, a missed clear is now harmless. **Runtime status (build clean; no console errors):** the Directory open/close cycle is verified clean and ~900 parent-canvas mousemoves dispatched while the Directory was open produced no tooltip ghost + no errors (exercising the new `directoryOpen` early-return). The actual hover-tooltip *repro* (hover an edge → tooltip → open Directory) is canvas-raycaster-gated — DOM-dispatched mousemoves don't trigger the Three.js edge raycaster (documented limitation), so the before/after couldn't be empirically demonstrated in-session; the fix is a straightforward early-return guard + render gate (low-risk, code-verified) plus the verified-clean cycle. **#203 closed.** Footer rolls forward to v0.18.0.1.

- **Phase 18.0 (prior phase):** **Demo prep quick wins.** Four S-effort polish items — the first demo-prep pass (Phase 17's RFP creation arc is fully CLOSED; **Phase 18 opens as the demo-prep umbrella** for items leading up to the demo walkthrough recording). **(Part 1) Selection ring centering** (`src/v2/AssetNode.jsx`, `AssetNodeDot`): the amber `isSelected` ring's `top:-6/left:-6` was measured from the inner 8×8 dot's *padding edge* (inside its 1.5px border), so the ring rendered ~1.5px down-and-right of the dot center. Switched to `top:'50%'/left:'50%'` + `transform:'translate(-50%,-50%)'`, which is immune to the border offset. Runtime-verified concentric (ring center == inner-dot center, **0px offset**). **(Part 2) View File button unification** (`src/components/DetailPanel/V22NodeDetailPanel.jsx`, `V22AssetPanel`): both the owner and non-owner File-section branches now render the large full-width button (formerly a small `ExpandButton` header icon for owners, and "Open Evidence Viewer" for non-owners); label changed to **"View File"**; the owner branch's file-metadata rows (Filename/Size/MIME/Hash/URI, owner-only per Phase 11D #135) are preserved. Deduped via a shared `viewFileButton` const (same `onExpandAsset` handler regardless of ownership). Runtime-verified: owner panel shows the 443px button + click opens the evidence viewer. **(Part 3 / #35) Amend Claim edge animation** (`src/v2/v2_2Data.js` + `src/v2/V2App.jsx`): `makeAmendedClaim` stamps `_isNew: true` + a numeric `_createdAt` on the new claim-ref DAs it builds for added/supersede-target Asset references; `deriveAgreementEdges`'s `pushEdge` propagates both onto the derived edge so V2Canvas's existing edge-draw animation (zero → grow `instanceCount`, gated to a 3s `_createdAt` window) animates the new Claim ↔ Asset edges in. Because claim-ref DAs are *permanent* internal DAs, `handleV22AmendClaimSubmit` clears the reveal flags ~1.1s after the draw-in (scoped to the just-added DA ids) — otherwise the edge would keep rendering with `_isNew`'s brighter/thicker styling. No cross-impact on the provisional `e-prov-` reveal path. **(Part 4 / #116) Agreements section on Eval/Parse Result panels** (`src/components/DetailPanel/V22NodeDetailPanel.jsx` + `src/v2/V2App.jsx`): extended the Phase 9C `AgreementsSection` (Actor/Asset/Claim) to **V22EvalResultPanel** (V2App filter `subject.kind === 'evalResult' && subject.id === node.id` — catches the evaluator's auto-disclosure/PoE DA flowing to the Claim owner + further disclosures) and **V22ParseResultPanel** (`subject.kind === 'parseResult'` → empty in V2.2, so the section renders nothing per 9C's `return null` convention). Both panels destructure the existing agreement props (already passed to `V22NodeDetailPanel` + spread `{...props}` to each panel by the router) and render `<AgreementsSection>`; no new primitives. Runtime-verified: Bob's "Evaluation of Power Regulation Module Assembly" panel renders AGREEMENTS → DISCLOSURE AGREEMENTS (1) with the eval-result-subject DA row. **Closes backlog #35 + #116** (both moved to the polish-backlog Completed section). **Runtime status (build clean; no console errors):** Parts 1, 2 (owner branch), 4 (Eval Result) confirmed live in the dev preview; Part 2 non-owner (the shared `viewFileButton` element — owner branch proves it), Part 3's rAF-paced edge-draw animation (the headless preview throttles rAF to ~1fps — documented limitation), and Part 4's empty Parse Result section (returns null by design) are code-verified + build-clean and flagged for a manual 60fps pass. Footer rolls forward to v0.18.0.

- **Phase 17.5.2.2 (prior phase):** **RFP arc post-closure cleanup.** Four manual-QA items after the arc closed at 17.5.2.1, all in `src/v2/V2App.jsx` + `src/v2/DirectoryLayer.jsx`. **(Part 1) Request Agreement regression on user-created RFPs:** `handleRequestAgreement` resolved the RFP via bare `buildV22SharedArtifacts()` — no session-state overlays — so it bailed at `if (!rfp) return` for user-created RFPs (same bug shape as 17.5.1.3's `handleCreateSolicitation`). Fixed by threading the full created → closed → removed merge chain (`mergeRemovedRfps(mergeClosedRfps(mergeCreatedRfps(buildV22SharedArtifacts(), v22CreatedRfps), v22ClosedRfpIds), v22RemovedRfpIds)`); deps `[]` → `[v22CreatedRfps, v22ClosedRfpIds, v22RemovedRfpIds]`. The rest of the handler body (anchorAsset / sharedClaim lookups, state setters, AI Shopper pre-fill) is unchanged. **(Part 2) Notification pan/zoom to 60%:** `selectRfp` imperative handle (DirectoryLayer) gained an optional `opts.zoom` — `const targetZoom = opts.zoom ?? Math.max(zoomRef.current, LOD_THRESHOLD)`; notification routing in V2App now passes `{ zoom: 0.6 }` so RFP notification clicks (solicitation received / rejected / accepted / closed / **reopened**) land at 60% (read the marker, keep cluster context). `routeToRfp` + internal click paths pass no opts → stay at full-card LOD (post-creation + Anchored-RFPs-row clicks unchanged). Asymmetric on purpose. **(Part 3) Reopen notification — REVERSES 17.5.1.4's "Reopen is silent (asymmetric)" decision:** `handleReopenRfp` now dispatches a `v22-rfp-reopened` notification to every UNIQUE solicitor party (deduped via a Set; zero solicitors → zero notifications), symmetric with `handleCloseRfp`'s dispatch shape. New render branch: full-saturation `var(--accent-indigo)` badge (vs the closed badge's muted `color-mix` blend), `RFP REOPENED` label, body "{owner} reopened the RFP … You can resume engagement."; `v22-rfp-reopened` added to the notification-routing type union so click routes the same as `v22-rfp-closed` (Directory → marker → panel, 60% landing). **(Part 4) Directory zoom controls shift left:** DirectoryLayer gained a `detailPanelOpen` prop; the chrome's `right` is now `detailPanelOpen ? PANEL_W + 12 : 12` with a `transition: 'right 200ms ease-out'`. V2App passes `detailPanelOpen={!!(v22DirectorySelectedClaim || v22DirectorySelectedRfp)}` (matches the parent-canvas panel-shift behavior). **Runtime status (build clean; no new console errors):** code-verified across all four parts; the pan/zoom-to-60% pacing + the notification round-trips are rAF-driven and the headless preview throttles rAF to ~1fps, so the in-browser animation pacing is **flagged for manual verification** (documented Three.js/preview limitation) — the logic mirrors the verified notification-routing path, and the non-animated parts (Request Agreement modal open, reopen notification dispatch + render, chrome shift) are directly confirmable. **The RFP arc is fully closed — no open follow-ups.** Footer rolls forward to v0.17.5.2.2.

- **Phase 17.5.2.1 (prior phase):** **RFP arc closing polish (regression fix + transition smoothing + visual cleanup).** Six bundled items. **(Part 1) Tab-crash regression fixed** (`src/v2/V2App.jsx`) on the 17.5.1.5 Anchored RFPs row click. Root cause: `routeToRfp`'s rAF select-retry chain (`trySelect`) was never cancelled when `routeToRfp` was re-invoked, and 17.5.2 raised its cap 60 → 240 frames (~4s @ 60fps) — so repeated RFP navigation (Anchored-RFPs click → For-Asset round trip → click again, rapid re-clicks) stacked multiple concurrent 4-second chains, each filtering the ~23k-entry `allDots` every frame and firing `setPinned`/pan-zoom on resolution, saturating the main thread → perceived tab freeze. (The `routeToRfp` no-opts body was otherwise byte-for-byte identical to 17.5.1.5; the cap bump + lack of cancellation is the only behavioral delta.) Fix: hold the in-flight chain's rAF id in a new `routeRfpSelectRafRef`, `cancelAnimationFrame` it at the start of `routeToRfp` (and zero it on terminate / on the cap), and cancel it in `routeToAsset` too (so a lingering chain can't fire on the closing Directory) — only one chain is ever live. **(Part 2) Smooth Directory → Parent transition** (`src/v2/DirectoryLayer.jsx`): new module const `DIRECTORY_EXIT_ZOOM = 0.6` + a `phase === 'out'` effect that fires `animatedPanToWithZoom(camPos, Math.min(zoomRef.current, 0.6), 900)` — easing the camera zoom outward over the 900ms reverse circle wipe, parallel to the Parent pre-positioning `routeToAsset` already fires (`canvasRef…animatedPanToWithZoom(asset.x, asset.y, 1.0, 500)`, the verified `handleViewEa` convention). `Math.min` keeps it outward-only; gating on `phase === 'out'` makes it apply to every close path (globe button, exit chip, double-click escape, For-Asset route). **(Part 3) Anchored RFPs row restructure** (`src/components/DetailPanel/V22NodeDetailPanel.jsx`, `AssetPanelRfpRow`): icons dropped from CLOSE/REOPEN/REMOVE (text-only `ActionLabel`s); the right region switched to a vertical column (`flexDirection: column`, `alignItems: flex-end`, `gap: 4`, `minWidth: 52`) so REOPEN+REMOVE stack when both render (closed RFP), matching the AMEND/REVOKE EA-row treatment; REMOVE keeps its `danger` red. **(Parts 4+5) ASSET / ACTOR header badges**: `V22AssetPanel` + `V22ActorPanel` now pass `typeLabel="ASSET"` / `typeLabel="ACTOR"` to `PanelHeader` (reusing the existing TYPE_BADGE_BG mono-uppercase pill) — reversing the 17.3 #202 removal for these two so every Detail Panel header carries a type badge consistently. **(Part 6) Changelog modal header copy** (`src/v2/V2App.jsx`): "Prototype Changelog" → "Changelog", "Radiant V2 — PCN Prototyping" → "Radiant v2.2". **The RFP creation arc is now fully CLOSED** (#192 Item J done across 17.5 → 17.5.0.x → 17.5.1 → 17.5.1.x → 17.5.2 → 17.5.2.1). **Runtime status (build clean; no new console errors):** Parts 1, 3, 4, 5, 6 **confirmed in the dev preview** — the anchored-RFP click routes cleanly (Directory opens, RfpDetailPanel shows Sentinel-4, page responsive) with a clean For-Asset round trip + no console errors; open-RFP shows text-only CLOSE; closed-RFP shows REOPEN/REMOVE stacked (verified `verticalGap: 4`, same column) with REMOVE red (`rgb(239,68,68)`); ASSET + ACTOR badges render in their headers; Changelog reads "Changelog" / "Radiant v2.2" with the new v0.17.5.2.1 entry. **Flagged for manual verification:** Part 2's camera zoom-out pacing — the headless preview throttles rAF to ~1fps (a self-rescheduling rAF loop never completed in 30s), so rAF-driven animation timing can't be confirmed in-session; the Parent pre-positioning + the close path are confirmed and the zoom-out is wired into the verified `phase === 'out'` close effect (documented Three.js/preview limitation — manual pass needed at 60fps foreground). Footer rolls forward to v0.17.5.2.1.

- **Phase 17.5.2 (prior phase):** **Post-creation orchestration + bidirectional Parent↔Directory navigation (closes the RFP creation arc).** Three navigation/transition pieces. **(Part A) Post-creation orchestration** (`src/v2/V2App.jsx`): `handleRfpCreationSubmit` now stamps `v22RecentlyCreatedRfpId` (new state) + fires `routeToRfp(newRfp.id, { rfp: newRfp })` after adding the RFP. `routeToRfp` already zooms to `LOD_THRESHOLD` (full-card LOD) + auto-selects, so no zoom-level plumbing was needed; a new `opts.rfp` fallback handles the stale-`v22CreatedRfps`-at-submit-time case (the merged lookup can't resolve the just-built RFP before the setState flush). The submit handler (declared before `routeToRfp`) calls it via a `routeToRfpRef` to avoid a forward-dep TDZ. The reveal id is threaded to DirectoryLayer (`recentlyCreatedRfpId` prop) and stamped `_isNew` on the synthetic RFP card; `AssetNode`'s RFP branch renders a green NEW badge (mirrors the parent-layer Phase 11D reveal). A clear-on-deselect effect drops the flag when `v22DirectorySelectedRfp` is no longer the new RFP. Two robustness follow-ups landed during verification: (i) `animatedPanToWithZoom` + the entry effect now cancel/skip the Phase 17.3.2 cinematic zoom-out when routing to a just-created RFP (via a `recentlyCreatedRfpIdRef`), so the galactic-entry lerp to ~15% doesn't override `selectRfp`'s pan to full-card LOD; (ii) `routeToRfp`'s rAF select-retry cap was raised 60 → 240 frames (the fresh-create layout can take longer than ~1s to include the new RFP). **(Part B) RFP → Asset nav** (`RfpDetailPanel.jsx` + `V2App.jsx`): the "For Asset" row (Phase 17.2.1.1) is now clickable (hover tint + pointer + `›` chevron) → new `routeToAsset(assetId)` helper (mirrors `handleViewEa`'s close-Directory-then-pan pattern) closes the RFP/Claim panels + Directory (reverse wipe), then selects + pans/zooms to the Asset on the Parent canvas. Inverse of 17.5.1.5's click-to-open-RFP; wired as `onOpenAsset` on the RfpDetailPanel mount. **(Part C) Double-click escape** (`src/v2/DirectoryLayer.jsx`): the canvas container gains `onDoubleClick={handleCanvasDoubleClick}` that fires `onClose` (the existing reverse circle wipe used by the globe button / exit chip) on empty-space dblclicks — gated on `phase === 'in'` (no-op mid-transition), `e.target === rendererRef.current.domElement` (HTML overlays — cards, cluster-label pillboxes, chrome — are different elements), and a `raycast` miss (a marker hit belongs to the marker). No visible affordance (hidden shortcut per Andrew). **The RFP creation arc (17.5 → 17.5.0.x → 17.5.1 → 17.5.1.x → 17.5.2) is now CLOSED**; polish-backlog #192 Item J fully closed. **Runtime status (build clean; no new console errors):** **confirmed in the dev preview** — creating an RFP auto-switches Parent → Directory and auto-opens its RfpDetailPanel (the new RFP, with the clickable "For Asset" row + `›` chevron, renders correctly); **Part C confirmed** — invoking the canvas `onDoubleClick` with the Directory renderer canvas as target fired the reverse wipe back to Parent (`v22DirectoryOpen` → false, panels cleared), while a dblclick whose target is the *parent* canvas is correctly ignored. **Flagged for manual verification (not visually confirmed in-preview):** the auto-zoom to full-card LOD + the green NEW badge on the card, and the "For Asset" click landing on the Asset panel. In repeated preview runs `selectRfp` did not pin/zoom the freshly-created marker (Directory stayed at ~15% galactic zoom, so the card + NEW badge never reached card-LOD); the preview session was also badly destabilized (a 2px-wide viewport, repeated reloads). The code mirrors the notification-routing `selectRfp` path verified in Phase 17.2.0.2 + the verified `handleViewEa` close-then-pan pattern, so the most likely cause is preview-environment instability rather than a logic defect — but it needs a manual pass to confirm. Footer rolls forward to v0.17.5.2.

- **Phase 17.5.1.5 (prior phase):** **Asset Detail Panel "Anchored RFPs" section.** New section in `V22AssetPanel` (`src/components/DetailPanel/V22NodeDetailPanel.jsx`) listing every RFP whose `assetId` matches the active Asset — the parent-canvas surface for RFPs (which are otherwise Directory-layer-only). **Data** (`src/v2/V2App.jsx`): `anchoredRfpsForNode` derived inside the parent Detail Panel mount block from the full created→closed→removed merge chain (`mergeRemovedRfps(mergeClosedRfps(mergeCreatedRfps(sharedForPanel, v22CreatedRfps), v22ClosedRfpIds), v22RemovedRfpIds)`), filtered to `r.assetId === node.id`, sorted newest-first by `createdDate`. **Section** (always rendered, even empty → muted "No RFPs anchored to this Asset."): new `AnchoredRfpsSection` + `AssetPanelRfpRow` + `RfpStatusPill` components. Each row: line 1 = RFP name + status pill (green `OPEN` / muted `CLOSED`), line 2 = "Posted {date} · N requirements"; the row body (an `AgreementRow`) click-navigates via the new reusable `routeToRfp(rfpId)` helper (extracted from the `v22-rfp-solicitation-received` notification routing — `setSel(null)` to close the Asset panel, `clearEdgeUiState`, open Directory, clear Claim panel, set selected RFP, rAF-retry `directoryLayerRef.selectRfp`). **Owner-only lifecycle actions** render inline on the row's right via `ActionLabel` (matching the DA/EA revoke-button visual): Close (open RFPs) or Reopen + Remove (closed RFPs). `ActionLabel` gained a `danger` prop (red base + brightened-red hover) for Remove. Non-owner rows show identity + navigation only (no action buttons). The row handlers reuse `handleCloseRfp` / `handleReopenRfp` / `handleRemoveRfp` (Phase 17.5.1.4), wired on the V22NodeDetailPanel mount as `onOpenRfp={routeToRfp}` / `onCloseRfp` / `onReopenRfp` / `onRemoveRfp` — so Close from a row fires solicitor notifications, Remove silently cascades, and the row's status pill + buttons flip in-place (the mount recomputes `anchoredRfpsForNode` each render). **Runtime-verified** in the dev preview (the Asset Detail Panel opens via the HTML parent-canvas Asset card, so this surface is fully scriptable — unlike RFP-marker clicks): section renders, empty state on RFP-less Assets, Sentinel-4 row with OPEN pill + Posted date on Bob's Avionics Module, Close→CLOSED+Reopen/Remove in-place, Remove drops the row; build clean; no new console errors. Footer rolls forward to v0.17.5.1.5.

- **Phase 17.5.1.4 (prior phase):** **RFP lifecycle expansion (Close notifications + Remove + Detail Panel refresh + placeholder dimming).** Four bundled items. **(Fix A) Detail Panel Close/Reopen button refresh** (`src/v2/V2App.jsx` RfpDetailPanel mount): the mount's `mergedShared` chain now includes `mergeCreatedRfps` (+ `mergeRemovedRfps`), so a user-created RFP is present in the merged set and `currentRfp.status` flips Close→Reopen in-place without deselect/reselect (the seeded path already worked; the prior chain omitted `mergeCreatedRfps` and fell back to the stale snapshot). **(Fix B) RfpCreationModal placeholder dimming** (`src/components/modals/RfpCreationModal.jsx`): name + description placeholders dropped to `color-mix(in srgb, var(--text-muted) 70%, transparent)` via a scoped `.rfp-create-field::placeholder` rule (inline styles can't target `::placeholder`). **(Feature C) Close RFP notification** (`v22-rfp-closed`): centralized `handleCloseRfp(rfp)` records the closure + dispatches a notification to every UNIQUE solicitor party (deduped via a Set; zero solicitors → zero notifications); `handleReopenRfp` is silent (asymmetric). Muted-indigo badge (`color-mix(--accent-indigo 50%, --text-muted)`), `RFP CLOSED` label, body "{owner} closed the RFP …". Click routing added to the existing solicitation branch (Directory → closed RFP marker → panel); the routing RFP lookup now runs the full created→closed→removed chain. Both the panel footer and the card action bar route close/reopen through these handlers so the notification fires from either surface. **(Feature D) Remove RFP** (closed-only): new `v22RemovedRfpIds` Set state + `mergeRemovedRfps` export in `v2_2Data.js` (filters `shared.rfps` + cascades to drop referencing `rfpSolicitations`); `buildV22DirectoryDataForRole` signature extended to `(roleId, provisionals, closedRfpIds, createdRfps, removedRfpIds)` with merge order provisionals → created → closed → removed; threaded through DirectoryLayer (new prop + memo dep). `handleRemoveRfp` is silent (no notification) + clears the Directory selection if the removed RFP was open. Remove is gated to `'closed'` RFPs only — surfaced as a danger-variant `ActionButton` in the RfpDetailPanel footer (alongside Reopen, glyph `⊟`, tooltip warns it's permanent) + a trash-icon on the full-LOD card action bar (`_directoryRemoveCandidate` stamp in DirectoryLayer, owner + closed). **Runtime-verified**: build clean; data-probes confirm `mergeRemovedRfps` removes the RFP + cascades its solicitations to 0 across all four role views, and the close-notification solicitor set is deduped (MicroCo+AuditCo from 3 solicitations) + empty when no solicitors. Live-UI confirmation of the close-notification inbox + Remove-from-panel + button refresh is canvas-raycaster-gated (opening the RfpDetailPanel needs an RFP-marker click) — code- + build- + data-verified, manual-verify per the documented Three.js limitation. Footer rolls forward to v0.17.5.1.4.

- **Phase 17.5.1.3 (prior phase):** **Create RFP cleanup III (solicitation regression + RS Detail Modal subheader + closed-RFP visual).** Three QA fixes on the 17.5.1.2 Create RFP flow. **(1) Solicitation submission regression** (`src/v2/V2App.jsx`, `handleCreateSolicitation`): the handler resolved the target RFP via `mergeClosedRfps(buildV22SharedArtifacts(), v22ClosedRfpIds)` — omitting `mergeCreatedRfps`, so a user-created RFP wasn't in the lookup set and the handler bailed at `if (!rfp) return`, silently no-op'ing Submit. Fix threads `mergeCreatedRfps(…, v22CreatedRfps)` into the chain (+ deps). Data-probe confirms the old chain misses a created RFP while the new chain finds it (seeded Sentinel-4 unaffected). The seeded-RFP "Submit doesn't fire" case is the documented Phase 17.2.1.1 §8.9.6.1 Claim-greying predicate working as intended (already-on-buyer's-network Claims are greyed/unselectable + gate Submit) — no code change. Fix 1.A (inline amber explanation) skipped: a greyed Claim can't be selected (onClick gated to `undefined`), so the line it would show is unreachable; the "ON NETWORK" pill + not-allowed cursor + tooltip already communicate it. **(2) RequirementsSetDetailModal subheader** (`src/components/modals/RequirementsSetDetailModal.jsx`): branched on publication — published → globe + "Published by {owner}"; unpublished (RFP-referenced-but-not-on-network RSes reaching the modal via `v22RfpRsLookupPool`) → plain "by {owner}" with no globe. `isPublished` + `owner` are read off the enriched pool entry (Phase 17.5.1.2) with `_publishedBy`/membership fallbacks. Stops the "Published by an unknown actor" misframing for private RSes. **(3) Closed-RFP Directory marker** (`src/v2/DirectoryLayer.jsx`): converted from a 1px `THREE.LineSegments` (`LineDashedMaterial` — WebGL ignores `linewidth`, so it rendered as a hairline next to open RFPs' ~2px hollow-square ring) to a thick dashed ring of filled dash quads (new module helper `buildClosedRfpRingDashes` → `THREE.Mesh` + `MeshBasicMaterial` vertexColors, DoubleSide). Border thickness matches the open ring (`RFP_BORDER_SCREEN_PX / zoom` world units); the dash pattern stays as the lifecycle indicator; stroke color is a muted-indigo 50/50 blend (`--accent-indigo` lerped toward `--text-muted`, via new helper `closedRfpStrokeColor`). The hover/select per-vertex brightening was generalized from the hardcoded 8-vertex assumption to the build's `vertsPerRfp` (stored in `closedRfpVertsPerRfpRef`). **Runtime-verified**: build clean; Fix 1 data-probe (old chain misses created RFP → bug; new chain finds it → fix); Directory opens + renders with zero console errors (new closed-RFP `MeshBasicMaterial` scene-init + `buildClosedRfpRingDashes` empty-centers rescale path don't crash). The closed-marker VISUAL + RS-modal subheader + full solicitation UI are canvas-raycaster-gated (opening the RfpDetailPanel needs an RFP-marker click; scripted full-LOD zoom coalesces unreliably) — confirmed code- + build- + data-verified, manual-verify per the documented Three.js limitation. Footer rolls forward to v0.17.5.1.3.

- **Phase 17.5.1.2 (prior phase):** **Create RFP cleanup II (cluster integration + RS click + RS row layout).** Three QA fixes on the 17.5.1.1 Create RFP flow. **(1) Cluster integration for cross-actor RFPs** (`src/v2/v2_2Data.js`, `buildV22DirectoryDataForRole`): created RFPs now join their owner's Directory cluster on every viewer's Directory instead of the orphan path. Root cause of the duplicate "GovCo" label: buyers like GovCo have no public Claims → no Claim cluster, and the `PRIMARY_PARTIES_FOR_ORPHAN_RFP` carve-out forced their RFPs through the orphan path, which renders one owner-pillbox-label PER orphan marker — so two GovCo RFPs (seeded Sentinel-4 + a user-created one) produced two labels. Fix: removed the carve-out; (a) attach each owner's RFPs to their existing Claim cluster, (b) upsert an RFP-only cluster for every remaining RFP owner (incl. the four primary switchable parties), (c) `otherRfps` now holds only genuine clusterless orphans (normally empty). The 17.5.1.1 orphan-spread positioning is preserved as the fallback. Each owner is now exactly one cluster with one label. **(2) RS chip click handler** (`src/v2/V2App.jsx`): the RequirementsSetDetailModal resolution now reads `v22RfpRsLookupPool` (published + RFP-referenced RSes) instead of `publishedRequirementSets` alone — own-private RFP-referenced RSes (e.g. GovCo's Material Compliance) are now clickable from the RFP Detail Panel. Single source for chip rendering and chip click. **(3) Two-line RS rows** (`src/components/DetailPanel/RfpDetailPanel.jsx`, `RequirementRow`): line 1 = RS name + canonical GlobeIcon (rendered whenever the RS is in `publishedRequirementSets`, regardless of owner or viewer) + version pill; line 2 = owner party name in muted secondary text (no "YOU" pill — the panel header already carries it for the RFP). `v22RfpRsLookupPool` entries gain derived `owner` (resolved via a roleId→party map over `DEMO_REQUIREMENT_SETS`, `_publishedBy`, or the active actor's party) + `isPublished` fields; CombinedRequestModal + SolicitationCreateModal RS sectioning unchanged (one-line shape). **Runtime-verified**: build clean; app loads + role-switch + full create-RFP flow (modal → publish) + Directory open across Bob/Alice with zero console errors; exactly ONE "GovCo" pillbox cluster label on Alice's Directory (Fix 1). Data-probe verified: a created GovCo RFP joins GovCo's cluster (with seeded Sentinel-4) + `otherRfps` empty on Alice/Carol/Dave; a created MicroCo RFP joins MicroCo's existing claim cluster on Bob's view; exactly one cluster per owner; pool enrichment resolves owner=GovCo + isPublished correctly (true for the three published RSes, false for Material Compliance). RfpDetailPanel two-line row VISUAL + private-RS click confirmed code- + data-verified (RFP-marker click is canvas-raycast-gated per the documented Three.js limitation; the panel rows are presentational, fed by verified-correct pool data). Footer rolls forward to v0.17.5.1.2.

- **Phase 17.5.1.1 (prior phase):** **Create RFP cleanup (cross-actor visibility + implicit-publication-via-reference + Your-first RS ordering).** Three QA fixes on the 17.5.1 Create RFP flow. **(1) Cross-actor RFP visibility** (`src/v2/DirectoryLayer.jsx`): a created RFP now shows on every actor's Directory, not just the creator's. Root cause was NOT data/plumbing (verified correct — `v22CreatedRfps` flows unconditionally, memo deps + `mergeCreatedRfps` right) — DirectoryLayer's orphan-RFP positioning placed every off-canvas-cluster RFP at one fixed coordinate (`userCenterX+600, userCenterY-320`), so a created GovCo RFP rendered exactly on top of the seeded Sentinel-4 RFP. Fix: a per-anchor index offsets same-owner orphan RFPs into a readable column (`baseY + idx * DOT_GRID * 2`). **(2) Implicit publication via reference** (`src/v2/V2App.jsx`): new `v22RfpRsLookupPool` useMemo (published RSes + every RFP-referenced RS resolved against network-wide `DEMO_REQUIREMENT_SETS` + the active actor's own RSes) passed to RfpDetailPanel + SolicitationCreateModal — a private RS named in an RFP now resolves instead of rendering "Standard not found". Resolution-only; the user-facing RS picker + Library are unaffected. **(3) Your-first RS ordering** (`v22AvailableRequirementsSetRows` + RfpCreationModal + CombinedRequestModal): rows gain `isOwn`; both modals render Your RSes first (own-and-published rows stay in Your with a trailing globe + tooltip "This Requirements Set has been published to the Radiant Network for any party to evaluate against."), non-own Published second (leading globe + "Published by {owner}"); empty sections skip their header. **Runtime-verified**: Bob's modal shows only YOUR (his published RSes carry globes+tooltip, Material Compliance no globe); Alice's modal shows YOUR (Incoming QC, no globe) then PUBLISHED (GovCo's RSes, "Published by GovCo"); the own-published globe tooltip renders on hover; data-layer probe confirms the RFP-RS pool resolves a private referenced RS; build clean; no console errors across the role-switch + both modal flows. Footer rolls forward to v0.17.5.1.1.

- **Phase 17.5.1 (prior phase):** **Create RFP flow (RfpCreationModal scaffolding).** The user-facing RFP creation flow — third leg of the Directory triad (browse + connect + create). **New `src/components/modals/RfpCreationModal.jsx`** — two-step modal mirroring CombinedRequestModal's `Modal`/`StepDots`/footer pattern. Step 1 (Form): RFP name (required) + description (required) + Requirements Sets multi-select scrollbox (Published section with globe + "Published by", then Your section, deduped); Continue gated on `name.trim() && description.trim() && selectedRsIds.size > 0`. Step 2 (Review): labeled summary (For Asset / RFP Name / Description / RS chips) + Back (preserves form state) / Publish. **Entry** from the Phase 17.5 "Create RFP" (`⬚`) button on the Asset card action bar or Detail Panel footer; `handleCreateRfp` now opens the modal pre-anchored (`v22RfpCreationContext = { asset }`) instead of logging. **Submit** (`handleRfpCreationSubmit`) builds the RFP via `makeRfp` (`id = makeArtifactId('rfp', '{party}-{slug}-{Date.now()}')`, `assetId` from the anchor, `status:'open'`) and adds it to the new `v22CreatedRfps` Map session state. **New `mergeCreatedRfps` export** in `v2_2Data.js` (mirror of `mergeClosedRfps`); `buildV22DirectoryDataForRole` signature extended to `(roleId, provisionals, closedRfpIds, createdRfps)` with merge chain **created → closed** (same-session create+close works). Threaded through DirectoryLayer (new `v22CreatedRfps` prop → builder 4th arg + memo dep). The RS-row pre-shaping is extracted to a shared `v22AvailableRequirementsSetRows` useMemo; CombinedRequestModal refactored to consume it (same output). Parent-canvas view-builder unchanged (RFPs are Directory-layer-only). **Runtime-verified** in the dev preview: modal opens pre-anchored from the footer; Continue correctly gated (disabled until name + description + ≥1 RS); both RS sections render; Step 2 review shows Asset/name/description/RS chips; Back preserves all state; Submit closes the modal with no console errors; data-layer probe confirms `mergeCreatedRfps` + the created RFP landing in the owner's `ownRfps`. Post-creation orchestration (layer switch + pan/zoom + auto-open Detail Panel + NEW badge) is **Phase 17.5.2**. Footer rolls forward to v0.17.5.1.

- **Phase 17.5.0.4 (prior phase):** **Disclosure boundary cleanup (umbrella DA pull-in + catalog Asset backfill + Library polish).** Three bundled demo-prep fixes in `src/v2/v2_2Data.js` (+ a Library polish). **(1) View-builder umbrella-DA pull-in fix.** `buildViewForActor()` gains an `isFullyAnchoredDa(da)` predicate (declared after `pairedDaIds`): a DA is "fully anchored" iff EA-paired (`pairedDaIds.has(da.id)`), OR subject is a PoE / Eval Result, OR it's a Phase 11D.3 proof-only Claim DA with populated `scope.poeIds`. Two parent-canvas pull-in loops now gate on it — the proof-only Claim pull-in and the grantor-side `granteeAssetId` Asset pull-in. Umbrella DAs (Phase 16.0 / 17.4 catalog visibility) fail all three checks → no longer leak onto the parent canvas; Directory-layer visibility unaffected. Probe-verified: Bob no longer shows `cChipcoFlashMem`/`cChipcoAdcDac`/`cMicroThermalPad`; Alice no longer shows `cChipcoAdcDac`; Dave no longer pulls `bAvionics`/`cAuditWorkspace`. Phase 11D.3 carveout preserved (Dave still pulls `cPrm` + Bob's PoE + Bob's ER via `daAliceToDavePrmProof`); EA-paired pull-ins intact (Bob ← cPrm/cVreg, Carol ← cPrm/cEmi, Alice ← bAvionics/cAuditWorkspace). **(2) Catalog Claim Asset backfill.** 17 new stub Assets (12 ChipCo + 5 MicroCo) added; the 12 Phase 16.0 Dave catalog Claims + 5 Phase 17.4 Alice umbrella-seed Claims now carry single-element `referencedAssetIds`; added to `daveOwnAssets`/`aliceOwnAssets` + the top-level `assets` array. The existing `claimRefEdges` loop emits their `da-ref-` DAs automatically — probe confirmed no new duplicate-id collisions (the 4 pre-existing benign ChipCo PrmIc/Vref `da-ref`/`da-parse` dupes are unchanged and don't touch the new assets). No more floating Claim nodes. **(3) LibraryModal Published Requirements empty state** gains the canonical 40px wire-globe icon (opacity 0.3, marginBottom 16) for parity with the other tabs — runtime-verified in the dev preview. Build clean; console shows only pre-existing benign DirectoryLayer overflow warnings. Footer rolls forward to v0.17.5.0.4.

- **Phase 17.5.0.3 (prior phase):** **Universal neutral footer buttons.** Completed the "all footer buttons neutral" policy from Phase 17.5.0.1 by dropping the `accent` prop from every remaining `FooterButton` call site in `V22NodeDetailPanel.jsx` — 11 call sites (12 `accent` keywords; Create PoE has active + 17.5.0.2-disabled branches): Actor Register Asset, Claim revoked-branch Dismiss, Claim owner Self-Evaluate, Claim non-owner Run Evaluation (active + pending-acceptance disabled), Claim warm + cold Request Evaluation Agreement, Eval Result orphaned Dismiss, Eval Result Create PoE (both branches), PoE Issue Badge, Badge Template Create new version. Every footer button now shares the single indigo-bordered icon-with-hover-expand-label treatment; no button is filled-indigo by default. `FooterButton` itself is unchanged. **`danger` and `amber` preserved** (Cancel Transfer, Cancel Request → red; Respond to Request → amber) — they carry semantic urgency, not "primary CTA" highlighting. Runtime-verified in the dev preview: Run Evaluation, Issue Badge, and Create PoE (previously accent-filled `rgb(129,140,248)`) now render with transparent background + the 40%-indigo neutral border; build clean; only pre-existing benign `NaN`-CSS-`left` transients in console. Footer rolls forward to v0.17.5.0.3.

- **Phase 17.5.0.2 (prior phase):** **Cross-panel footer icon propagation + visible-but-disabled audit.** Propagated the icon-with-hover-expand-label footer pattern (Asset footer in 17.5 / polished 17.5.0.1) to every other `FooterButton` call site in `V22NodeDetailPanel.jsx` — Actor / Claim / Eval Result / PoE / Badge Template panels (Parse Result has no footer). `FooterButton` itself is unchanged; each edit just passes the new `icon` prop. Icons reuse V22ActionBar glyphs where they overlap (Amend Claim `✎`, Self-Evaluate / Run Evaluation `◆`, Re-run `↻`, Create PoE `◈`, Request EA `▷`, View EA `◉`, Issue Badge = `BadgeShieldIcon`, etc.); three footer-only actions get new picks — Dismiss `⊠`, Respond to Request `↪`, Create new version `＋`. **New behavior:** the Eval Result panel's Create PoE now renders **visible-but-disabled** when `node._alreadyWrapped` (chain PoE-terminated), mirroring Re-Run's existing Phase 13.1 / 13.3 treatment — previously it disappeared. This required dropping the `!node._alreadyWrapped` gate on V2App's `onCreatePoE` prop (line ~8354) so the handler reaches the panel; the panel owns the enabled/disabled decision and the disabled button carries no onClick (a wrapped chain still can't be re-finalized). **Deviation flagged:** the brief listed V2App changes as Changelog+version only, but QA #8 can't pass without the `onCreatePoE` wiring change — made it and noted it. **Scope boundary flagged:** `RfpDetailPanel.jsx` (local `ActionButton`) and the DA / EA agreement panels use their own button components, not `FooterButton`, so per the stop-and-flag rule they're left untouched (follow-up migration). Runtime-verified in the dev preview: Claim panel renders Run Evaluation `◆` + Issue Badge shield SVG; Eval Result renders Re-run `↻` + Create PoE `◈`; the PoE-terminated Eval Result shows both as visible-but-disabled (`cursor: not-allowed`); a non-terminated Eval Result keeps Create PoE enabled; build clean; only pre-existing benign `NaN`-CSS-`left` transients in console. Footer rolls forward to v0.17.5.0.2.

- **Phase 17.5.0.1 (prior phase):** **Asset footer styling polish.** Tightens the visual treatment of the 17.5 Asset Detail Panel footer. Three changes to `FooterButton`'s icon-mode neutral default state (`src/components/DetailPanel/V22NodeDetailPanel.jsx`): (1) border picks up a 40%-indigo blend (`color-mix(in srgb, var(--accent-indigo) 40%, var(--border))`, matching `AssetNode.jsx`'s `WARM_BORDER`); (2) background tints to `var(--bg-raised)` on hover; (3) border deepens to a 60%-indigo blend on hover — all on a 120ms `background`/`border-color` transition. Gated on `isNeutral` (`!accent && !danger && !amber && !disabled`) and scoped to the icon-mode branch, so variant buttons (accent/danger/amber), disabled buttons, and the legacy label-only branch on other panels are untouched (cross-panel propagation is a later phase). Plus: Request Agreement in the Asset footer drops its `accent` prop, so all six Asset-footer buttons now read as a uniform neutral row with no single button visually dominant. Runtime-verified in the dev preview; build clean. Footer rolls forward to v0.17.5.0.1.

- **Phase 17.5 (prior phase):** **RFP creation entry-point foundation.** First phase of the 17.5.x arc (user-facing RFP creation — the third leg of the Directory triad: browse + connect + create). **`FooterButton` refactor** (`src/components/DetailPanel/V22NodeDetailPanel.jsx`): new `icon`-with-hover-expand-label pattern — default state shows the icon only, hover animates the button width (~150ms ease) to reveal the label inline, and the explanatory Tooltip from `title` still pops (label = verb, tooltip = explanation). The `icon` prop is **gated**: when present → the new collapse/expand button (`flex: 0 0 auto`); when absent → the legacy label-only button (`flex: 1`) unchanged — preserving the ~18 other FooterButton call sites that pass no icon. (Brief said "required `icon`"; made it optional/gated to avoid breaking those — surfaced as a deviation.) **6th action "Create RFP"** added on both the V22ActionBar ASSET branch (`AssetNode.jsx`) and the Asset Detail Panel footer; order: Register Asset → Request Agreement → Parse Evidence → Create Claim → Create RFP → Transfer (Cancel-Transfer pending button also gets the `✕` icon). The Asset footer's six buttons sit in a scoped inner flex row so hover expansion doesn't squeeze siblings; `PanelLayout`'s shared footer container is untouched. Both surfaces dispatch `createRfp` via `onV22CardAction` / `onCreateRfp`; **`handleCreateRfp` in V2App is a placeholder logger** in 17.5 — the RfpCreationModal lands in 17.5.1. Icon `⬚` matches `CATEGORY_CONFIG.rfp.icon`. Closes the Phase 10.2 "crowded footer" flag + fixes the long-standing Transfer cut-off. Runtime-verified in the dev preview (parent-canvas Asset cards are clickable HTML, so the Detail Panel opened via DOM despite the 3D-raycaster limitation): footer renders 6 collapsed icon buttons (~40px), hover expands Register Asset 40→123px (label maxWidth 0→220px, opacity 0→1), both footer + action-bar Create RFP log `[Phase 17.5] createRfp dispatched for Asset: …`, Create Claim still opens its modal, build clean. Footer rolls forward to v0.17.5.

- **Phase 17.4.5 (prior phase):** **Internal "ownership" DA leak fix on Directory Claim panel.** When Alice (MicroCo) viewed a ChipCo Claim's Detail Panel on the Directory layer, the Agreements section leaked ChipCo's internal ownership DA next to her legitimate umbrella DA. **Diagnosis** (a seed-data bug, not a panel/filter bug — the panel renders strictly from `disclosureAgreementsForNode` and the V2App filter is correct): `v2_2Data.js`'s `aliceOwnClaims = claims.map(...)` iterated the full primary-Claim array while hardcoding `owner: alice.party`, minting spurious MicroCo→MicroCo internal ownership DAs over every ChipCo Claim (ids colliding with `daveOwnClaims`'). The MicroCo/MicroCo duplicate passes the party-presence filter because Alice IS both parties. **Fix:** restrict `aliceOwnClaims` to `claims.filter((c) => c.owner === alice.party)`. Owner-side preserved — Dave still sees his ChipCo internal ownership DA on his own Claims. Backlog #204 (extract `makeOwnershipDAsForActor` helper) + #205 (tidy duplicate `da-ref`/`da-parse` ids) filed in Phase 17.4.6 as seed-resilience carry-overs. Footer rolled forward to v0.17.4.5.

- **Phase 17.4.4 (prior phase):** **Revoke wiring on Directory Claim panel + #203 backlog entry.** The Directory V22ClaimPanel mount (`v22DirectorySelectedClaim`) rendered the umbrella DA row's Revoke button, but it did nothing — the mount was missing the `onRevokeDa` / `onRevokeEa` props. Wired both to the existing `handleOpenRevocationConfirm(agreement, type)` (the same handler the parent canvas uses, which already handles grantee-side revocation by branching on `agreement.grantor.party === activeRole.party`). No new handler logic. After confirm, the reactive flow (`setV22Provisionals` → `buildV22DirectoryDataForRole` filtering `!d._revokedMeta`) reshapes the perimeter + drops umbrella-only Claims automatically; also-public Claims stay visible as public dots. Filed backlog #203 (rare edge-tooltip stickiness across layer transition). Footer rolled forward to v0.17.4.4.

- **Phase 17.4.3 (prior phase):** **Polish wrap fix: Minkowski perimeter + legend z + edge tooltip cleanup.** Two 17.4.2 items still misbehaving + a long-standing ghost-tooltip bug. **Minkowski perimeter** — `umbrellaOutlinePath`'s 3+ branch now calls `collinearStadium` (Minkowski-sum-with-disk capsule) unconditionally; the 17.4.2 `isCollinear` gate (epsilon `DOT_RADIUS / 2`) was too tight, so near-collinear subsets (Bob's 7 ChipCo umbrella dots in a vertical strip) fell through to `offsetPolygonOutward` on a thin hull and still rendered as a line. `isCollinear` + `offsetPolygonOutward` retained but marked unused (`void`). **Legend z** — `DirectoryLegendBar` `zIndex: 50 → 400`, above the active cluster label (`Z_ACTIVE_CLUSTER_LABEL = 300`) that was covering it. **Edge tooltip cleanup** — new `clearEdgeUiState()` helper (`clearHoverState()` + `setEdgeHover(null)` + `setEdgeMenu(null)`) called from all three Directory-open paths (chrome/globe toggle, node-double-click, RFP-solicitation notification) + the role-switch handler. Root cause: V2App's `edgeHover`/`edgeMenu` (the pinned "SELECT EDGE TO VIEW" box) were never cleared on layer/role change — only V2Canvas's internal hover indicator was — so a pinned edge menu ghosted over the Directory and reappeared on role switch-back. Footer rolls forward to v0.17.4.3.

- **Phase 17.4.2 (prior phase):** **Polish wrap fix: collinear perimeter + legend positioning + distance-based active-cluster buffer.** Three 17.4.1 items that visual QA showed didn't land. **Collinear perimeter** — the 17.4.1 inflation halving was correct, but a straight-grid-row umbrella subset made `convexHull` degenerate and `offsetPolygonOutward` rendered it as a line. New `isCollinear` test (perpendicular-distance epsilon `DOT_RADIUS / 2`) routes the degenerate case to a new `collinearStadium` (Minkowski-sum-with-disk: circle around each point + convex hull, generalizing the 2-dot stadium). Genuinely-2D subsets keep the existing `offsetPolygonOutward` path. **Legend positioning** — `DirectoryLegendBar` `bottom: 12 → 40` to clear the ~32px global app footer bar that was covering it. **Distance-based active-cluster buffer** — the 17.4.1 area-based `4 × DOT_GRID²` buffer only widened the cell ~1 dot (Lloyd's redistributes non-isotropically), so it's removed; replaced by a post-Lloyd's seed correction that pushes each neighbour seed radially outward from the active seed until the active + neighbour cluster edges (radius ≈ `sqrt(targetArea / π)`) are ≥ `8 × DOT_GRID` apart, then recomputes the final Voronoi with adjusted seeds. Seeds clamp to the usable canvas; clamped-short cases log a warning + accept the partial buffer. Footer rolls forward to v0.17.4.2.

- **Phase 17.4.1 (prior phase):** **Polish wrap: perimeter shape fix + Directory legend + active-cluster buffer + active label z.** Four QA-driven polish items on the umbrella DA arc. **Perimeter inflation halved** (`DOT_GRID` → `DOT_GRID / 2`) across all three `umbrellaOutlinePath` branches (1-dot circle, 2-dot stadium, 3+ hull offset) so corners sit at the midpoint between umbrella dots and adjacent non-umbrella dots (was over-extending a full cell, cutting through dot positions). Geometry function otherwise unchanged. **New `DirectoryLegendBar`** (bottom-left, mirrors V2Canvas's parent LegendBar): Full/Selective/Proof-Only disclosure dots (indigo/amber/green) + Request for Proposals hollow square (indigo); inline hover tooltips (no portal). **4-dot buffer** added to the active actor's own cluster in the Lloyd's target-area computation (`isOwnCluster` spec gains `4 × DOT_GRID²` before the overhead factor) so neighbouring mock clusters stop crowding it. **Active actor label z-index** bumped to a dedicated `Z_ACTIVE_CLUSTER_LABEL = 300` (above other cluster labels' 100–~200 ranks, below RFP labels/zoom controls/cards/tooltips). Footer rolls forward to v0.17.4.1.

- **Phase 17.4 (prior phase):** **Umbrella DA arc: perimeter fix + seed expansion + Directory panel/card parity + perimeter hover.** Andrew's Option #3 (grouping perimeter, no edges) ratified. **Perimeter rendering fixed** — root cause: `packClusterDense` placed the umbrella subset on the innermost cells (sorted by distance from center), which RING the central label hole, so the convex-hull perimeter enclosed the hole + actor pillbox. Fix: the packer relocates the umbrella subset into a coherent edge-blob (anchored at the inner cell furthest from center, grown by nearest-neighbor) so the hull is a tight off-center polygon wrapping just the umbrella dots; RFP cells stay outermost; `umbrellaOutlinePath` unchanged. **Seed expansion** — 14 new umbrella DAs: Alice→Bob (5, type variety), Alice→Carol (3, full), Dave→Alice (3, 2 selective + 1 proofonly), Dave→Carol (3, full). 5 new non-public MicroCo Claims added (MicroCo's 3 existing are all public, and public takes precedence over umbrella). Demo: Bob sees ChipCo (7) + MicroCo (5) perimeters; Alice ChipCo (3); Carol MicroCo (3) + ChipCo (3); Dave none. **Directory V22ClaimPanel parity** — the Directory mount now threads the DA/EA Agreements section lists (from merged shared artifacts, filtered to this Claim + the active actor's agreements) + name resolvers, so umbrella Claims show their DA + the warm-path Request EA footer button (opens EARequestModal). Cold-path + View-EA states from 17.3 preserved; mutation handlers omitted (grantor-side actions; umbrella viewer is grantee). **Card action bar warm-path parity** — new `_directoryWarmPathRequestCandidate` marker (DA exists, no EA), mutually exclusive with cold-path + View-EA markers; AssetNode renders the warm-path Request EA button; DirectoryLayer receives `disclosureAgreements` to compute it. **Perimeter hover + tooltip** — point-in-polygon hit-test in the mouse-move handler (independent of the dot raycast so it never blocks clicks); hover bumps border 0.55→1.0 + stroke 1.5→2.5px + fill 8%→16%; HTML tooltip "You have Disclosure Agreements with the Claims inside this perimeter." + "From {grantor}" sub-line. Footer rolls forward to v0.17.4.

- **Phase 17.3.2 (prior phase):** **Fan-out improvements + leak investigation + RFP owner card action bar.** Five items batched before Phase 17.4 opens. **Fan-out jitter** bumped 250 → 600 ms; **wave speed** bumped 7000 → 9500 wu/sec — wavefront travels faster; jitter is now ~50% of base radial duration so per-instance randomness dominates the perception of "next dot" for a clearly jagged organic edge. **CSS wipe duration** bumped 550 → 900 ms (`clip-path` transition in DirectoryLayer's scene container) so the wipe and fan-out feel like one continuous transition rather than wipe-then-dots. **Cinematic zoom-out** during fan-out: camera starts at `INITIAL_ZOOM × 1.3` (30% tighter view) and eases out to `INITIAL_ZOOM` over 1500 ms via the same `easeOut` curve. New `cinematicZoomRafRef` cancels the lerp on role-switch / close-during-zoom. **Safari crash investigation** — root cause: WebGL context exhaustion from rapid Directory open/close cycles (each cycle disposes + recreates the entire Three.js renderer; Safari's ~16-context limit + slow driver release lets cycles 10+ accumulate). **Surgical fix applied**: scene cleanup now calls `WEBGL_lose_context.loseContext()` before `renderer.dispose()` to release the context synchronously. React state-thrashing suspect ruled out (React 18 batching). **RFP owner Close/Reopen action bar** on the full-size card — DirectoryLayer stamps `_directoryCloseCandidate` (owner + open) and `_directoryReopenCandidate` (owner + closed); AssetNode RFP early-return renders padlock-icon button; V2App routes through the same `setV22ClosedRfpIds` updates the panel footer fires. Footer rolls forward to v0.17.3.2.

- **Phase 17.3.1 (prior phase):** **Polish wrap: RFP card action bar + RfpDetailPanel cleanup + Requirements rows + fan-out animation tweaks.** Four items batched before Phase 17.4 opens. **RFP full-size card** gains a Solicit-with-my-Claim action bar parallel to the Claim card pattern shipped in 17.3. DirectoryLayer stamps `_directorySolicitCandidate` on the synthetic RFP node when the active actor is a non-owner of an open RFP with no existing solicitation from them; AssetNode's RFP early-return surfaces a paper-airplane SVG icon button on hover/select. Click dispatches `solicitWithClaim` through `onV22CardAction` and V2App routes to the same SolicitationCreateModal the panel footer opens. **RfpDetailPanel categorical pill cleanup** extends #202 from 17.3: "POSTED BY" row drops the "ACTOR" pill; "FOR ASSET" row drops the "ASSET" pill. Relational headers + the "YOU" indicator preserved. #202 now fully closed across V22NodeDetailPanel + RfpDetailPanel. **Requirements section overhaul**: renamed "Required Standards" → "Requirements"; layout converted from horizontal chips to vertical full-width clickable rows with version-as-small-badge-pill on the right; hover state + click open the new RequirementsSetDetailModal. Missing-RS rows render dashed-border muted "Standard not found ({rsId})" non-clickable. **New RequirementsSetDetailModal** (`src/components/modals/RequirementsSetDetailModal.jsx`) — read-only RS view: header (name + version badge), sub-header (Published by + globe icon), scrollable body with each requirement as id-pill + label + description + optional Criterion. ESC + backdrop close via shared Backdrop primitive. **Fan-out animation tweaks**: `directoryLoadAnimation.js` waveSpeed 4500 → 7000 wu/sec; dotFadeMs 200 → 90 ms; new `jitterMs` (default 250 ms) seeded random per-instance start offset (xmur3 + mulberry32 PRNG, seeded by `{roleId}:{i|r}` so mid-flight re-renders stay consistent while each entry rolls fresh) gives a jagged organic wavefront; eased via `easeOut(t) = 1 - (1-t)^3`. RFP markers participate via new `rfps` + `setRfpAppear` API. **DirectoryLayer per-instance scale-driven appearance** (replaces 16.2.3 color-multiplier-from-black): `dotOpacitiesRef` reinterpreted as per-instance MATRIX SCALE factor (0 = invisible, 1 = final size). Dots render at final color from the moment they appear; the wave grows each dot from a point to its full size. New `flushDotMatrices` + `flushRfpMatrices` integrated into the animate-loop dirty-flag pattern. `flushDotColors` simplified (no opacity multiplier). RFP hit-test mesh keeps full scale (clickable mid-wave). Footer rolls forward to v0.17.3.1.

- **Phase 17.3 (prior phase):** **Claim Detail Panel EA content on Directory + card action bar + #202 ACTOR/ASSET label cleanup.** Opens the action surface for Directory-layer Claim discovery. **V22NodeDetailPanel Claim view** gains an EA-status section near the top of the body — "An Evaluation Agreement is required to evaluate this Claim." (no-EA cold-path, amber callout) or "An Evaluation Agreement is in place with {owner}." (EA-exists, indigo callout). Footer extensions match: "Request Evaluation Agreement" (opens AssetPickerModal cold-path) or "View Evaluation Agreement" (closes Directory, pans parent canvas to the Claim node, selects the EA edge, opens the EA Detail Panel via the existing `setOpenAgreement` mechanism). Owner viewing own Claim gets no EA-status section + no footer EA actions. The pre-existing Phase 11C warm-path (DA exists, no EA) is preserved unchanged. **AssetNode V22ActionBar Claim branch** surfaces parallel CTAs on Directory full-size Claim cards — Request EA (▷) when the synthetic node carries `_directoryRequestEaCandidate`, View EA (◉) when it carries `_directoryExistingEa`. Both stamps are driven by DirectoryLayer's card-overlay render path, which resolves the EA via `getActiveEaForClaimAndRequester`. Parent-canvas Claim cards unaffected. **AssetPickerModal** re-mounted in V2App for the cold-path Directory Claim CTA, with a generalized prop contract: accepts a generic `targetClaim` + `context` ({ type: 'directory-claim' } | { type: 'rfp', rfp }) — context drives the subtitle / context block. Legacy solicitation/solicitorClaim/rfp props preserved as fallback for future re-use. Continue resolves the picked Asset + Claim and opens CombinedRequestModal pre-filled (`requesterAsset` + `initialPin`, no `initialRequirementsSetIds` since no RFP context). **New shared predicate** `getActiveEaForClaimAndRequester(claim, requesterParty, evaluationAgreements)` exported from `v2_2Data.js`, filters out declined/revoked EAs, prefers active over pending-acceptance + most recent by `terms.createdDate` on tie-breaking. **V2App handlers**: `handleRequestEaForClaim`, `handleAssetPickedForClaim`, `handleViewEa` (declared after `nodeMap` + `canvasRef` are in scope to avoid TDZ on the useCallback deps array). DirectoryLayer threads `evaluationAgreements` + `onClaimCardAction` props end-to-end. **#202 closed**: ACTOR + ASSET typeLabel pills removed from V22ActorPanel + V22AssetPanel headers; the PanelHeader's `typeLabel` prop is now optional. CLAIM / EVAL RESULT / PARSE RESULT / PROOF OF EVALUATION / BADGE TEMPLATE panels keep their type label since those are abstract artifact types. RfpDetailPanel's relational "Posted by" + "For Asset" rows unchanged. Footer rolls forward to v0.17.3.

- **Phase 17.2.1.2 (prior phase):** **Hotfix: CombinedRequestModal RS pre-selection + Detail Panel label backlog entry.** Closes the last QA item against the 17.2 arc. Root cause: V2App's `handleRequestAgreement` only stored the first id of `rfp.requirementsSetIds` (`suggestedRequirementsSetId` singular) and the CombinedRequestModal mount wrapped it back into `[id]`, so multi-RS RFPs (Bob's Sentinel-4 referencing both `MIL-PRF-55681 Compliance v2` and `System Integration Requirements v1`) only pre-checked the first RS. Fix threads the full `rfp.requirementsSetIds` array as `suggestedRequirementsSetIds` (plural); the mount block prefers the plural array, falling back to the singular for the AI Shopper cold-path entry. CombinedRequestModal's `useState(() => [...initialRequirementsSetIds])` initializer was already correct — bug was entirely upstream at the V2App handler + mount. New backlog **#202** filed: Remove "ACTOR" and "ASSET" small-caps labels from Detail Panels (pairs naturally with Phase 17.3 Claim Detail Panel content work). Footer rolls forward to v0.17.2.1.2.

- **Phase 17.2.1.1 (prior phase):** **Hotfix: RFP-bound Asset + Claim greying + EA+DA notification Directory close + CombinedRequestModal RS scrollbox.** Four QA-driven items closing out the 17.2 arc. **RFP factory** `makeRfp` extends the required field set with `assetId` (validated, throws on missing); all 118 seeded RFPs populated — Bob's Sentinel-4 RFP binds to `bAvionics` ("Avionics Module"); each of the 20 RFP-only mock-cluster owners gets one stub Asset (`asset-{actor.id}-rfp-anchor`, owner = actor.party); mixed-cluster owners reuse their first existing Asset. `generateRfpsForActor(actor, count, defaultAssetId)` requires `defaultAssetId` so missing-Asset seed inconsistencies surface at build time, not at render time. **RfpDetailPanel** gets a new "For Asset" row between the Posted-by row and the Description block — ASSET pillbox + Asset name; "(Asset not found)" muted fallback when `rfp.assetId` doesn't resolve. V2App threads a derived `assetsById` Map down to the mount. **Accept flow simplified**: AssetPickerModal removed from the Accept path — `handleRequestAgreement` goes directly to CombinedRequestModal pre-filled with the RFP's bound Asset (via `requesterAsset`), solicitor's Claim PIN (`initialPin`), and RFP's `requirementsSetIds` (`initialRequirementsSetIds`). AssetPickerModal component file preserved for future create-RFP work; only the V2App mount + handlers were removed from the Accept flow path. `v22AcceptingSolicitation` retained as the in-flight context object. **SolicitationCreateModal** Claim picker greys out Claims already mapped to the RFP owner via an active or pending EA (predicate: `grantee.party === rfp.owner && grantor.party === solicitorParty && subject.kind === 'claim' && !_declineMeta && !_revokedMeta`). Greyed rows render at opacity 0.45 with `cursor: not-allowed`, no click handler, "ON NETWORK" pillbox, tooltip "Already on {ownerParty}'s network". Without this gate, the cold-path CombinedRequestModal would resolve the PIN to `already-disclosed` and block submission — surfacing the impossible state upstream lets the user pick a different Claim before opening the modal. **EA+DA notification click handlers** (`v22-request`, `v22-request-ea-only`, `v22-ea-accepted`, `v22-ea-declined`) close the Directory layer if it's open before cold-path navigation — previously the Directory stayed open behind the response modal / over the parent canvas. **CombinedRequestModal RS scrollbox** data source extended to all published Requirements Sets (network-wide) so RFP pre-selection works for any actor's published RS; per-row rendering gains a globe icon (canonical GlobeIcon) + "Published by {owner party}" muted line below the name. Own (non-published) RSes appended below published rows with their pre-17.2.1.1 minimal treatment (no globe, no owner line). Dedupe by id (own wins so own-private RSes can never disappear). Cold-path entry (Claim PIN click on parent canvas) unaffected. Architecture spec §8.7 + §8.9.6 updated. Footer rolls forward to v0.17.2.1.1.

- **Phase 17.2.1 (prior phase):** **RFP Accept flow / Request Agreement.** Closes the placeholder-disabled "Request Agreement" button on owner-view SolicitationCards. The full RFP arc (post → discover → solicit → reject/accept → formalize EA+DA) is now demoable end-to-end. **Flow.** Bob views Alice's pending solicitation in the RfpDetailPanel → clicks "Request Agreement" → **new AssetPickerModal** opens (single-select scrollable list of Bob's owned Assets + context block: solicitor + RFP + Claim name) — Bob picks one of his own Assets to anchor the requested mapping per the architectural rule that disclosure + evaluation requests must originate from an Asset (so the parent canvas can lay out the request-node + edge). Continue closes the picker and **opens the existing CombinedRequestModal pre-filled** via the same mechanism the AI Shopper uses: `requesterAsset` = the picked Asset shape from `shared.assets`, `initialPin` = the solicitor's Claim PIN, `initialRequirementsSetIds` = the first RFP requirements-set id. **No CombinedRequestModal refactor needed** — the stop-and-surface gate from the brief was passed cleanly; existing pre-fill props handle the entire pre-population. **Submit-side effect** lives inside `handleV22RequestSubmit` conditional on a new `v22AcceptingSolicitation` state — after the existing provisional EA+DA creation, the branch calls `acceptSolicitation(solicitation, pair.evaluationAgreement.id)` (new pure transform in v2_2Data.js), writes the updated solicitation back into `v22Solicitations`, fires a new `v22-rfp-solicitation-accepted` notification on the solicitor's inbox, and clears the in-flight state. **Cold-path preserved bit-identically** — the conditional is a no-op when `v22AcceptingSolicitation` is null (every existing entry to CombinedRequestModal). **`makeRfpSolicitation` gains an `acceptedEaId` field** (default null) — set by `acceptSolicitation` to link the accepted solicitation back to its resulting EA artifact. **New `v22-rfp-solicitation-accepted` notification type** — green ACCEPTED badge, "{requester} accepted your solicitation — see your new EA on the parent canvas" copy, click routes to Directory → RFP marker → RfpDetailPanel where the solicitor-view accepted-state surfaces the EA pointer. Joins the standard `v22-request` notification on Alice's inbox — dual notification by design (one informational pointer to the RFP/solicitation context, one actionable pointer to the live EA+DA artifact). **SolicitationCard accepted-state treatment** (both viewer modes): green status badge, "Agreement requested on {date}" line, no action buttons. Owner sees "see the provisional EA on the parent canvas"; solicitor sees "Accepted — see your new Evaluation Agreement on the parent canvas". **RfpDetailPanel's `onRequestAgreement` prop** wired through from V2App to SolicitationCard. **Architecture spec gains §8.9.6** with the full Accept-flow chain + dual notification model + cold-path preservation rationale. **Build clean.** Data-layer probe confirms: `acceptSolicitation` produces correct accepted shape with eaId, source not mutated, throws on missing inputs, mergeSolicitations overlays the accepted solicitation onto shared, seeded Bob has 3 owned Assets to pick from, Sentinel-4 RFP has 2 requirementsSetIds. Footer rolls forward to v0.17.2.1.

- **Phase 17.2.0.4 (prior phase):** Hotfix — LOD threshold revert + zoom control z-index + AI Shopper modal lag. Three QA-driven fixes. **(1) LOD thresholds reverted** from 17.2.0.3's overly-aggressive 2.5–5.5 band to 3.4–5.0. At the 17.2.0.3 lower bound (250 %), mini-cards visibly overlapped in dense clusters (Pinnacle Systems et al.). Reverting `MID_LOD_THRESHOLD` to 3.4 restores screen-spacing > card-width at the threshold (close to the pre-17.2.0.3 density-invariant value of `MINI_CARD_W / DOT_GRID = 3.333`); `LOD_THRESHOLD` stays slightly wider at 5.0 (was originally 4.375); `MAX_ZOOM` stays at 6.5 from 17.2.0.3. `selectRfp`'s `Math.max(zoomRef.current, LOD_THRESHOLD)` automatically resolves to the new 5.0 — notification clicks still land at full-card LOD. **(2) Directory zoom controls raised** from `zIndex: 50` to `zIndex: 1700`. The mini-card and full-card overlays sit at z=1500 default / z=1600 selected and were covering the +/-/FIT buttons when cards landed in the upper-right region. The new value sits between the card layer and the header pillbox (z=2000) / tooltip cards (z=2500). Cluster labels stay at their existing z-tier per brief. **(3) AI Shopper modal lag fixed** — measured 81 ms click-to-render via puppeteer (versus the ~5 s reported pre-fix). Two surgical fixes: **(a)** module-level cache for `buildV22SharedArtifacts` in `v2_2Data.js` (320 ms cold → 0.001 ms cached). The function rebuilds a ~23k-Claim, ~92k-DA dataset on every call and has 20+ call sites in V2App; the cache returns the same object on subsequent calls. Safe because every existing caller treats the result as read-only and the function has no inputs — but if any future change mutates the returned data, that change MUST also invalidate the cache or shape itself as a new merge layer like `mergeProvisionals`. **(b)** O(n²) → O(n) rewrite of the AI Shopper IIFE's public-claims derivation in V2App: replaced `publicDas.map(d => shared.claims.find(c => c.id === d.subject.id))` (~1.6 s alone on the seeded data) with a precomputed `claimsById` Map for O(1) lookups. Footer rolls forward to v0.17.2.0.4.

- **Phase 17.2.0.3 (prior phase):** RFP description in solicitation modal + expanded mini-card zoom range. Two polish items batched before Phase 17.2.1's Accept-flow lift. **(1) RFP description block** added to `SolicitationCreateModal` between the modal subtitle and the Required Standards accordion. Matches `RfpDetailPanel`'s description treatment: prose body with `white-space: pre-wrap` for multiline preservation; "No description provided." muted-italic fallback when `rfp.description` is empty/null. Thin section divider below pairs visually with the existing divider between the accordion and the Claim picker. **(2) Mini-card zoom range widened** in `DirectoryLayer.jsx`. `MID_LOD_THRESHOLD` changed from `MINI_CARD_W / DOT_GRID` (= 3.333) to an explicit `2.5` (250 % zoom); `LOD_THRESHOLD` changed from `CARD_W / DOT_GRID` (= 4.375) to an explicit `5.5` (550 %). `MAX_ZOOM` bumped `5.0 → 6.5` so the new full-card threshold sits well below the cap with comparable headroom. Breaks the original "density invariant" (cards never overlap horizontally at the threshold) — at 2.5 zoom × 48 wu DOT_GRID = 120 px screen spacing vs 160 px mini-card width, mini-cards may overlap horizontally inside dense clusters at the low end of the band. Per Andrew, the readability win outweighs the mild collision risk. `selectRfp`'s `Math.max(zoomRef.current, LOD_THRESHOLD)` override (Phase 17.2.0.2) automatically picks up the new 5.5 → notification clicks still land at full-card LOD. Footer rolls forward to v0.17.2.0.3.

- **Phase 17.2.0.2 (prior phase):** Notification click completion + own-cluster label + RS accordion in solicitation modal. Three items, all polish on top of 17.2 / 17.2.0.1: **(1) Notification click completion.** Phase 17.2.0.1's `panToRfp` was pan-only at current zoom, so from the 15 % galactic-view default a notification click pannned negligibly and didn't select the marker on-canvas. Replaced with `selectRfp(rfp)` (imperative handle), backed by a new `focusRfpInternal(d, dotIndex, { zoom })` helper shared between `handleMouseUp`'s RFP branch, `onCardClick`'s RFP branch, AND the external imperative path. The helper does setPinned (drives on-canvas tooltip + select-state brightening via flushRfpColors) plus `animatedPanToWithZoom` with panel-offset compensation. Internal click paths keep current zoom (existing behaviour preserved); external `selectRfp` overrides to `Math.max(zoomRef.current, LOD_THRESHOLD)` so a notification click from the galactic view lands at full-card LOD. V2App's notification handler calls `selectRfp` (was `panToRfp`); the rAF retry loop guard from 17.2.0.1 is preserved verbatim. **(2) Active actor's own-cluster label amber styling on the Directory layer.** PillboxLabel accepts a new `isOwn` boolean prop; when true, renders with `background: var(--accent-amber)` + `color: var(--bg-deep)` for contrast. Cluster label render block computes `isOwn = !!layout.activeParty && cluster.ownerParty === layout.activeParty` per cluster. Bob → GovCo styled; Alice → MicroCo styled; Dave → ChipCo styled; Carol/AuditCo has no cluster on the Directory so the gating naturally degenerates. DOM-probe-verified: exactly 1 amber pill (`rgb(245, 158, 11)`) on Bob's view, 98 neutral. **(3) SolicitationCreateModal — Required Standards accordion** rendered above the Claim picker. New `requirementsSets` prop (V2App threads `publishedRequirementSets`, same source the RFP Detail Panel uses). New `RsAccordionEntry` sub-component renders each `rfp.requirementsSetIds` entry as a clickable header (name + version pill + chevron); expanded state renders the RS's requirements list with id (mono pill), label, description (secondary), and criterion (muted italic). Multi-expand via `useState(() => new Set())`. Missing-RS case → "(Standard not found)"; zero RS IDs → "No required standards specified." 240 px max-height scrollbox bounds the section growth; the existing 90 vh modal cap (ModalShared) handles small viewports. Footer rolls forward to v0.17.2.0.2.

- **Phase 17.2.0.1 (prior phase):** Hotfix — notification routing + Directory cursor/hover + V2Canvas2 setState fix. Three runtime-QA items from Phase 17.2 closed in one pass. **(1) Notification click routing** for `v22-rfp-solicitation-received` and `v22-rfp-solicitation-rejected` now fires the camera pan + zoom step — the 17.2 implementation registered three of four sub-steps (open Directory, clear Claim panel, set RFP panel) but omitted the pan to the marker. New `useImperativeHandle`-exposed `panToRfp(rfp)` method on DirectoryLayer; V2App's notification handler retries via rAF until the Directory's phase machine reaches `in` and the layout is built (capped at 60 frames / ~1s — Directory opening is ~600ms). DirectoryLayer wrapped in `forwardRef` for this. **(2) Cursor pointer on Directory hover** wired via imperative `canvasEl.style.cursor` update in `handleMouseMove` based on raycast hit (the canvas's inline `cursor: default` style masked any container-level change). AssetNode / AssetNodeMini wrappers already carry `cursor: pointer`, so mid/full-LOD card hover works automatically. Reset on `onMouseLeave`. **(3) RFP open-marker hover/select brightening** now visibly renders. The 17.2 path wrote `instanceColor = (1,1,1)` against an indigo material color → multiplier landed at identity → no visible delta. Fix: material color → white (so per-instance color carries the entire signal), base instance color = indigo, `flushRfpColors` lerps base 0.35 toward white on hover and 0.65 on select. Closed-RFP per-vertex colors get the same hover/select split (was: single 0.55 lerp for both). **(4) V2Canvas2 setState-in-render warning fixed**: root cause was the `setV22DirectoryOpen((open) => ...)` functional updater calling `canvasRef.current.clearHoverState()` as a side effect inside the reducer — React executes functional updaters during the next render's reducer phase, so `clearHoverState` triggered V2Canvas's setState while V2App was rendering. Surgical fix: lift the side effect out of the updater (read current `v22DirectoryOpen` from closure inside the onClick handler, run side effects, then call setState). No structural refactor; ~10 LOC. The stack trace pinpointed the offender via puppeteer + CDP `Runtime.consoleAPICalled` capture (V8 stack trace surfaced `clearHoverState @ V2Canvas.jsx:1757 ← <anon> @ V2App.jsx:3107`). Footer rolls forward to v0.17.2.0.1.

- **Phase 17.2 (prior phase):** RFP solicitation flow (submit + deliver + reject loop). Opens the seller→buyer engagement axis against the Phase 17 RFP arc. Sellers solicit one of their existing Claims against an open RFP via a new "Solicit with my Claim" footer button on the RfpDetailPanel; the RFP owner sees incoming solicitations in a new section above the panel footer and can reject them with an optional reply; the solicitor sees their own outgoing solicitation in the same panel, gets a notification on rejection, and revisits the panel to see the rejection state. **New factory** `makeRfpSolicitation` in `v2_2Data.js` with `{ id, rfpId, claimId, solicitor, recipient, message, status, createdDate, respondedDate, rejectionMessage }` shape and `'pending' | 'rejected' | 'accepted'` status taxonomy (accepted is reserved for Phase 17.2.1's Request Agreement flow). **New `mergeSolicitations(shared, solicitations)`** overlay (mirror of `mergeProvisionals` / `mergeClosedRfps` — Map-backed, returns identity when empty). **New V2App state** `v22Solicitations: Map<id, RfpSolicitation>` (Map so updates mutate in place via `setV22Solicitations(prev => new Map(prev).set(id, updated))`) + transient `v22SolicitOpenForRfp` and `v22SolicitationToReject` modal-open state. **Two handlers** (`handleCreateSolicitation`, `handleRejectSolicitation`) build artifacts + fire notifications via the existing `enqueueV22NotificationForRequester` pattern. **Two new notification types** `v22-rfp-solicitation-received` (to RFP owner on submit) and `v22-rfp-solicitation-rejected` (to solicitor on reject); both route on click to Directory → RFP marker → RfpDetailPanel (auto-dismiss since the loop terminates after rejection). **Three new components**: `SolicitationCreateModal` (Claim picker + 500-char optional message field, picker defaults to zero selected per CLAUDE.md convention), `SolicitationRejectModal` (300-char optional reply, red destructive submit), `SolicitationCard` (renders a single solicitation with status badge, message blocks, action bar — Request Agreement disabled with "Coming in Phase 17.2.1" tooltip; Reject enabled on pending). **RfpDetailPanel three-way visibility branch** above the footer: owner sees "Incoming Solicitations (N)" + cards (empty case shows muted "No solicitations yet."); solicitor sees "Your Solicitation" + their single card (REJECTED state surfaces buyer's reply in a separate block); other non-owner sees no section. **Footer extensions**: non-owner + status open + no existing → "Solicit with my Claim" affirm button; non-owner + existing → muted "Already solicited — see above." line. **DirectoryLayer RFP marker hover + select brightening**: per-instance `instanceColor` attribute on `rfpMesh` + `rfpFillMesh` (brightens to white on hover/select index); per-vertex `color` attribute on the closed-RFP `LineDashedMaterial` (`vertexColors: true`; lerp toward white on the hovered/selected closed RFP's 8-vertex range). New `flushRfpColors` flush callback parallel to `flushDotColors`; integration via `rfpDirtyRef` flag flushed once per animation-loop tick. **Detail Panel 1px gap fix** (Item 21): both Directory-layer Detail Panel mount blocks (Claim + RFP) bumped `bottom: 28` → `bottom: 27` to close a 1px hairline gap above the app footer. **Architecture spec §8.9 + Changelog bullet** capture the artifact + visibility model. Out of scope: Accept flow / Request Agreement (placeholder disabled button in 17.2; full implementation in Phase 17.2.1), withdraw / amend / multi-Claim solicitations, seeded solicitations (demo participants create them in real time), parent-canvas RFP rendering. Footer rolls forward to v0.17.2.

- **Phase 17.1 (prior phase):** RFP close/reopen lifecycle (owner-side). First lifecycle phase against the RFP arc. The RFP owner (e.g. Bob for the seeded Sentinel-4 RFP) can close their RFP from the Detail Panel footer; closed RFPs render with a dashed-outline visual treatment at every Directory LOD on the owner's view and are filtered out of every non-owner's Directory. Reopen restores the open state. **`makeRfp` factory** validates `status` against `'open' | 'closed'` (throws on unknown) and accepts `closedDate` (ISO string, null when open). **New exported helpers** in `v2_2Data.js`: `closeRfp(rfp, closedDate?)`, `reopenRfp(rfp)`, and `mergeClosedRfps(shared, closedRfpIds)` — pure transforms layered on the shared artifact set via the existing merge pattern. Session state in V2App: `v22ClosedRfpIds: Map<rfpId, ISO closedDate>` (Map so re-renders see stable timestamps). **`buildV22DirectoryDataForRole(roleId, provisionals, closedRfpIds)`** chains the new merge internally and applies asymmetric filtering: `ownRfps` includes all of the owner's RFPs regardless of status; `otherRfps` + per-cluster `cluster.rfps` filter on `status === 'open'`. **RfpDetailPanel** gains a `CLOSED` status badge (muted grey/dim treatment), a "Closed YYYY-MM-DD · HH:MM UTC" row when closed, and an owner-only footer with a single direct-action button — "Close this RFP" (neutral grey) or "Reopen this RFP" (indigo affirming). The selected RFP is re-resolved from merged shared artifacts each render so the panel reflects the latest status after a close/reopen click. **DirectoryLayer**: new `closedRfpMesh` (single `THREE.LineSegments` + `LineDashedMaterial`) renders dashed outlines for closed-and-owned RFPs; `BufferGeometry` rebuilt on every layout / zoom change (4 line segments per closed-owned, `computeLineDistances()` required after each rebuild for the dashing to render). Existing `rfpMesh` + `rfpFillMesh` partition closed-owned instances to `hidden` matrices; `rfpHitMesh` keeps all instances so closed-owned remains clickable for reopen. LOD-visibility effect extended to hide the dashed mesh alongside the other RFP meshes at `zoom ≥ MID_LOD_THRESHOLD`. **AssetNode + AssetNodeMini** RFP early-return branches read `node.isClosed` (plumbed from the synthetic node) and switch `border-style: solid → dashed` on both the inner card border and the outer selection ring; hover-preview portal inherits the dashed treatment via the same synthetic node. **Architectural rationale**: closed-RFP visibility is intentionally asymmetric — the owner sees their own closed RFPs (so existing solicitation notifications remain actionable) while non-owners see them filtered out (so new solicitations can't be filed against a closed RFP — the seller-side entry point disappears). Footer rolls forward to v0.17.1.

- **Phase 17.0.2 (prior phase):** Hotfix — TDZ crash in AssetNode RFP branch (hoisted `hoverSelectColor` above the RFP early-return). Phase 17.0.1's RFP early-return in `AssetNode` (full) referenced `hoverSelectColor` before its `const` declaration — the Phase 16.2.11 declaration site sat further down in the function body, after the new early-return. Every RFP card render at mid/full LOD and every RFP card click triggered a TDZ `ReferenceError: Cannot access 'hoverSelectColor' before initialization`. Fast zoom-to-full-LOD on Bob's view crashed the app to a black screen. **Fix**: hoist the `hoverSelectColor` declaration above the RFP early-return (3-line move; no semantic change for non-RFP renders since the value depends only on `node.category`). The original declaration site is removed to prevent a duplicate `const`. `AssetNodeMini`'s RFP branch already had its `hoverSelectColor` declaration above it — no change needed there. Swept both components for parallel TDZ-trapped variables; none surfaced. **Process note**: the 17.0.1 → 17.0.2 incident is the canonical example for the "render-path branches with state-dependent code paths require runtime LOD-swap + hover + selection + click exercises at every affected LOD" rule. 17.0.1 marked items as "Code-verified" that bypassed the React render path entirely (the fiber-state-mutation probe never actually triggered AssetNode's RFP early-return through normal click → re-render flow). Footer rolls forward to v0.17.0.2.

- **Phase 17.0.1 (prior phase):** RFP card LOD swap + hover-preview pinning. RFP markers reach LOD parity with Claims: hollow square at dot-LOD, AssetNodeMini at mid-LOD, AssetNode (full) at full-LOD — same zoom thresholds (MID_LOD_THRESHOLD ≈ 3.33×, LOD_THRESHOLD ≈ 4.38×) as the Phase 16.2.7 Claim pipeline. The stale-pinned-tooltip bug from 17.0 QA is structurally fixed via a discriminated-union `pinned` state. **AssetNode + AssetNodeMini extended with a 5th schema** (`node.category === 'rfp'`) — early-return branches render the minimal RFP layout (full: `RFP` pill + name + "Posted by {owner}"; mini: `RFP` pill + name only). No badges, no minibars, no action bar, no dive-hint, no edge-endpoint indicator. RFPs join the amber-on-hover/select branch in `hoverSelectColor` (Phase 16.2.11 was claim-only). Default border falls through cleanly to WARM_BORDER (no disclosure type). `CATEGORY_CONFIG.rfp` added for completeness. **DirectoryLayer card-overlay render block** iterates both Claim AND RFP entries from `allDots`; each RFP builds an inline synthetic node `{ id, category: 'rfp', rfp, name, ownerParty, owner }` and routes to the same Card component (only inner schema differs). Hollow-square outline + tinted-fill + hit-test meshes all hide at `zoom ≥ MID_LOD_THRESHOLD` (extending the dot-mesh hide path) so cards don't render behind squares and the hit-test mesh doesn't intercept clicks meant for cards above. **`pinned` extended to discriminated union** — exactly one of `{ claim, ... }` or `{ rfp, ... }` at any time. All RFP click paths (hollow-square at low zoom + onCardClick at mid/full LOD) call `setPinned` with the RFP shape. Atomic overwrite resolves the 17.0 stale-tooltip bug structurally — clicking from Claim to RFP or vice versa swaps the pinned tooltip cleanly. **New `RfpTooltipCard`** mirrors `ClaimTooltipCard` for the dot-LOD pinned-tooltip path; pinned-tooltip render block branches on `pinned.claim` vs `pinned.rfp`. **Empty-canvas click** clears both discriminators (`setPinned(null)` + `onClaimDotClick(null)` + `onRfpClick(null)`). **V2App.jsx**: footer v0.17.0 → v0.17.0.1; Changelog modal entry prepended.

- **Phase 17.0 (prior phase):** Clickable RFP markers + read-only RFP Detail Panel (Directory only). Opens the Phase 17 RFP arc against polish-backlog #192 (Item J: RFP / Public Directory request flow with self-evaluation). 118 seeded RFPs (Bob's Sentinel-4 + 20 RFP-only buyer mocks + 4 mixed actors) are now clickable. **Hit-test mesh**: new invisible solid-square `THREE.InstancedMesh` in `DirectoryLayer.jsx` (`rfpHitMeshRef`) parallels the visible hollow-square outline; matrices in lockstep via the existing populate + rescale-on-zoom paths. Resolves raycasts on the marker's interior (the outline ring's hole leaves a dead zone otherwise). Defensive InstancedMesh settings preserved (`frustumCulled = false`, unbounded `boundingSphere`). **Raycast** extended to consult both dot mesh + RFP hit-test mesh; closer hit wins (defensive — RFPs placed outside cluster dots). RFP hit fires `onRfpClick(rfp)` + the standard `animatedPanToWithZoom` pan-to-center mirror of Phase 16.1.3 Item 6 / 16.2.9 Item 1. **New RfpDetailPanel** (`src/components/DetailPanel/RfpDetailPanel.jsx`) — read-only: header pillbox `RFP` + status badge + close + name; "Posted by" + owner ACTOR pillbox + optional YOU badge; description (or muted "No description provided"); "Required Standards" chip list (each rsId → `{name} v{version}` resolved against `publishedRequirementSets`, muted raw-id fallback otherwise); posted date `YYYY-MM-DD · HH:MM UTC`. No footer / action buttons. **V2App.jsx wiring**: new `v22DirectorySelectedRfp` state next to `v22DirectorySelectedClaim`; mutual exclusion at the click-handler level (each setter clears the other); both states cleared on Directory close + globe-button toggle. Panel mount shell mirrors the Claim mount (position:fixed top:61 right:0 bottom:28 width:480 zIndex:200). Changelog modal entry prepended for v0.17.0. **Out of scope**: RFP card LOD swap (17.0.1), RFP hover-preview (17.0.1), RS chip clicks (17.0.1), `makeRfp` lifecycle metadata (17.1), supplier response (17.2), buyer review + initiate formal EA (17.2.1), parent-canvas RFP rendering (later), Claim Detail Panel polish (17.3), umbrella DA edges (17.4+), create-RFP entry point (17.5+).

- **Phase 16.2.11 (prior phase):** Phase 16 wrap-up: Alice grouping + footer version + parent-layer visual calibration. Three loose ends closed before Phase 17 opens. **Item 1 — Alice per-Claim y-band grouping fix**: `buildV22Canvas` chain anchor reads the evaluated Claim's y first (was `EA.granteeAssetId` Asset y per Phase 16.2.2). The granteeAssetId Asset (Bob's `bAvionics`, Carol's `cAuditWorkspace`) isn't on grantor-direction views like Alice's, so chains were scattering via the symmetric-distribution pass-2 fallback. New policy: `anchorIdForOrigin` returns Claim id (was granteeAssetId); `anchorYForOrigin` returns Claim y (was Asset y), falls back to granteeAssetId.y, then Number.POSITIVE_INFINITY. Bob's view unaffected because pulled Claims at `symmetricRowY(i)` coincide with owned Assets at `symmetricRowY(i)`. Alice's view now exhibits the per-Claim y-band rule: each Claim's chain within 0-2 rows. Asset-clustering-near-Claim (separate, more substantial concern) deferred per brief's escape clause. **Item 2 — Footer version**: bumped `v0.16.2.0` → `v0.16.2.11`. Backtrack-hotfix freeze formally ends. **Item 3a — Parent Claim disclosure-type plumb**: `buildV22Canvas` computes `_disclosureType` per Claim node (own = 'full', pulled = type of the DA where `grantee.party === actor.party && subject.kind === 'claim' && subject.id === claim.id`). V2Canvas passes `disclosureType={node.category === 'claim' ? node._disclosureType : undefined}` to both AssetNode and AssetNodeMini; the Phase 16.2.10 disclosure-type branches now fire on parent. Bad-health overrides disclosure-type per the priority chain (Bob's PRM = selective → red border for bad health + amber-tinted background). **Item 3b — Non-Claim hover/select indigo**: new `hoverSelectColor` derived from `node.category` in both AssetNode and AssetNodeMini — Claims use amber, non-Claims brighten to `var(--accent-indigo)`. Three sites updated per component (inner border, outer selection ring, inner-div hover background). AssetNodeDot intentionally untouched. Net cross-layer rule: Claims (either layer) carry disclosure-color defaults that yield to amber on hover/select; non-Claim node types (parent only) are always indigo-themed (40% WARM_BORDER → 100% indigo on hover/select). **11 missed Changelog entries (16.2.6 → 16.2.10) already prepended in Phase 16.2.10**; new 16.2.11 entry prepended in this phase. **Phase 16 (Directory Layer + parent-canvas visual calibration) is now CLOSED.** Footer rolls forward to v0.16.2.11.

- **Phase 16.2.10 (prior phase):** Disclosure-type colored borders + tinted backgrounds on Directory Claim cards. Adds the disclosure-type color signal (already present on dots since Phase 16.1.3 Item 8) to AssetNodeMini (mid-LOD) and AssetNode (full-LOD) cards on the Directory layer. **Item 1**: `DirectoryLayer.jsx` plumbs `disclosureType` from cluster spec items → `placed` dot construction → `allDots.push` → the card overlay render block; the JSX passes `disclosureType={d.disclosureType}` to the active `Card` component. RFP `allDots` entries carry `disclosureType: 'full'` for shape consistency (RFPs don't render as cards). **Item 2**: `AssetNodeMini` accepts the new `disclosureType` prop; its `borderColor` chain gains three new branches inserted between `hasBadHealth` and the `WARM_BORDER` fallback (`disclosureType === 'full' ? var(--accent-indigo) : 'selective' ? var(--accent-amber) : 'proofonly' ? var(--accent-green) : WARM_BORDER`). The inner-div `background` chain gains the same three branches as the new default, replacing the plain `var(--bg-card)` when `disclosureType` is set — 12% color-mix tint (`color-mix(in srgb, var(--bg-card) 88%, var(--accent-*))`). **Item 3**: `AssetNode` (full) gets the same prop + same border chain insertion; for the background chain, the disclosure-type branches sit ABOVE `isCounterpartyNode` — rationale: disclosure type is the more-specific signal (which-data-is-visible); counterparty is the less-specific signal (whose-data-is-it). Directory cards always carry `disclosureType` so they always land in the new branch; parent canvas Claims (which don't pass the prop) fall through to the existing chain unchanged. **Hover/select precedence preserved**: hovering switches the border to amber (existing behavior); selection adds the outer thick amber ring on top; disclosure-type color disappears under those states. **Item 4 — Changelog catchup**: 11 missed entries (16.2.6 through 16.2.10) prepended to the in-app Changelog modal in `V2App.jsx`, matching the existing entry format. **Priority #1 (Directory appearance and feel) is now complete.** Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.9 (prior phase):** Card-click pan + hover-preview offset fix. Two surgical QA-driven fixes following Phase 16.2.8. **Item 1**: `onCardClick` in `DirectoryLayer.jsx` now appends the same `animatedPanToWithZoom(d.x + panelOffsetWorld, d.y, targetZoom, 500)` block the dot-click handler runs (Phase 16.2.7 omitted this on the wrong rationale that the user is "already viewing the card"). Same 500 ms ease, `targetZoom = zoomRef.current` (no zoom change). `animatedPanToWithZoom` added to the `useCallback` deps. **Item 2**: `AssetNodeMini`'s portal-tooltip transform in `AssetNode.jsx` (line ~1534) changed from `translate(-50%, -50%) scale(0.85)` to `translate(calc(-50% + ${(ACTION_BAR_W * 0.85) / 2}px), -50%) scale(0.85)`. The wrapper is `CARD_W + ACTION_BAR_W = 244 px`; without the shift, the visible card body's centre lands 14.45 px LEFT of `tooltipPos.x` (mini-card's amber hover border peeks out on the right of the preview — obvious on Directory's unscaled 160 px mini-cards, subtly off on parent canvas too). Subtle parent-canvas alignment improvement comes free. No changes to AssetNodeDot's tooltip, RFP rendering, cluster labels, mock data, or other Directory state. Build clean; no console errors. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.8 (prior phase):** Directory Claim card population (CLAIM/minibar/badges). Phase 16.2.7 shipped the LOD swap by passing raw `claim` (output of `makeClaim`) as the `node` prop — cards rendered title + owner only, no CLAIM type label, no minibar, no badges. Root cause: `AssetNode` / `AssetNodeMini` read `node.category`, `node.displayHealth`, `node._activeBadges` — none on raw `makeClaim` output. Fix routes Directory Claims through the existing `claimToNode(claim, rollup, x, y)` helper in `v2_2Data.js` + new mock distribution helpers. (1) **New `mockClaimHealth(claimId)`** (`v2_2Data.js`, near `claimToNode`): returns `{ ok, warn, bad }` rollup keyed by claim.id via `seededRandom(hashString(claimId + ':health'))`. Distribution: 50% no minibar, 5% all-green ≥ 2 badges, 5% all-green 1 badge, 10% all-green no badges, 15% has-bad (red border), 15% mixed. (2) **New `mockClaimBadges(claimId, health)`**: returns `[{ id, badgeName, badgeVersion, issuerParty }]` only when `health.bad === 0 && health.warn === 0 && health.ok > 0`. Inline `MOCK_BADGE_NAMES` (12 standards) + `MOCK_BADGE_ISSUERS` (5 issuers). Badge shape matches `BadgeChipContainer.jsx`'s consumer contract verbatim. (3) **View-builder plumb**: `buildV22DirectoryDataForRole` builds `cluster.nodesByClaimId` Maps per cluster + a top-level `ownNodesByClaimId` Map; each value is `claimToNode(...)` with `_activeBadges` attached. (4) **DirectoryLayer plumb**: `buildItems` reads from these Maps and threads `node` through `placed` dots → `allDots` → the JSX card overlay block, which now passes `node={d.node || d.claim}` (raw fallback). **Click flow preserved**: `onCardClick` and dot-click both continue passing raw `d.claim` to `onClaimDotClick` (the Detail Panel still receives the shape it expects). **Probe verified** across 22,994 enriched nodes on Bob's view: 49.9%/20.0%/15.2%/15.0% distribution; 10.0% with ≥ 1 badge; 4.9% with ≥ 2 badges. Determinism passes. **Dev server verified** at zoom 5.0: cards show CLAIM teal label, owner, minibar (~half), red borders on has-bad cards, badge chips on a subset. No console errors. No changes to AssetNode.jsx, BadgeChipContainer.jsx, RFP rendering, or `makeClaim` output. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.7 (prior phase):** Mini-card + full-card LOD on Directory + size tweak. Closes polish-backlog #200. Three coordinated changes in `DirectoryLayer.jsx`: (1) `BASE_DOT_FACTOR` 0.85 → 0.70 + `BASE_RFP_FACTOR` 0.95 → 0.80 — marker sizes in the linear regime shrink ~16-18%, cap-engaged appearance unchanged. (2) `MAX_ZOOM` 1.5 → 5.0 — makes room for two LOD swap thresholds. (3) **LOD swap for Claim dots only** — at zoom ≥ `MID_LOD_THRESHOLD = 160/48 = 3.333` dots are replaced by `AssetNodeMini` HTML overlays; at zoom ≥ `LOD_THRESHOLD = 210/48 = 4.375` they swap to full `AssetNode`. Both card components reused verbatim from `AssetNode.jsx` (brief's hard rule: NO modifications). **Density invariant**: cards render at FIXED NATURAL PIXEL SIZE (no `transform: scale()`) — at the threshold, screen-space inter-dot spacing exactly equals card width, so cards never overlap. Viewport-cull skips cards outside `viewport + cardW/cardH` buffer. New `dotsMesh.visible` `useEffect`, hover-clear `useEffect`, gated `ClaimTooltipCard` render, new `onCardClick` `useCallback` (mirrors dot click flow but skips animated pan/zoom). RFPs stay as hollow squares at all LODs — RFP card swap deferred. Cluster pillbox labels stay as `PillboxLabel`. Card dimension constants mirrored in `DirectoryLayer.jsx` (sync'd duplicate; `AssetNode.jsx` doesn't export them). **Verified** on dev server (1400×900, Bob's view): at zoom 366% (mid-LOD), 23 AssetNodeMini cards rendered with non-overlapping spacing; at zoom 500% (full-LOD), AssetNode full cards visible; dot mesh hidden in both regimes. Phase 16.1.4 / 16.1.5 / 16.2.3 / 16.2.6.1 / 16.2.6.4 / 16.2.6.5 / 16.2.6.6 defensive settings preserved. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.6.6 (prior phase):** RFP marker scaling + tinted fill. Resolves four 16.2.6.5 QA issues — RFP squares oversized at high zoom (no cap), borders proportionally tiny, adjacent grid-cell squares overlapping, and markers reading as empty rectangles without fill. Six items in `DirectoryLayer.jsx`: (1) new constants `BASE_RFP_FACTOR = 0.95`, `MAX_SCREEN_RFP_PX = 22`, `RFP_FILL_ALPHA = 0.15`, `RFP_BASE_OUTER = DOT_GRID × 0.95 = 45.6 wu` (was 1.2). `RFP_OUTER_SIZE` kept as backwards-compat alias. New `computeRfpWorldSize(zoom)` parallels `computeDotWorldSize`. (2) Outline geometry built at `RFP_BASE_OUTER`. (3) New `rfpFillMeshRef` + `PlaneGeometry(RFP_BASE_OUTER²)` + indigo `MeshBasicMaterial({ transparent, opacity: 0.15 })` companion InstancedMesh added BEFORE the outline mesh so the outline draws on top of the fill. Disposal added to cleanup. (4) Populate loop bakes per-instance scale via `m.makeScale + setPosition` and writes to both meshes. (5) New `rfpScaleRef` + rescale-on-zoom `useEffect` updates both meshes' matrices on scale change ≥ 1e-4. Effect declared BEFORE the border-rebuild effect so `rfpScaleRef` is current when read. (6) Border-rebuild formula: `RFP_BORDER_SCREEN_PX / (scale × zoom)` — below the cap reduces to the pre-16.2.6.6 formula; above the cap settles to a single geometry. **Verified**: 99 PillboxLabel + 118 RFP markers render on Bob's view at zoom 0.15. Dot rendering unchanged; mixed clusters preserve dots-inner-squares-outer pattern. No NaN warnings. Phase 16.1.4 / 16.1.5 / 16.2.3 / 16.2.6.1 / 16.2.6.4 / 16.2.6.5 defensive settings preserved. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.6.5 (prior phase):** RFP cluster expansion (20 RFP-only + 4 mixed actors). Populates the Directory's previously-empty bottom third with buyer-side signal: 20 new RFP-only mock actors (98 total RFPs across NavalSys Authority through Vesper Defense) + 4 mixed actors with both Claims and RFPs (Lighthouse Programs / Marigold Systems / Quarry Industries / Auger Defense — 170 Claims + 19 RFPs). New exported `generateRfpsForActor(actor, count)` helper uses existing `hashString` + `seededRandom` (mulberry32) for deterministic procedural RFP names. View builder extended with `PRIMARY_PARTIES_FOR_ORPHAN_RFP` exception set (GovCo/MicroCo/AuditCo/ChipCo) — every cluster carries `cluster.rfps`; RFP-only owners upsert as new clusters except primary parties (preserves 16.2.6.3 GovCo orphan-label path on Alice/Carol/Dave views). `computeLayout` gains an `ACTOR_KIND` taxonomy + branched initial seed positions (RFP-only bottom-bias 0.70-0.94 × usable height; mixed cross-zone band 0.60-0.70; Claims upper 0.10-0.65); target-area formula extended with `RFP_AREA_FACTOR = 1.44`; hard y-clamp inside Lloyd's loop pins RFP-only seeds. Per-RFP-marker label render filters on `clusterIdx === -1` (orphan-only). Probe-verified totals: actors 77→101, claims 22,824→22,994, assets 22,821→22,991, DAs 91,321→92,001, rfps 1→118. Cluster overflow grew 18 → 20 (+2, within brief's "expected 4-8" tolerance — Bowsprit Defense + Andromeda Defense newly overflow at the denser seed). **Polish-backlog #201 closed** by this phase. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.6.4 (prior phase):** Label z-order fix (smaller clusters on top). Single-item phase resolving label-overlap issues surfaced after 16.2.6.3's 25-actor expansion: ChipCo buried behind Prism Aerospace, MicroCo behind Precision Components, Photonix truncated behind Substrate Dynamics. Two new module-level constants in `DirectoryLayer.jsx`: `Z_BASE_CLUSTER_LABEL = 100` + `Z_RFP_LABEL = 1100`. `PillboxLabel` accepts a new `zIndex` prop (default 4) applied to the absolutely-positioned outer container. Cluster-label render block sorts `layout.allClusters` by `dots.length` descending, builds a party→rank Map, threads `zIndex={Z_BASE_CLUSTER_LABEL + rank}` per label — largest cluster gets lowest z, smallest gets highest. RFP owner-actor labels (16.2.6.3 Item 4) get `zIndex={Z_RFP_LABEL}` so they always render above every cluster label. Trade-off per brief: large clusters' pills may have corners clipped behind small-cluster pills (acceptable — large cluster identity is unmistakable from its footprint). DOM probe verified on Bob's view: Helios (3,250 dots) at z=101 → Photonix at z=172, MicroCo at z=173, GovCo at z=174 — inverse correlation with dotCount. On Alice's view the GovCo RFP owner-label renders at z=1100, above all cluster labels. No regression. Phase 16.1.4 / 16.1.5 / 16.2.3 / 16.2.6.1 defensive settings preserved. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.6.3 (prior phase):** More mock actors + label scaling + RFP hollow square + top inset trim. Six surgical polish items addressing post-16.2.6.2 QA. (1) **25 more mock supplier Actors** (5 medium + 20 small) added to a new `PHASE_16_2_6_3_NEW_MOCK_ACTORS` constant in `v2_2Data.js`, concatenated with `PHASE_16_2_6_NEW_MOCK_ACTORS` at the same expansion loop. All reuse 16.2.6 verticals + procedural Claim spec pipeline. Directory now ~22,824 dots across 77 Actors (was 52). Cluster boundaries grow ~48% — more "butting" between adjacent clusters. Delta: +1,215 Claims / Assets and +4,860 DAs vs 16.2.6.2. (2) **Zoom-aware cluster label scaling**: new `BASE/MIN/MAX_LABEL_FONT_PX` + `computeLabelFontSize(zoom) = 14 × sqrt(zoom)` clamped to [11, 18]. `PillboxLabel` accepts `fontPx` prop; padding em-based (`0.3em 0.7em`) + `lineHeight: 1` + flex centering so pill scales proportionally and text stays exact-centered at any zoom. (3) **Wave reveal speed 3000 → 4500 wu/sec** in `directoryLoadAnimation.js`. Duration drops ~3s → ~2s. (4) **RFP marker owner-actor label**: PillboxLabel below the marker for every RFP whose owning actor ISN'T the active actor. Fixes orphaned-RFP-marker on Alice / Carol / Dave views (GovCo now gets a label adjacent to its bottom-anchor RFP). (5) **RFP marker → hollow square primitive** (indigo) using existing `makeHollowSquareGeometry`. Outer `DOT_GRID × 1.2`, border thickness rebuilds on zoom change via new `rfpBorderRef` + zoom `useEffect` so on-screen border stays ~2px. Single ShapeGeometry rebuild per zoom transition. No RFP click handler exists pre-16.2.6.3 — the brief's "Detail Panel on RFP click" verification is a pre-existing gap deferred to RFP MVP (17.0–17.2). (6) **`DOMAIN_INSET_TOP` 500 → 475** (= `64 css px / 0.15 + DOT_GRID`). Static value over dynamic measurement so layout pipeline doesn't couple to parent DOM mount order. Visible top buffer below header now matches L/R 1-cell edge buffer. (7) **polish-backlog #201** added: Directory RFP cluster expansion at canvas bottom third. **Known regressions surfaced + accepted per brief's pinned conventions**: cluster overflow now affects 18 clusters (was 10 in 16.2.6.2) — more seeds competing for the same usable area; per brief's "Lloyd's at the current scale is already non-converging... Accept" rule. NaN-CSS-left React warning gone (covered by 16.2.6.1's umbrella SVG guard + 16.2.6.3's PillboxLabel flex centering). Phase 16.1.4 / 16.1.5 / 16.2.3 defensive settings preserved. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.6.2 (prior phase):** Edge buffers + zoom range + dot scaling + wipe origin. Four surgical polish items addressing post-16.2.6.1 QA. (1) **Voronoi-domain insets** in `DirectoryLayer.jsx`: four new constants (`DOMAIN_INSET_LEFT/RIGHT = DOT_GRID`, `DOMAIN_INSET_TOP = 500`, `DOMAIN_INSET_BOTTOM = 250` world units) shrink the tessellation rectangle inward — 1-cell edge buffer on left/right, world-space equivalent of the app header (~61 css px) + footer (~32 css px) at MIN_ZOOM = 0.15 on top/bottom. Both `delaunay.voronoi(...)` call sites pass the inset bounds. Lloyd's target-sum compared against `usableArea` instead of full canvas (new ratio ~0.78). (2) **`MAX_ZOOM` 4.0 → 1.5** — paired with new dot-size cap; beyond 150% no useful detail until Directory mini-card swap (deferred, filed as polish-backlog #200). (3) **Non-linear dot scaling**: new `BASE_DOT_FACTOR = 0.85` (was 0.95) + `MAX_SCREEN_DOT_PX = 22` + `computeDotWorldSize(zoom)` helper. Cap threshold ≈ 0.54 zoom. Implementation: per-instance matrix bakes scale via `m.makeScale(s, s, 1); m.setPosition(d.x, -d.y, 0)` (NOT mesh-level scale, which would also move positions). Zoom-change `useEffect` rewrites all instance matrices when scale changes; `dotScaleRef` short-circuits no-op rewrites. `DOT_RADIUS` adjusted to `DOT_GRID × BASE_DOT_FACTOR / 2` (= 20.4). (4) **Wipe origin** default moved from bottom-left (`'0% 100%'`) to bottom-center (`'50% 100%'`) — visually says "the network expands from your active-actor anchor." Pre-pinned `wipeOrigin` (Phase 11.8 #44 node-double-click) still wins; only the chrome-globe-click default path changes. (5) **Polish-backlog #200** added: Directory mini-card swap at higher zoom. **Known regressions surfaced + accepted per brief's pinned conventions** (16.2.6.x candidates): Lloyd's residual now 174.6 wu (vs 161 wu in 16.2.6.1) due to 7% smaller usable area; cluster overflow now affects 10 clusters (Helios 503/3250 down to Apex 201/325). Both per brief's "surface + accept" rule. NaN-CSS-left React warning from 16.2.6.1 not observed (covered by 16.2.6.1's umbrella SVG guard). Phase 16.1.4 lifecycle + Phase 16.1.5 InstancedMesh defensive settings + Phase 16.2.3 camera-init NaN guard preserved. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.6.1 (prior phase):** Dense organic cluster packing + ~22k dot expansion. Three coordinated changes resolve the trio of QA issues from Phase 16.2.6 (intra-cluster spiral gaps, half-empty canvas, super-jumbo overflow) via a single algorithmic shift: **dense Voronoi-clipped grid fill** replaces Vogel sunflower in `DirectoryLayer.jsx`. (1) New `packClusterDense` — each cluster's Voronoi polygon shrunk inward by 1 DOT_GRID, every grid cell inside enumerated (label hole excluded), sorted by distance from centre, first N filled. New helper `shrinkConvexPolygon` (per-vertex bisector miter, orientation-detected); existing `pointInPolygon` reused. Removed `vogelPosition` / `GOLDEN_ANGLE` / `SUNFLOWER_SCALE` / `SUNFLOWER_MAX_ITER_PER_DOT` / `INTER_CLUSTER_BUFFER` (now intrinsic to the shrink) / `isInLabelHole` (inlined). (2) **Lloyd's target area** swapped from proportional `canvasArea × (dots/total)` to physically-grounded `(dots × DOT_GRID² + LABEL_HOLE_AREA) × 1.20`. Target-sum-vs-canvas-area surface added; current ratio 0.73 (within brief's 70–80% target). (3) **35 mock supplier dot counts expanded ~6.5×** in `v2_2Data.js`'s `PHASE_16_2_6_NEW_MOCK_ACTORS` (Helios 500 → 3250, etc., down to Cordite Labs 5 → 35). Same 35 actors, same verticals, same procedural pipeline. New totals: Directory now ~21,609 dots (21,435 new mock + 157 existing + 17 switchable). Delta: +18,147 Claims, +18,147 Assets, +72,588 DAs vs 16.2.6 baseline. (4) `DOT_RADIUS = DOT_GRID × 0.475` (was 0.425) — diameter `0.95 × DOT_GRID` so adjacent dots butt with ~5% gap. (5) `MAX_DOTS` 10000 → 25000 for InstancedMesh capacity. **Defensive fixes**: shrink-collapse fallback (ChipCo's tiny cell reverts to unshrunken on `console.warn`); finite-vertex filter on umbrella SVG path render (drops paths with NaN screenPoints from the brief mount-race). **Known regressions surfaced per brief pinned conventions, filed as 16.2.6.2 candidates**: (a) Lloyd's residual still 161 wu at 20-iter cap (≈ 3.4 × DOT_GRID) — brief forbids unilaterally raising the cap; (b) cluster overflow on 9 clusters at new counts (Helios 789/3250, Atlas 468/2950, Polaris 300/2600, Vortex 854/2300, Vega 946/1200, Aurora 620/800, Beacon 520/900, Solstice 268/700, Apex 202/325); (c) ChipCo's tiny cell collapses on shrink. Visual result remains the brief's intent: top 75% of canvas densely populated, organic cluster shapes (Voronoi-polygon-edged), label holes preserved, inter-cluster buffer clearly visible, super-jumbos visibly massive. Phase 16.1.4 lifecycle + Phase 16.1.5 InstancedMesh defensive settings + Phase 16.2.3 camera-init NaN guard preserved. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.6 (prior phase):** Directory seed expansion to ~3,288 dots (procedural lexicons). 35 new mock supplier Actors added to the Directory beneath Bob's Sentinel-4 program; total now 52 (4 switchable primary + 12 existing mock + 35 new mock + Radiant Network). Heavy-tailed by design — 4 super-jumbo clusters (Helios 500, Atlas 450, Polaris 400, Vortex 350) carry ~50% of new dots. **Procedural Claim names**: new `VERTICAL_LEXICONS` constant in `v2_2Data.js` (21 verticals, hand-defined) + new exported helper `generateClaimSpecsForVertical(actorParty, vertical, count)` produces deterministic `{family} {prefix}-{number} {docType}` names via `hashString(actorParty) → mulberry32(seed)` PRNG so the same `(actor, vertical, count)` tuple always yields identical specs across module reloads. Per-actor disclosure-type mix `(i × 7 + seed) % 100` produces deterministic 60/25/15 indigo/amber/green spread per cluster (Helios verified: 300/125/75 of 500). **Counts**: +3,288 Claims, +3,288 Assets, +13,152 DAs (3 internal own/ref + 1 public per Claim); 4 invariants verified via data-layer probe (per-actor claim, asset, DA counts; zero umbrella DAs from any new actor; every new public DA has grantee=Radiant Network + subject.kind=claim). Existing 12-actor + 4-primary seeds frozen (NovaFab anchor claim "Rad-Hard 0.18µm CMOS Logic Array RHF-820 Datasheet" spot-checked unchanged). **Lloyd's iteration cap** bumped 10 → 20 in `DirectoryLayer.jsx`. **Brief vs. table discrepancy surfaced**: brief headline says "3,328 new dots" but the dotCount column actually sums to 3,288; implementation matches the canonical table. Also added a `'Connectors & Backshells'` lexicon to fill the gap between Item 1 (row 20, zenith-components) and Item 2 (vertical lexicons table). **Known regressions (filed as 16.2.6.1 candidates)**: (a) four super-jumbo clusters overflow the sunflower placement loop (Atlas 124/450, Polaris 120/400, Helios 261/500, Vortex 297/350) — Voronoi cells too small for assigned dot counts at this scale; (b) Lloyd's residual still 101.4 wu (≈ 2.1 × DOT_GRID = 48) at the bumped cap. Both surface via `console.warn` per the brief's "stop and surface, don't raise the cap unilaterally" rule. Phase 16.1.4 lifecycle + Phase 16.1.5 InstancedMesh defensive settings + Phase 16.2.3 camera-init NaN guard preserved. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.5.1 (prior phase):** Smooth cluster dot geometry. One-line bump: cluster-dot `CircleGeometry` segment count 16 → 64 in `DirectoryLayer.jsx`. With Phase 16.2.5's `DOT_RADIUS = 20.4` (7× pre-16.2.5), 16-sided dots were visibly polygonal at zoom > ~100%. 64 segments produces a perceptually smooth circle at every supported zoom up to MAX_ZOOM = 4.0. Per-frame cost negligible. RFP hollow circles (separate code path) untouched. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.5 (prior phase):** Grid alignment + dot rendering hotfix. Three constant changes that reconcile cluster dot positions with the background dot matrix and make dots readable at default 15% zoom. **`DOT_GRID` 12 → 48** to match the background grid's actual world-space spacing (`GRID_SPACING` was `4×DOT_GRID = 48`); cluster sunflower-snapped positions now land on background-grid intersections at every zoom. **`DOT_RADIUS` 3 → `DOT_GRID × 0.425` (≈20.4 wu)** so dots fill ~85% of their grid cell. **`SUNFLOWER_SCALE` 1.7 → 1.0** so Vogel arm spacing matches grid. Combined: pixelated sunflower clusters read as solid filled discs around the centered label hole. Derived constants scale automatically (`LABEL_HOLE_W = 288`, `LABEL_HOLE_H = 144`, `INTER_CLUSTER_BUFFER = 96`, `LLOYD_CONVERGENCE_DELTA = 48`). Lloyd's residual at the 12-actor seed: 193.8 wu (≈4× new 48-wu threshold, was ≈16× old 12-wu threshold); per brief, accepted with `console.warn`. **Brief Item 2 caveat surfaced**: footprint actually grows ~2.4× because `DOT_GRID` scaled too, but net visual reads as denser since dot size grew 6.8× — intent preserved. Phase queue updated: 16.2.6 (seed expansion to ~3k dots) next, then 16.2.7 (Alice per-Claim grouping). Phase 16.1.4 + 16.1.5 + 16.2.3 defensive settings preserved. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.4 (prior phase):** Directory galactic view v2 (Voronoi packing + sunflower clusters). `DirectoryLayer.jsx` redesigned end-to-end. **Canvas** shrinks 17280×11170 → 11520×7447 (MBP 16" at 15% zoom). `MIN_ZOOM` / `INITIAL_ZOOM` 0.1 → 0.15. **Voronoi packing**: Lloyd-iterated centroidal tessellation replaces polar Poisson disc; active Actor's seed pinned at bottom-center anchor (index 0); cells target area ∝ dot count via `stepFactor = 0.5 + 0.5*tanh(area_error)`. Iteration cap 10. **Sunflower clusters**: Vogel phyllotaxis around each Voronoi cell centroid with a reserved 6×3 cell `LABEL_HOLE` at the center. Inter-cluster buffer of `2 × DOT_GRID` enforced during dot acceptance. Umbrella items placed first so they occupy inner spiral arcs. **Centered Actor label**: PillboxLabel moved from above-grid to cluster center via `transform: translate(-50%, -50%)`. Three.js Actor-squares mesh kept (lifecycle symmetry) but rendered with `count=0`. **Umbrella outline**: convex hull of umbrella dots (Andrew's monotone-chain) offset outward by 1 DOT_GRID; amber stroke + 8% amber fill — reverts Phase 16.1.3 grey treatment per brief. L-shape rectangular construction removed. **Loading animation v2**: `playDirectoryLoadAnimation` extended with `umbrellaOutlines` + `setUmbrellaOpacity`; outlines fade 0 → 1 in sync with their cluster label. **d3-delaunay** added as dependency (`^6.0.4`). **Lloyd's convergence caveat**: at the 12-actor seed, max displacement after 10 iterations is ~194 wu (above 12-wu threshold); per brief, accept + `console.warn` rather than unilaterally raising the cap. Phase 16.2.5 (seed expansion to ~3k dots) is next in queue; 16.2.6 (Alice per-Claim grouping) follows. Phase 16.1.4 + 16.1.5 defensive settings + camera-init NaN guard preserved. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.3 (prior phase):** Directory galactic view + loading animation. `DirectoryLayer.jsx` rewritten to use a bounded 17280×11170 design surface (matching 16" MBP logical resolution at 10% zoom) + radial polar Poisson disc fan-out + radial wave loading animation. **Canvas + zoom**: `MIN_ZOOM` 0.5 → 0.1; `INITIAL_ZOOM` set to 0.1; on every Directory entry (initial mount + role switch) camera resets to `(CANVAS_WIDTH/2, CANVAS_HEIGHT/2)` at zoom 0.1, showing the full canvas in an MBP viewport. Zoom-percentage display maps zoom × 100 directly. **Anchor** at `(8640, 8936)` — canvas-bottom-center, 20% up from the bottom edge; Carol (anonymous, no own cluster) still uses the same anchor as the polar origin. **Polar fan-out** replaces the Phase 16.2 deterministic-bbox-overlap loop: each non-active cluster picks (θ, r) via Poisson disc sampling — θ ∈ [-75°, +75°] from straight up, r ∈ [2000, ~8377]. Sort by descending Claim count; 50 retries with seed-perturbed samples; 12-cell buffer + canvas-bounds check. **Pan-bounds recompute per zoom** so at 0.1 the bounds collapse to a single point and at higher zoom they open up without revealing void beyond the canvas. **FIT** recalculates zoom from `min(viewport.w/CANVAS_WIDTH, viewport.h/CANVAS_HEIGHT)`. **Loading animation** (new helper `src/v2/directoryLoadAnimation.js`): cluster dots fade in via a radial wave emanating from the anchor on every entry (~3s total); per-dot t_start = d / waveSpeed (3000 wu/s); ramp opacity 0 → 1 over 200ms; labels fade in 100ms after first dot in their cluster. Dot opacity is realised via per-instance color multiplication on the existing InstancedMesh. Click on empty canvas during animation calls `skip()` and snaps to 1.0; dot clicks open Detail Panel normally without skipping; pan/zoom run independently. **#196 closed** — superseded by the polar Poisson disc fan-out. **Camera-init NaN guard** added: `updateCamera` bails when container width/height ≤ 0, preventing zero-frustum NaN from propagating through `worldToScreen` and breaking HTML overlay positioning. Phase 16.1.5 + 16.1.4 defensive settings intact. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.2 (prior phase):** Parent canvas node spreading. Refactor of `buildV22Canvas` in `src/v2/v2_2Data.js` (the parent-canvas layout function). **Three coordinated changes**: (1) **chain-row coalescing**: each evaluation chain (one row per `chainOrigin`) is now y-anchored to its evaluator's `EA.granteeAssetId` Asset on canvas via a two-pass allocator (pass 1 — first chain at each anchor takes that anchor's y exactly; pass 2 — subsequent chains pick nearest non-colliding y via symmetric outward search). Chain successors (re-runs) share their origin's y so a chain reads as a single horizontal row. (2) **Increased grantee-direction x-gap**: `COL_PULLED_CLAIM` bumped 2100 → 2400 (downstream constants bumped similarly) so the gap between rightmost chain node and the pulled Claim is 400 world units — comfortably above the brief's ≥240 minimum. (3) **PoE column order on grantor-direction views**: `proofOfEvalPulledPoEs` moved from "between Claim and ER" (100-unit gap to Claim) to "right of ER" (matching `Claim → ER → PoE → counterparty Asset` order). **Architectural deviation surfaced**: both Bob's evaluations carry `granteeAssetId: bAvionics` in the seed, so the brief's "bAvionics for PRM eval, bThermal for VReg eval" example doesn't hold — strict per-chain anchor alignment is impossible when chains share an anchor; the two-pass allocator stacks them symmetrically. Zero node-overlap pairs across all four role views verified. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2.1 (prior phase):** `aVregTestReport` ownership DA bugfix. One-line fix in `buildV22SharedArtifacts` adding `aVregTestReport` to the `aliceOwnAssets` array. Phase 15.4 promoted the Asset to an unattached "floating" entry that Alice attaches to her VReg Claim during the Re-Run demo prereq, but omitted the Actor→Asset ownership DA. Result: the Asset rendered floating on Alice's parent canvas with no Full Disclosure edge to the MicroCo Actor card. New internal DA `da-own-asset-vreg-test-report` (grantor + grantee both MicroCo, subject `{kind:'asset', id: aVregTestReport.id}`, type: 'full'). Disclosure-agreements grows by exactly 1. Re-Run demo prereq flow unchanged — ownership and Claim-reference are orthogonal; Alice still amends VReg Claim to add `aVregTestReport` as a referenced Asset later. Footer stays at v0.16.2.0 per backtrack-hotfix convention.

- **Phase 16.2 (prior phase):** Directory Layer seed expansion. Round 17 opens. The Directory now hosts **12 new mock supplier Actors** representing the defense-electronics supply chain underneath Bob's Sentinel-4 satellite program: NovaFab (25 Claims), ElectroGrid (24), Precision Components (18), AvionicSys (17), Substrate Dynamics (16), Helix RF (11), Optech Sensors (10), SolarVantage (10), ThermaCore (9), CompoStruct (9), Photonix (4), Cryotek (4). Total: **157 new Claims, 157 stub Assets, and 628 new internal + public DAs** (1 own-asset + 1 own-claim + 1 claim-ref-asset + 1 public per Claim). Every new Actor is non-switchable (`user: null`, `credits: 0`) and discloses exclusively to the Radiant Network — **no umbrella DAs** to any of the four primary actors. The four existing actor seeds (Bob/GovCo, Alice/MicroCo, Carol/AuditCo, Dave/ChipCo) are frozen — none of their Claims, Assets, DAs, EAs, RFPs, PoEs, Eval Results, or Parse Results were modified. New exported helper `seedMockSupplierActor({ id, party, vertical, claimSpecs, baseDate })` in `v2_2Data.js` produces an Actor + Claims + 1 stub Asset per Claim + ownership DAs + public DAs in one call; a companion `pickDirectoryType(i, total)` interleaver assigns disclosure types per Claim targeting ~60% full / 25% selective / 15% proofonly so every cluster paints a visible indigo + amber + green mix (with a forced fixed pattern for n ≤ 4 to keep Photonix and Cryotek non-monotone). **DirectoryLayer placement-scaling patch**: the initial cluster x-spread band scales with cluster count `N` (`xSpread = Math.max(800, 600 × (N − 1))`) instead of the previous hardcoded 800-px band. With 14 non-own clusters on Bob's view the initial band is ~7800 px — comfortably enough that the 30-attempt up-zigzag retry no longer needs to compensate. Force-directed layout (#196) remains deferred. Phase 16.1.5's defensive `frustumCulled = false` + unbounded `boundingSphere` settings on every `InstancedMesh` left intact; Phase 16.1.4's `shouldMountScene` lifecycle pattern left intact.

- **Phase 16.1.5 (prior phase):** Hotfix: frustum culling + raycast bounding-sphere fix. Shared root cause for two QA issues from Phase 16.1.4 — `THREE.InstancedMesh.boundingSphere` auto-computed from underlying geometry vertices (which sit at world origin because instance positions live in per-instance matrices), so the cached sphere is tiny (radius = `DOT_RADIUS = 3`, centred at origin). Both the renderer's frustum check and the raycast pre-filter read this cached sphere — the tiny origin-sphere fell outside the camera frustum at high zoom and outside click rays at any zoom. Fix: on each InstancedMesh (dots + Actor squares + RFP), `frustumCulled = false` + `boundingSphere = new THREE.Sphere(origin, Infinity)`.

- **Phase 16.1.4 (prior phase):** Hotfix: scene-init `useEffect` declared `phase` in deps → cleanup fired on every internal phase transition (`opening → in`, `in → out`) → disposed populated InstancedMesh mid-flight; the downstream `useLayoutEffect` didn't re-run because React batched the false/true `threeReady` toggle into a single update with no net change. Fix: derive a stable `shouldMountScene = phase !== 'closed'` boolean and depend on that. Scene-init now runs setup only on `closed → non-closed` and cleanup only on `non-closed → closed`.

- **Phase 16.1.3 (prior phase):** Directory parent-parity fixes + disclosure-type-based dot colors. Nine items including: dot lifecycle hardened via `useLayoutEffect`; Actor square as hollow `ShapeGeometry` mesh (border scales with zoom); squareCell reserved in cluster layout (no overlap); zoom controls top:73; Detail Panel `bottom:28`; click pans camera to centre the dot with panel-aware offset; Request EA modal close stays on Directory; dot colors map to disclosure TYPE (full→indigo, selective→amber, proof-only→green) with neutral grey L-shape umbrella boundary; RFP as cyan hollow circle.

- **Phase 16.1.2 (prior phase):** Spatial model rewrite. Corner card removed; user's own cluster anchored at canvas-horizontal-center + bottom-third vertically; new `isUserVisible` flag for anonymous-actor case (Carol/AuditCo); Actor squares in Three.js; all edges dropped from Directory; 12-cell buffer between cluster bboxes; InstancedMesh persists across data changes. Filed #196 (force-directed layout, deferred).

- **Phase 16.1.1 (prior phase):** Three.js Directory hotfix — eleven QA fixes on top of 16.1.0 including wheel-zoom binding, sync render after InstancedMesh attach, 1-cell buffer around umbrella subset, cluster vertically centered on Actor square.

- **Phase 16.1.0 (prior phase):** Three.js migration. DirectoryLayer's rendering pipeline migrated from HTML/CSS dots + CSS-tiled background to Three.js scene + InstancedMesh dots + Points grid + scroll-zoom + drag-pan. Pan via drag, zoom via wheel (zoom-around-cursor); MIN_ZOOM=0.5, MAX_ZOOM=4.0. Three.js scope kept narrow (only dots + grid); HTML/SVG preserved for tooltip card, label pillboxes, umbrella borders. Filed #195 (Three.js edge migration, deferred).

- **Phase 16.0.x (prior phases):** 16.0 — Directory Layer foundations replacing Phase 7/11A/11B placeholder scaffolding (new visual model: dot matrix background, per-Actor clusters, hollow indigo Actor squares, per-role view filtering, `buildV22DirectoryDataForRole` view-builder, Dave/ChipCo catalog grown 2→14 Claims, skeletal `makeRfp` factory). 16.0.1, 16.0.2, 16.0.3 — layout polish + PDF.js worker fix + duplicate React key fix + curved Bezier umbrella edges + footer z-index bump. Closed #43 + #45. Filed #193, #194.

- **Active phase queue:** Round 19 in progress — **Phase 18.3.1.1 shipped** (Directory action-bar: hide Issue Badge, surface Reject Solicitation — two surgical fixes closing 18.3.1 QA #8. (Fix 1) The non-owner Issue Badge `buttons.push` in AssetNode's CLAIM branch was unconditional → appeared on every Directory Claim card and no-op'd on click (Issue Badge's `onClaimCardAction` dispatch isn't wired for Directory synthetic nodes). Gated behind a new `isDirectoryMountedClaim` check keyed on the Directory stamp family (`_directoryWarmPathRequestCandidate` / `_directoryExistingEa` / `_directoryRequestEaCandidate` / `_solicitationContext`); parent-canvas non-owner Claims keep Issue Badge. (Fix 2) 18.3.1 Deviation #2 left the Directory CARD synthetic node un-stamped with `_solicitationContext`, so the Reject icon never reached the solicited Claim's dot — added a parallel inline stamp at the DirectoryLayer card adapter (array-form `solicitations.find`, importing from V2App would be a wrong-direction dep) + a `rejectSolicitation` branch in V2App's `onClaimCardAction` dispatcher (the Directory card routes through `onClaimCardAction`, not the parent-canvas `onV22CardAction` switch — without the branch the new button would be a dead affordance, the exact bug Fix 1 closes). Reject parity restored at the Directory card surface. Build clean (132 modules, 0 errors); app boots clean — footer reads v0.18.3.1.1. The Directory-card rendering + Reject cascade are canvas-raycaster-gated + need a live solicitation → flagged for a manual pass. Footer rolls forward to v0.18.3.1.1). This sits on top of **Phase 18.3.1** (Bob-side solicitation UI: clickable rows, context-aware Claim panel, Reject cascade — six surface-level parts reusing Phase 18.3 primitives. (A) `SolicitationCard` redesigned — actor row + dead `ActorPill` dropped, title = Claim name + "from {owner}" subtitle, `MessageBlock` indigo edge removed, card body clickable (both roles, 4%-indigo hover) routing to the offered Claim. (B) New `handleSelectSolicitation` → existing `routeToPublishedClaim(claimId)`, threaded through RfpDetailPanel to both card mounts (declared after `routeToPublishedClaim` to avoid a deps TDZ — NOT after `handleRejectSolicitation` as the prompt's stale line numbers said). (C) New module-scoped `computeSolicitationContext` stamps `_solicitationContext` on Claim nodes at both mount paths (`v22DataWithReveal` parent + Directory `syntheticNode`) when a pending solicitation-DA targets the viewer. (D) V22ClaimPanel footer gains an additive `✕` Reject `FooterButton`; warm-path Request EA still renders alongside. (E) V22ActionBar CLAIM branch gains an additive `✕` Reject icon + `rejectSolicitation` dispatch — footer↔action-bar parity. (F) `v22-rfp-solicitation-received` notification clicks route to the offered Claim (Directory → Claim panel); the four other RFP notification types still route to the RFP marker. Reject reuses Phase 18.3's `handleRejectSolicitation` cascade-revoke; Request EA reuses the Phase 11C warm-path by structural identity. Build clean (132 modules, 0 errors); app boots clean — parent canvas renders + footer v0.18.3.1 — exercising the modified `v22DataWithReveal` hot-path memo without crash. The redesigned card + Reject footer/action-bar + notification round trip need a live solicitation (canvas-raycaster-gated Directory RFP-marker → SolicitationCreateModal flow) → flagged for a manual pass. Footer rolls forward to v0.18.3.1). This sits on top of **Phase 18.3.0.2** (Self-eval orphan check signal swap — replaced 18.3.0.1's dead `er.evaluatorParty === claim.owner` self-eval signal with `er.evaluationAgreementId.startsWith('self-eval-')`. `evaluatorParty` isn't preserved on the output Eval Result artifact by `makeEvaluationRunArtifacts` (stored as `owner`/`evaluator`, not `evaluatorParty`; no `er.evaluatorParty` read sites exist in V2.2), so the bypass returned false against undefined and never fired → self-eval Eval Results still showed the false "Orphaned Evaluation Result" banner. The new prefix check is unique (`V2App:1913` is the sole emitter of `self-eval-`) + complete + operates on a guaranteed-populated field; strict relaxation, so cross-party orphan cases still flag. Restores Re-Run + Create PoE to the Eval Result footer (footer↔action-bar parity) with no V22NodeDetailPanel edit. Verified via `git diff` + clean build; the live self-eval footer walkthrough + the cross-party orphan regression are canvas-gated → flagged for a manual pass. Footer rolls forward to v0.18.3.0.2). This sits on top of **Phase 18.3.0.1** (Solicitation modal polish + self-eval orphan fix — five items from 18.3 QA. (1) Modal title → "Solicit '{rfp.name}' with my Claim". (2) "Step N of M" pill gets `whiteSpace: nowrap` + `flexShrink: 0` in ModalShared so it never wraps under a long title. (3) Submit-lag → backlog #206, no code. (4) Step 1 Claim rows show three counts — referenced assets • parse results • PoEs — from the props V2App already threads, so the user can see which Claims support Selective/Proof-Only before Step 2 (zero counts shown, not suppressed). (5) Self-eval Eval Results no longer falsely flagged orphaned — the synthetic self-eval EA is unpersisted, so the V2App `isOrphaned` predicate now bypasses when `er.evaluatorParty === claim.owner` (a strict relaxation), restoring Re-Run + Create PoE to the Eval Result footer to match the action bar. Runtime-verified the modal title + inline step pill + the three-count subtitle; the orphan bypass is a strict-relaxation predicate edit. Footer rolls forward to v0.18.3.0.1). This sits on top of **Phase 18.3** (Solicitations carry a Disclosure Agreement — architecture + creation flow. Each RFP solicitation now mints a paired DA (solicitor → RFP owner, scoped to the offered Claim) atomically, so the owner gets real visibility instead of just a message + reference. **(A)** `makeRfpSolicitation` gains a `disclosureAgreementId` link-back; the view-builder override + cluster inclusion are **already delivered by existing infrastructure** — a solicitation DA is structurally a directed/umbrella claim DA, so the existing umbrella pass pulls the offered Claim into the solicitor's cluster + party-scoping suppresses any public DA (no redundant directed-DA passes added). **(B)** `SolicitationCreateModal` restructured into a **4-step flow** (Standards + Claim → Disclosure Type → Scope + Expiry + Message → Review) mirroring CombinedResponseModal's `directoryPublishMode` primitives. **(C)** `handleCreateSolicitation` mints the DA atomically into `v22Provisionals` alongside the solicitation; a shared `revokeSolicitationDa` helper cascade-revokes the linked DA on reject AND on RFP close. Runtime-verified the full 4-step modal + the atomic mint (Alice's EMI Shield panel shows "Full Disclosure with GovCo" after submit) + data-probed the cascade (active DA present in Bob's view, revoked one absent). **Seed caveat:** every Claim is already public in the seed, so the cluster appear/disappear effect isn't observable for seeded Claims — the verified effects are the atomic mint, the directed scope, and the cascade. Owner-side UI lands in **Phase 18.3.1**). This sits on top of **Phase 18.2.3** (Post-publish orchestration + already-published gate + Parse Asset rename — three independent items. **(A)** Publishing a Claim now auto-routes Parent → Directory, pans/zooms the new dot to 60%, opens its Detail Panel, and stamps a NEW badge until you navigate away — new `routeToPublishedClaim` callback (mirror of `routeToRfp`) + DirectoryLayer imperative `selectClaim` + `focusClaimInternal` helper + `_isNew` stamp on the synthetic Claim card + a clear-on-deselect effect. **(B)** The Publish to Directory action is **disabled with an explanatory tooltip** on Claims that already have an active Radiant Network DA — `v22DataWithReveal` stamps `_publishedToDirectory` (predicate mirrors §18.2.1.1), read by both the V22ClaimPanel footer button + the V22ActionBar CLAIM-branch button (`ActionButton` gained `disabled` support); clears reactively on revoke; defensive dispatch no-op gate added. **(C)** Parse modal "Parse Evidence" → "Parse Asset" across the title + entry-point labels (dispatch action name `parseEvidence` unchanged); `AssetFileViewer` now shows a "No preview available." placeholder when an Asset's `localPath` resolves to the SPA `index.html` fallback (missing physical PDF — `onLoad` content-sniff for `#root`/the app title + an `onError` handler) instead of rendering the whole app inside the iframe. Runtime-verified Alice/MicroCo: (A) PCB Stackup publish auto-transitions to Directory + opens its panel with the new "with Radiant Network" row (60% camera + NEW-badge-on-card rAF/Three-gated → flagged); (B) footer + action-bar Publish disabled+tooltip on published PRM Assembly, enabled on unpublished PCB Stackup, flag correct across all 8 Alice Claims; (C) "Parse Asset" title + missing-PDF RF Filter → "No preview available", real-PDF PRM Datasheet → iframe renders; build clean, zero console errors). This sits on top of **Phase 18.2.2** (Retire `PublishToDirectoryModal`, reuse `CombinedResponseModal` — the bespoke publish modal from 18.2 is **deleted**; the Publish-to-Directory flow now runs through `CombinedResponseModal` via a new `directoryPublishMode` prop that branches the existing 4-step flow into 3 steps (Type → Scope → Review): `totalSteps` short-circuits to `3`, the Step 1 grid drops the Decline card (4 → 3 cols), the Step 3 EA-Terms step is gated out, the Step 4 review gate moves to `step === totalSteps` and hides the EA-Expires + Acknowledgments rows, the banner/header/scope copy go publish-specific, and `handleSubmit` fires DA-only `onAccept({ type, scope, daTerms: { expires } })`. V2App swaps the mount to `CombinedResponseModal directoryPublishMode={true}` with a synthesized `request` + the cold-path `referencedAssets`/`parseResults`/`poesForClaim` contract, and renames the handler payload field `expiryIso` → `daTerms.expires`. Parity-by-construction with the responder flow), and **Phase 18.2.1.1** (V2App PoE picker filter hotfix — single-symbol fix at two parallel V2App modal-mount sites: both the `CombinedResponseModal` Proof-Only step (`poesForClaim`) and the `AmendDisclosureModal` Proof-Only scope picker (`candidatePoEs`) pre-filtered PoEs with `poe.claimId === <claim> && poe.owner === da.grantor.party`. The owner clause was wrong — a proof-only Claim DA's grantor right derives from Claim ownership, not PoE ownership, so any PoE visible to the grantor on that Claim (incl. third-party-authored ones, the common evaluator→claim-owner case) is a valid scope candidate. The clause hid every third-party PoE (surfaced by 18.2.1 QA: Carol's PoE on Alice's EMI Shield Claim showed the empty state). Fixed by dropping the clause at both sites (Claim-membership-only filter) — matching the seed precedent + Phase 18.2's correctly-wired `PublishToDirectoryModal`. Strict superset of the old filter, so self-eval PoEs still match. No view-builder/modal/data-model changes), and **Phase 18.2.1** (CombinedResponseModal hotfix — two surgical fixes surfaced during 18.2 QA, both predating 18.2: **Bug 1 (P0 crash)** — the cold-path `totalSteps` formula collapsed to `1` at Step 1 before a disclosure type was picked, rendering an ungated final-step "Accept" that crashed V2App's accept handler with null `type`/`scope`; fixed by hardcoding the non-decline cold path to `4` so StepDots reads "Step 1 of 4" + a disabled "Continue" renders until a type is picked. **Bug 2 (copy)** — the Step 2 Proof-Only branch's stale "Evaluation Results" copy → "Proofs of Evaluation" across the FieldLabel, description, empty-state copy+styling (re-matched to the neutral Selective empty state), and the Step 4 review row label. No data-model/handler/prop changes. **Note:** the prompt for 18.2.1 re-scoped the slot from the originally-predicted publish-to-Directory post-creation orchestration to this hotfix; the publish post-creation orchestration is now an un-numbered deferred follow-up), and **Phase 18.2** (Publish Claims to the Public Directory — foundation: restores the V2.0/V2.1 publish-to-Radiant-Network mechanic. New two-step `PublishToDirectoryModal` (Form → Review) on the V22ClaimPanel footer + V22ActionBar CLAIM branch (owner-only): pick disclosure type (Full / Selective / Proof-Only — the latter disabled until the Claim has a PoE), scope, expiry; Submit mints a DA with `grantee = Radiant Network` and pushes it into `v22Provisionals.disclosureAgreements`, where the existing Directory builder's public-DA detection surfaces the Claim as a dot in the owner's cluster — no new merge helper or builder signature change. **Foundation only** — post-creation orchestration deferred), and **Phase 18.1** (Placeholder RELEASE action on Internal Disclosure Agreement rows: every Internal DA row in the shared `<AgreementsSection>` now renders a muted, disabled `RELEASE` `ActionLabel` in the AMEND slot of `DisclosureAgreementRow`, propagating across Actor / Asset / Claim / Eval Result / Parse Result panels via the single shared component. New module-scoped `DA_SUBJECT_KIND_LABEL` map drives a subject-aware tooltip ("Releasing your disclosure agreement to this [Asset/Claim/Evaluation Result/Parse Result/Proof of Evaluation] will remove it from your network. Coming soon.") resolved from the DA's own `subject.kind`, not the panel node type. Disabled-only surface — no handler, no data-model changes; previews the future "release this artifact from my network" mechanism), and **Phase 18.0.1** (Hotfix: sticky edge tooltip on Parent → Directory transition — closes #203, a demo-blocking bug. V2Canvas's `onMouseMove`-bound `handleEdgeHover` had no Directory-open guard, so post-`clearEdgeUiState` mousemoves re-raycasted parent edges and re-set V2App's `edgeHover`, ghosting the "SELECT EDGE TO VIEW" tooltip over the Directory. Fix: new `directoryOpen` prop on V2Canvas with early-return guards in `handleEdgeHover`/`handleBackgroundClick` + a hover-state-clear `useEffect` on the open flip; V2App passes `directoryOpen={v22DirectoryOpen}`, gates both `EdgeHoverMenu` mounts on `!v22DirectoryOpen`, and adds `setEdgeMenuPanning(false)` to `clearEdgeUiState`), and **Phase 18.0** (Demo prep quick wins — the first demo-prep pass: parent-canvas dot selection ring re-centered on `AssetNodeDot`; `V22AssetPanel` "View File" button unified across owner + non-owner branches; #35 closed — new Amend-Claim Asset-reference edges now draw in via `_isNew`/`_createdAt` on the claim-ref DAs + a reveal-flag clear-timer; #116 closed — Agreements section extended to Eval Result + Parse Result Detail Panels). **Phase 17's RFP creation arc is fully CLOSED; Phase 18 is the demo-prep umbrella** for items leading up to the demo walkthrough recording. Phase 18.0 sits on top of **Phase 17.5.2.2** (RFP arc post-closure cleanup: fixed the Request Agreement regression on user-created RFPs — `handleRequestAgreement` now resolves the RFP against the full created→closed→removed merge chain; RFP notification clicks now pan/zoom to 60% via a new `selectRfp` `opts.zoom` override — full-card LOD reserved for the deliberate-focus post-creation/Anchored-row paths; Reopen now fires a `v22-rfp-reopened` notification to every unique solicitor — REVERSING the 17.5.1.4 "Reopen is silent" decision; Directory zoom controls shift left when a Detail Panel opens via a new `detailPanelOpen` prop), **Phase 17.5.2.1** (RFP arc closing polish: fixed the tab-crash regression on the Anchored RFPs row click — `routeToRfp`'s rAF select-retry chain is now cancellable + cancelled before re-invocation, so concurrent chains can't stack and saturate the main thread; smooth Directory → Parent transition — the camera eases outward to ~60% over the reverse wipe via a `phase === 'out'` effect, on every close path; Anchored RFPs row actions are text-only with REOPEN+REMOVE stacked vertically like the AMEND/REVOKE EA rows; ASSET + ACTOR type badges added to their Detail Panel headers; Changelog modal header reworded to "Changelog" / "Radiant v2.2"), and **Phase 17.5.2** (post-creation orchestration + bidirectional Parent↔Directory navigation: creating an RFP auto-routes Parent → Directory + zooms to the new marker at full-card LOD + opens its panel + shows a NEW badge; the RfpDetailPanel "For Asset" row click-navigates back to the Asset on Parent via `routeToAsset`; double-clicking empty Directory space returns to Parent via the reverse wipe), and **Phase 17.5.1.5** (Asset Detail Panel "Anchored RFPs" section: V22AssetPanel lists every RFP anchored to the Asset — newest-first from the full merge chain — with click-to-navigate via the reusable `routeToRfp` helper + owner-only inline Close/Reopen/Remove `ActionLabel`s reusing the 17.5.1.4 handlers; the parent-canvas surface for managing Directory-layer-only RFPs), **Phase 17.5.1.4** (RFP lifecycle expansion: Close fires a `v22-rfp-closed` notification to every unique solicitor; new Remove RFP action — closed-only — silently deletes the RFP + cascades its solicitations via `mergeRemovedRfps`; Detail Panel Close/Reopen button refreshes in-place via the created+removed merge chain; Create RFP modal placeholder dimmed), **Phase 17.5.1.3** (solicitation Submit regression fixed for user-created RFPs — `handleCreateSolicitation` now threads `mergeCreatedRfps` into its RFP lookup; RequirementsSetDetailModal subheader branches published vs unpublished — globe + "Published by" only when published; closed-RFP Directory marker rebuilt as a thick muted-indigo dashed ring matching the open ring's stroke thickness), **Phase 17.5.1.2** (created RFPs integrate into the owner's existing Directory cluster — single label per owner — via `buildV22DirectoryDataForRole` upserting RFP-only clusters for every owner incl. primary parties, removing the `PRIMARY_PARTIES_FOR_ORPHAN_RFP` carve-out; RS chip click resolves against `v22RfpRsLookupPool`; two-line RS rows in RfpDetailPanel with globe + owner), **Phase 17.5.1.1** (cross-actor RFP visibility via orphan-RFP position spreading; implicit-publication-via-reference RS resolution pool for RfpDetailPanel; Your-first RS picker ordering with own-published globe+tooltip), **Phase 17.5.1** (Create RFP flow: two-step `RfpCreationModal` → `makeRfp` → `v22CreatedRfps` → owner's Directory cluster via `mergeCreatedRfps`), **Phase 17.5** (RFP creation entry-point foundation), and the 17.5.0.x series (17.5.0.1 Asset footer polish, 17.5.0.2 cross-panel icon propagation + Create PoE visible-but-disabled, 17.5.0.3 universal neutral footer buttons, 17.5.0.4 disclosure boundary cleanup) — the user-facing RFP creation flow (third leg of the Directory triad: browse + connect + create). **Deferred follow-up:** `RfpDetailPanel` (`ActionButton`) + DA/EA agreement panels use their own footer button components, not `FooterButton` — their footer-icon migration was flagged out of scope in 17.5.0.2. **The RFP creation arc is now fully CLOSED** (17.5 → 17.5.0.x → 17.5.1 → 17.5.1.x → 17.5.2 → 17.5.2.1 → 17.5.2.2; polish-backlog #192 Item J fully closed; no open follow-ups). **Next up:** continued demo-prep polish under the Phase 18 umbrella, and the next major work item from the deferred queue. **The publish-to-Directory arc (18.2 → 18.2.1 → 18.2.1.1 → 18.2.2 → 18.2.3) is functionally complete** (entry points + modal consolidation + already-published gate + post-publish orchestration); **the solicitation-DA arc (18.3 architecture + creation flow → 18.3.1 owner-side UI) is now functionally complete** (creation + atomic DA mint + owner-side clickable rows + context-aware Reject + notification routing). **Phase 18.3.1 manual-QA follow-ups** (canvas-raycaster-gated — need a live solicitation created via the Directory RFP-marker → SolicitationCreateModal flow): redesigned SolicitationCard layout + hover/click-to-navigate (both viewer roles); Bob's Claim panel footer + action-bar showing Request EA + Reject when `_solicitationContext` is stamped; Reject cascade (DA revoked + solicitation rejected + solicitor notified) from both footer + action bar; `v22-rfp-solicitation-received` notification routing to the offered Claim (not the RFP); the four other RFP notification types still routing to the RFP; the post-acceptance Reject-button-disappears regression; and the non-solicitation-Claim no-Reject regression smoke. **18.3 / 18.3.0.1 manual-QA follow-ups:** the live owner-side Reject walkthrough (canvas-gated; 18.3.1 has now redesigned the reject entry surface); the 18.3.0.1/18.3.0.2 self-eval footer walkthrough (run a self-eval → confirm no orphan banner + footer shows Re-Run + Create PoE + Create PoE works end-to-end) + the genuine-orphan regression (revoked cross-party EA still flagged orphaned). **Manual-QA follow-ups** (preserved-by-construction / rAF-gated, not live-confirmed in-session): the 18.2.2 cold-path response modal regression (4 cards / 4 steps / EA-Expires + Acknowledgments rows) + EA-only warm path + Decline path (gated behind `isDirectoryPublish`, no seeded `v22-request`); the 18.2.1 cold-path Bug 1 live confirmation; the 18.2.3 60%-camera-landing + NEW-badge-on-card visual + Selective/Proof-Only publish orchestration parity + revoke→re-enable; and the 18.2 Selective-publish + Directory-dot paths. Then continued demo-prep polish under the Phase 18 umbrella, and the next major work item from the deferred queue (Directory Layer enhancements, AI Shopper, Cascading Disclosures, Detail Panel content cleanup, Netgraph cleanup, etc.). Seed-resilience backlog carry-overs **#204** (extract `makeOwnershipDAsForActor`) + **#205** (tidy duplicate `da-ref`/`da-parse` ids) remain open from the 17.4.5 post-mortem. After 17.5: **Phase 17.6+** — solicitation withdraw / amend, multi-Claim solicitations, solicitor-side "open the resulting EA" deep-link via `acceptedEaId`. **Deferred (still open):** parent-layer EA-required-message-when-not-needed bug — Andrew can't recall the symptom; deferred unless it resurfaces. Remaining Claim Detail Panel content items (#58, #104, #161, #180) can be folded into later detail-panel polish phases (#116 closed in Phase 18.0). Later: Cascading Disclosures (#26) → Netgraph cleanup (#4, #130) → Search/aggregate (#27) → Network Event Log (#30). See `ROUND-17-CONTEXT.md` for per-phase load-in lists. **Deferred from 16.2.11**: Asset-position-clustering-near-referencing-Claim on Alice's view — requires Asset-placement re-architecture, scoped to a future dedicated phase.

---

## Round 16 retrospective

Round 16 focused entirely on the Directory Layer. Started from Phase 7/11A/11B placeholder scaffolding; ended at a Three.js-rendered, pan-zoom-capable, disclosure-type-coloured Directory with stable lifecycle and clickable dots. Lessons worth carrying forward:

- **`THREE.InstancedMesh` requires defensive culling settings.** Set `frustumCulled = false` AND override `boundingSphere = new THREE.Sphere(origin, Infinity)` on every InstancedMesh. The auto-computed bounding sphere is geometry-derived (origin-centred, tiny) and breaks BOTH renderer frustum culling AND raycast pre-filter at high zoom or when instances are far from origin. (Phase 16.1.5 root cause.)

- **`useEffect` deps including phase state machine values cause render-disposing-mid-flight bugs.** When a scene-init effect declares `phase` in its dep array, internal phase transitions trigger cleanup + recreate cycles that wipe populated meshes. Cure: derive a stable boolean (`shouldMountScene = phase !== 'closed'`) and depend on that. The downstream useLayoutEffect's deps must also be stable or include refs that update on each render. (Phase 16.1.4 root cause.)

- **Hybrid Three.js + HTML/SVG architecture for the Directory Layer.** Three.js renders the scene, background grid (Points), dot InstancedMesh, Actor squares (hollow ShapeGeometry InstancedMesh), and RFP hollow circles. HTML/SVG renders: tooltip card, label pillboxes, L-shape umbrella borders, zoom controls, "← Back to Network" exit chip, "RADIANT NETWORK" header pillbox. HTML overlays are projected from world to screen coords via a `worldToScreen` helper that runs on every camera change. Mouse handlers are React JSX event props on the container `<div>`, bound via `useCallback` with full deps — NOT `addEventListener` calls inside a long-lived useEffect.

- **Commit at every phase boundary without exception.** Round 16 lost track of this when 16.0.3 → 16.1.0 surfaced 13 unpushed phases at catchup time. The current workflow: one Claude Code prompt = one phase = one commit, direct to main. The worktree feature is currently disabled.

- **Forward-progress phases roll the footer constant forward; backtrack-hotfix versions don't.** A backtrack-hotfix is a version INSERTED into the Changelog modal between existing entries — these leave the footer unchanged because the latest forward-progress phase number stays canonical. The Round 16 "footer frozen at v0.15.9" rule across the entire 16.0.x cycle was an over-application of this convention; 16.1.0 corrected it.

---

## Phase log

The full per-phase completion log lives in [`CLAUDE-phase-log.md`](./CLAUDE-phase-log.md). It contains every phase note from Phase 1 (V2.2 data model foundation, 2026-04-17) through Phase 17.2 (RFP solicitation flow, 2026-05-18), in chronological order.

The original V2.2 migration ran in eight phases:

- [x] Phase 1: Data Model Foundation
- [x] Phase 2: Parent Layer Restructure
- [x] Phase 3: Edge Clickability + Agreement Panels
- [x] Phase 4: Combined Request + Response Flows
- [x] Phase 5: Evaluation Flow + Eval Results on Parent Layer
- [x] Phase 6: Amendment Flows
- [x] Phase 7: Directory Layer + AI Shopper (superseded by Round 16)
- [x] Phase 8: Consolidation + Cleanup

Post-migration work shipped through Phases 9A → 9E-parallel.x, 9D.1 → 9D.2.x, 10.x, 11A → 11E.1, 12.x, 13.x, 14.x (Badges), 15.x (PDF annotation), and 16.x (Directory Layer Three.js + lifecycle + bounding-sphere fixes). See `CLAUDE-phase-log.md` for the full narrative.
